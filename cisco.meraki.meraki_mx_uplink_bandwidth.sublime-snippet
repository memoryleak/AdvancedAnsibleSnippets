<snippet>
  <content><![CDATA[- name: ${1:Manage uplinks on Meraki MX appliances}
  cisco.meraki.meraki_mx_uplink_bandwidth:
    state: ${2:# Specifies whether object should be queried, created/modified, or removed}
    org_name: ${3:# Name of organization associated to a network}
    org_id: ${4:# ID of organization associated to a network}
    net_name: ${5:# Name of network which VLAN is in or should be in}
    net_id: ${6:# ID of network which VLAN is in or should be in}
    wan1: ${7:# Configuration of WAN1 uplink}
    wan2: ${8:# Configuration of WAN2 uplink}
    cellular: ${9:# Configuration of cellular uplink}
    auth_key: ${10:# Authentication key provided by the dashboard}
    host: ${11:# Hostname for Meraki dashboard}
    use_proxy: ${12:# If C(no), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_https: ${13:# If C(no), it will use HTTP}
    output_format: ${14:# Instructs module whether response keys should be snake case (ex}
    output_level: ${15:# Set amount of debug output during module execution}
    timeout: ${16:# Time to timeout for HTTP requests}
    validate_certs: ${17:# Whether to validate HTTP certificates}
    rate_limit_retry_time: ${18:# Number of seconds to retry if rate limiter is triggered}
    internal_error_retry_time: ${19:# Number of seconds to retry if server returns an internal server error}
  tags:
    - cisco
    - meraki
    - meraki_mx_uplink_bandwidth]]></content>
  <tabTrigger>cisco.meraki.meraki_mx_uplink_bandwidth</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage uplinks on Meraki MX appliances</description>
</snippet>