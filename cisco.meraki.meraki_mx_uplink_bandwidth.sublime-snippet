<snippet>
  <content><![CDATA[- name: ${1:Manage uplinks on Meraki MX appliances}
  cisco.meraki.meraki_mx_uplink_bandwidth:
    auth_key: ${2:# Authentication key provided by the dashboard}
    cellular: ${3:# Configuration of cellular uplink}
    host: ${4:# Hostname for Meraki dashboard}
    internal_error_retry_time: ${5:# Number of seconds to retry if server returns an internal server error}
    net_id: ${6:# ID of network which VLAN is in or should be in}
    net_name: ${7:# Name of network which VLAN is in or should be in}
    org_id: ${8:# ID of organization associated to a network}
    org_name: ${9:# Name of organization associated to a network}
    output_format: ${10:# Instructs module whether response keys should be snake case (ex}
    output_level: ${11:# Set amount of debug output during module execution}
    rate_limit_retry_time: ${12:# Number of seconds to retry if rate limiter is triggered}
    state: ${13:# Specifies whether object should be queried, created/modified, or removed}
    timeout: ${14:# Time to timeout for HTTP requests}
    use_https: ${15:# If C(no), it will use HTTP}
    use_proxy: ${16:# If C(no), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    validate_certs: ${17:# Whether to validate HTTP certificates}
    wan1: ${18:# Configuration of WAN1 uplink}
    wan2: ${19:# Configuration of WAN2 uplink}
  tags:
    - cisco
    - meraki
    - meraki_mx_uplink_bandwidth]]></content>
  <tabTrigger>cisco.meraki.meraki_mx_uplink_bandwidth</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage uplinks on Meraki MX appliances</description>
</snippet>