<snippet>
  <content><![CDATA[- name: ${1:Boot system using specific media through HP iLO interface}
  community.general.hpilo_boot:
    force: ${2:# Whether to force a reboot (even when the system is already booted)}
    host: ${3:# The HP iLO hostname/address that is linked to the physical system}
    image: ${4:# The URL of a cdrom, floppy or usb boot media image in the form V(protocol://username:password@hostname:port/filename)}
    login: ${5:# The login name to authenticate to the HP iLO interface}
    media: ${6:# The boot media to boot the system from}
    password: ${7:# The password to authenticate to the HP iLO interface}
    ssl_version: ${8:# Change the ssl_version used}
    state: ${9:# The state of the boot media}
  tags:
    - community
    - general
    - hpilo_boot]]></content>
  <tabTrigger>community.general.hpilo_boot</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Boot system using specific media through HP iLO interface</description>
</snippet>