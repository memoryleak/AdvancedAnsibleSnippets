<snippet>
  <content><![CDATA[- name: ${1:Manage Azure SIG Image instance}
  azure.azcollection.azure_rm_galleryimage:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${7:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${8:# Azure client ID}
    cloud_environment: ${9:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    description: ${10:# The description of this gallery Image Definition resource}
    disallowed: ${11:# Disallowed parameter values}
    end_of_life_date: ${12:# The end of life date of the gallery Image Definition}
    eula: ${13:# The Eula agreement for the gallery Image Definition}
    gallery_name: ${14:# The name of the Shared Image Gallery in which the Image Definition is to be created}
    hypervgeneration: ${15:# This property allows you to specify the Hyper V Version of the Virtual Machines}
    identifier: ${16:# Image identifier}
    location: ${17:# Resource location}
    log_mode: ${18:# Parent argument}
    log_path: ${19:# Parent argument}
    name: ${20:# The name of the gallery Image Definition to be created or updated}
    os_state: ${21:# The allowed values for OS State are C(generalized)}
    os_type: ${22:# This property allows you to specify the type of the OS that is included in the disk when creating a VM from a managed image}
    password: ${23:# Active Directory user password}
    privacy_statement_uri: ${24:# The privacy statement uri}
    profile: ${25:# Security profile found in ~/}
    purchase_plan: ${26:# Purchase plan}
    recommended: ${27:# Recommended parameter values}
    release_note_uri: ${28:# The release note uri}
    resource_group: ${29:# The name of the resource group}
    secret: ${30:# Azure client secret}
    state: ${31:# Assert the state of the GalleryImage}
    subscription_id: ${32:# Your Azure subscription Id}
    tags: ${33:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${34:# Azure tenant ID}
    thumbprint: ${35:# The thumbprint of the private key specified in I(x509_certificate_path)}
    x509_certificate_path: ${36:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_galleryimage]]></content>
  <tabTrigger>azure.azcollection.azure_rm_galleryimage</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure SIG Image instance</description>
</snippet>