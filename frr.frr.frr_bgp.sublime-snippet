<snippet>
  <content><![CDATA[- name: ${1:Configure global BGP settings on Free Range Routing(FRR)}
  frr.frr.frr_bgp:
    config: ${2:# Specifies the BGP related configuration}
    operation: ${3:# Specifies the operation to be performed on the BGP process configured on the device}
  tags:
    - frr
    - frr
    - frr_bgp]]></content>
  <tabTrigger>frr.frr.frr_bgp</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configure global BGP settings on Free Range Routing(FRR)</description>
</snippet>