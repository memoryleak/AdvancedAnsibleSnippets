<snippet>
  <content><![CDATA[- name: ${1:NetApp ONTAP NTFS file security permissions}
  netapp.ontap.na_ontap_file_security_permissions:
    access_control: ${2:# An Access Control Level specifies the access control of the task to be applied}
    acls: ${3:# A discretionary access security list (DACL) identifies the trustees that are allowed or denied access to a securable object}
    cert_filepath: ${4:# path to SSL client cert file (}
    control_flags: ${5:# Specifies the control flags in the SD}
    feature_flags: ${6:# Enable or disable a new feature}
    force_ontap_version: ${7:# Override the cluster ONTAP version when using REST}
    group: ${8:# Specifies the owner's primary group}
    hostname: ${9:# The hostname or IP address of the ONTAP instance}
    http_port: ${10:# Override the default port (80 or 443) with this port}
    https: ${11:# Enable and disable https}
    ignore_paths: ${12:# For each file or directory in the list, specifies that permissions on this file or directory cannot be replaced}
    key_filepath: ${13:# path to SSL client key file}
    ontapi: ${14:# The ontap api version to use}
    owner: ${15:# Specifies the owner of the NTFS security descriptor (SD)}
    password: ${16:# Password for the specified user}
    path: ${17:# The path of the file or directory on which to apply security permissions}
    propagation_mode: ${18:# Specifies how to propagate security settings to child subfolders and files}
    state: ${19:# Whether the specified file security permission should exist or not}
    use_rest: ${20:# Whether to use REST or ZAPI}
    username: ${21:# This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required}
    validate_certs: ${22:# If set to C(no), the SSL certificates will not be validated}
    validate_changes: ${23:# ACLs may not be applied as expected}
    vserver: ${24:# Name of the vserver to use}
  tags:
    - netapp
    - ontap
    - na_ontap_file_security_permissions]]></content>
  <tabTrigger>netapp.ontap.na_ontap_file_security_permissions</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp ONTAP NTFS file security permissions</description>
</snippet>