<snippet>
  <content><![CDATA[- name: ${1:Sends Toast windows notification to logged in users on Windows 10 or later hosts}
  community.windows.win_toast:
    expire: ${2:# How long in seconds before the notification expires}
    group: ${3:# Which notification group to add the notification to}
    msg: ${4:# The message to appear inside the notification}
    popup: ${5:# If C(no), the notification will not pop up and will only appear in the Action Center}
    tag: ${6:# The tag to add to the notification}
    title: ${7:# The notification title, which appears in the pop up}
  tags:
    - community
    - windows
    - win_toast]]></content>
  <tabTrigger>community.windows.win_toast</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Sends Toast windows notification to logged in users on Windows 10 or later hosts</description>
</snippet>