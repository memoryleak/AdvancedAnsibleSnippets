<snippet>
  <content><![CDATA[- name: ${1:Resource module for Allowed Protocols}
  cisco.ise.allowed_protocols:
    allowChap: ${2:# AllowChap flag}
    allowEapFast: ${3:# AllowEapFast flag}
    allowEapMd5: ${4:# AllowEapMd5 flag}
    allowEapTls: ${5:# AllowEapTls flag}
    allowEapTtls: ${6:# AllowEapTtls flag}
    allowLeap: ${7:# AllowLeap flag}
    allowMsChapV1: ${8:# AllowMsChapV1 flag}
    allowMsChapV2: ${9:# AllowMsChapV2 flag}
    allowPapAscii: ${10:# AllowPapAscii flag}
    allowPeap: ${11:# AllowPeap flag}
    allowPreferredEapProtocol: ${12:# AllowPreferredEapProtocol flag}
    allowTeap: ${13:# AllowTeap flag}
    allowWeakCiphersForEap: ${14:# AllowWeakCiphersForEap flag}
    description: ${15:# Allowed Protocols's description}
    eapFast: ${16:# The eapFast is required only if allowEapFast is true, otherwise it must be ignored}
    eapTls: ${17:# The eapTls is required only if allowEapTls is true, otherwise it must be ignored}
    eapTlsLBit: ${18:# EapTlsLBit flag}
    eapTtls: ${19:# The eapTtls is required only if allowEapTtls is true, otherwise it must be ignored}
    id: ${20:# Resource UUID, Mandatory for update}
    ise_debug: ${21:# Flag for Identity Services Engine SDK to enable debugging}
    ise_hostname: ${22:# The Identity Services Engine hostname}
    ise_password: ${23:# The Identity Services Engine password to authenticate}
    ise_single_request_timeout: ${24:# Timeout (in seconds) for RESTful HTTP requests}
    ise_username: ${25:# The Identity Services Engine username to authenticate}
    ise_uses_api_gateway: ${26:# Flag that informs the SDK whether to use the Identity Services Engine's API Gateway to send requests}
    ise_uses_csrf_token: ${27:# Flag that informs the SDK whether we send the CSRF token to ISE's ERS APIs}
    ise_verify: ${28:# Flag to enable or disable SSL certificate verification}
    ise_version: ${29:# Informs the SDK which version of Identity Services Engine to use}
    ise_wait_on_rate_limit: ${30:# Flag for Identity Services Engine SDK to enable automatic rate-limit handling}
    name: ${31:# Resource Name}
    peap: ${32:# Allowed Protocols's peap}
    preferredEapProtocol: ${33:# The preferredEapProtocol is required only if allowPreferredEapProtocol is true, otherwise it must be ignored}
    processHostLookup: ${34:# ProcessHostLookup flag}
    requireMessageAuth: ${35:# RequireMessageAuth flag}
    teap: ${36:# The teap is required only if allowTeap is true, otherwise it must be ignored}
  tags:
    - cisco
    - ise
    - allowed_protocols]]></content>
  <tabTrigger>cisco.ise.allowed_protocols</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for Allowed Protocols</description>
</snippet>