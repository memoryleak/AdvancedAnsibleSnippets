<snippet>
  <content><![CDATA[- name: ${1:Set vlan information}
  inspur.ispim.edit_vlan:
    interface_name: ${2:# Set interface_name}
    vlan_status: ${3:# Enable or disable vlan}
    vlan_id: ${4:# The Identification for VLAN configuration(2-4094)}
    vlan_priority: ${5:# The priority for VLAN configuration(1-7)}
    provider: ${6:# A dict object containing connection details}
    host: ${7:# Specifies the DNS host name or address for connecting to the remote device over the specified transport}
    username: ${8:# Configures the username to use to authenticate the connection to the remote device}
    password: ${9:# Specifies the password to use to authenticate the connection to the remote device}
  tags:
    - inspur
    - ispim
    - edit_vlan]]></content>
  <tabTrigger>inspur.ispim.edit_vlan</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Set vlan information</description>
</snippet>