<snippet>
  <content><![CDATA[- name: ${1:Resource module for Sda Virtual Network Ip Pool}
  cisco.dnac.sda_virtual_network_ip_pool:
    autoGenerateVlanName: ${2:# It will auto generate vlanName, if vlanName is empty(applicable for L3 and INFRA_VN)}
    dnac_debug: ${3:# Flag for Cisco DNA Center SDK to enable debugging}
    dnac_host: ${4:# The Cisco DNA Center hostname}
    dnac_password: ${5:# The Cisco DNA Center password to authenticate}
    dnac_port: ${6:# The Cisco DNA Center port}
    dnac_username: ${7:# The Cisco DNA Center username to authenticate}
    dnac_verify: ${8:# Flag to enable or disable SSL certificate verification}
    dnac_version: ${9:# Informs the SDK which version of Cisco DNA Center to use}
    ipPoolName: ${10:# IpPoolName query parameter}
    isBridgeModeVm: ${11:# Bridge Mode Vm enablement flag (applicable for L3 and L2 and default value is False )}
    isCommonPool: ${12:# Common Pool enablement flag(applicable for L3 and L2 and default value is False )}
    isIpDirectedBroadcast: ${13:# Ip Directed Broadcast enablement flag(applicable for L3 and default value is False )}
    isL2FloodingEnabled: ${14:# Layer2 flooding enablement flag(applicable for L3 , L2 and always true for L2 and default value is False )}
    isLayer2Only: ${15:# Layer2 Only enablement flag and default value is False}
    isThisCriticalPool: ${16:# Critical pool enablement flag(applicable for L3 and default value is False )}
    isWirelessPool: ${17:# Wireless Pool enablement flag(applicable for L3 and L2 and default value is False )}
    poolType: ${18:# Pool Type (applicable for INFRA_VN)}
    scalableGroupName: ${19:# Scalable Group Name(applicable for L3)}
    siteNameHierarchy: ${20:# SiteNameHierarchy query parameter}
    trafficType: ${21:# Traffic type(applicable for L3 and L2)}
    validate_response_schema: ${22:# Flag for Cisco DNA Center SDK to enable the validation of request bodies against a JSON schema}
    virtualNetworkName: ${23:# VirtualNetworkName query parameter}
    vlanId: ${24:# Vlan Id(applicable for L3 , L2 and INFRA_VN)}
    vlanName: ${25:# Vlan name represent the segment name, if empty, vlanName would be auto generated by API}
  tags:
    - cisco
    - dnac
    - sda_virtual_network_ip_pool]]></content>
  <tabTrigger>cisco.dnac.sda_virtual_network_ip_pool</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for Sda Virtual Network Ip Pool</description>
</snippet>