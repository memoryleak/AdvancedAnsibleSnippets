<snippet>
  <content><![CDATA[- name: ${1:Resource module for Sda Virtual Network Ip Pool}
  cisco.dnac.sda_virtual_network_ip_pool:
    autoGenerateVlanName: ${2:# It will auto generate vlanName, if vlanName is empty(applicable for L3 and INFRA_VN)}
    ipPoolName: ${3:# IpPoolName query parameter}
    isCommonPool: ${4:# Common Pool enablement flag(applicable for L3 and L2 and default value is False )}
    isIpDirectedBroadcast: ${5:# Ip Directed Broadcast enablement flag(applicable for L3 and default value is False )}
    isL2FloodingEnabled: ${6:# Layer2 flooding enablement flag(applicable for L3 , L2 and always true for L2 and default value is False )}
    isLayer2Only: ${7:# Layer2 Only enablement flag and default value is False}
    isThisCriticalPool: ${8:# Critical pool enablement(applicable for L3 and default value is False )}
    isWirelessPool: ${9:# Wireless Pool enablement flag(applicable for L3 and L2 and default value is False )}
    poolType: ${10:# Pool Type (applicable for INFRA_VN)}
    scalableGroupName: ${11:# Scalable Group Name(applicable for L3)}
    siteNameHierarchy: ${12:# SiteNameHierarchy query parameter}
    trafficType: ${13:# Traffic type(applicable for L3 and L2)}
    virtualNetworkName: ${14:# VirtualNetworkName query parameter}
    vlanId: ${15:# Vlan Id(applicable for L3 , L2 and INFRA_VN)}
    vlanName: ${16:# Vlan name represent the segment name, if empty, vlanName would be auto generated by API}
    dnac_host: ${17:# The Cisco DNA Center hostname}
    dnac_port: ${18:# The Cisco DNA Center port}
    dnac_username: ${19:# The Cisco DNA Center username to authenticate}
    dnac_password: ${20:# The Cisco DNA Center password to authenticate}
    dnac_verify: ${21:# Flag to enable or disable SSL certificate verification}
    dnac_version: ${22:# Informs the SDK which version of Cisco DNA Center to use}
    dnac_debug: ${23:# Flag for Cisco DNA Center SDK to enable debugging}
    validate_response_schema: ${24:# Flag for Cisco DNA Center SDK to enable the validation of request bodies against a JSON schema}
  tags:
    - cisco
    - dnac
    - sda_virtual_network_ip_pool]]></content>
  <tabTrigger>cisco.dnac.sda_virtual_network_ip_pool</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for Sda Virtual Network Ip Pool</description>
</snippet>