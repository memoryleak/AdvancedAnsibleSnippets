<snippet>
  <content><![CDATA[- name: ${1:Resource module for Ipsec}
  cisco.ise.ipsec:
    authType: ${2:# Authentication type for establishing connection}
    certId: ${3:# ID of the certificate for establishing connection}
    configureVti: ${4:# Authentication type for establishing connection}
    espAhProtocol: ${5:# Encryption protocol used for establishing connection}
    hostName: ${6:# Hostname of the node}
    iface: ${7:# Ethernet port of the node}
    ikeReAuthTime: ${8:# IKE re-authentication time}
    ikeVersion: ${9:# IKE version}
    ise_debug: ${10:# Flag for Identity Services Engine SDK to enable debugging}
    ise_hostname: ${11:# The Identity Services Engine hostname}
    ise_password: ${12:# The Identity Services Engine password to authenticate}
    ise_single_request_timeout: ${13:# Timeout (in seconds) for RESTful HTTP requests}
    ise_username: ${14:# The Identity Services Engine username to authenticate}
    ise_uses_api_gateway: ${15:# Flag that informs the SDK whether to use the Identity Services Engine's API Gateway to send requests}
    ise_uses_csrf_token: ${16:# Flag that informs the SDK whether we send the CSRF token to ISE's ERS APIs}
    ise_verify: ${17:# Flag to enable or disable SSL certificate verification}
    ise_version: ${18:# Informs the SDK which version of Identity Services Engine to use}
    ise_wait_on_rate_limit: ${19:# Flag for Identity Services Engine SDK to enable automatic rate-limit handling}
    localInternalIp: ${20:# Local Tunnel IP address}
    modeOption: ${21:# The Mode type used for establishing the connection}
    nadIp: ${22:# NAD IP address for establishing connection}
    phaseOneDHGroup: ${23:# Phase-one DH group used for establishing connection}
    phaseOneEncryptionAlgo: ${24:# Phase-one encryption algorithm used for establishing connection}
    phaseOneHashAlgo: ${25:# Phase-one hashing algorithm used for establishing connection}
    phaseOneLifeTime: ${26:# Phase-one connection lifetime}
    phaseTwoDHGroup: ${27:# Phase-two DH group used for establishing connection}
    phaseTwoEncryptionAlgo: ${28:# Phase-two encryption algorithm used for establishing connection}
    phaseTwoHashAlgo: ${29:# Phase-two hashing algorithm used for establishing connection}
    phaseTwoLifeTime: ${30:# Phase-two connection lifetime}
    psk: ${31:# Pre-shared key used for establishing connection}
    remotePeerInternalIp: ${32:# Remote Tunnel IP address}
  tags:
    - cisco
    - ise
    - ipsec]]></content>
  <tabTrigger>cisco.ise.ipsec</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for Ipsec</description>
</snippet>