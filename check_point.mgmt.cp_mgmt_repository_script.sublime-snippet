<snippet>
  <content><![CDATA[- name: ${1:Manages repository-script objects on Checkpoint over Web Services API}
  check_point.mgmt.cp_mgmt_repository_script:
    name: ${2:# Object name}
    script_body: ${3:# The entire content of the script}
    script_body_base64: ${4:# The entire content of the script encoded in Base64}
    tags: ${5:# Collection of tag identifiers}
    color: ${6:# Color of the object}
    comments: ${7:# Comments string}
    ignore_warnings: ${8:# Apply changes ignoring warnings}
    ignore_errors: ${9:# Apply changes ignoring errors}
    details_level: ${10:# The level of detail for some of the fields in the response can vary from showing only the UID value of the object to a fully detailed representation of the object}
    state: ${11:# State of the access rule (present or absent)}
    auto_publish_session: ${12:# Publish the current session if changes have been performed after task completes}
    wait_for_task: ${13:# Wait for the task to end}
    wait_for_task_timeout: ${14:# How many minutes to wait until throwing a timeout error}
    version: ${15:# Version of checkpoint}
  tags:
    - check_point
    - mgmt
    - cp_mgmt_repository_script]]></content>
  <tabTrigger>check_point.mgmt.cp_mgmt_repository_script</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages repository-script objects on Checkpoint over Web Services API</description>
</snippet>