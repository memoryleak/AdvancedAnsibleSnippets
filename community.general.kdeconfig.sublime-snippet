<snippet>
  <content><![CDATA[- name: ${1:Manage KDE configuration files}
  community.general.kdeconfig:
    attributes: ${2:# The attributes the resulting filesystem object should have}
    backup: ${3:# Create a backup file}
    group: ${4:# Name of the group that should own the filesystem object, as would be fed to I(chown)}
    kwriteconfig_path: ${5:# Path to the kwriteconfig executable}
    mode: ${6:# The permissions the resulting filesystem object should have}
    owner: ${7:# Name of the user that should own the filesystem object, as would be fed to I(chown)}
    path: ${8:# Path to the config file}
    selevel: ${9:# The level part of the SELinux filesystem object context}
    serole: ${10:# The role part of the SELinux filesystem object context}
    setype: ${11:# The type part of the SELinux filesystem object context}
    seuser: ${12:# The user part of the SELinux filesystem object context}
    unsafe_writes: ${13:# Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object}
    values: ${14:# List of values to set}
  tags:
    - community
    - general
    - kdeconfig]]></content>
  <tabTrigger>community.general.kdeconfig</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage KDE configuration files</description>
</snippet>