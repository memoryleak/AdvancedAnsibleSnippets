<snippet>
  <content><![CDATA[- name: ${1:Send Amazon Simple Notification Service messages}
  community.aws.sns:
    access_key: ${2:# AWS access key ID}
    application: ${3:# Message to send to application subscriptions}
    aws_ca_bundle: ${4:# The location of a CA Bundle to use when validating SSL certificates}
    aws_config: ${5:# A dictionary to modify the botocore configuration}
    debug_botocore_endpoint_logs: ${6:# Use a C(botocore}
    email: ${7:# Message to send to email subscriptions}
    email_json: ${8:# Message to send to email-json subscriptions}
    endpoint_url: ${9:# URL to connect to instead of the default AWS endpoints}
    http: ${10:# Message to send to HTTP subscriptions}
    https: ${11:# Message to send to HTTPS subscriptions}
    lambda: ${12:# Message to send to Lambda subscriptions}
    message_attributes: ${13:# Dictionary of message attributes}
    message_deduplication_id: ${14:# Only in connection with the message_group_id}
    message_group_id: ${15:# A tag which is used to process messages that belong to the same group in a FIFO manner}
    message_structure: ${16:# The payload format to use for the message}
    msg: ${17:# Default message for subscriptions without a more specific message}
    profile: ${18:# A named AWS profile to use for authentication}
    region: ${19:# The AWS region to use}
    secret_key: ${20:# AWS secret access key}
    session_token: ${21:# AWS STS session token for use with temporary credentials}
    sms: ${22:# Message to send to SMS subscriptions}
    sqs: ${23:# Message to send to SQS subscriptions}
    subject: ${24:# Message subject}
    topic: ${25:# The name or ARN of the topic to publish to}
    validate_certs: ${26:# When set to C(false), SSL certificates will not be validated for communication with the AWS APIs}
  tags:
    - community
    - aws
    - sns]]></content>
  <tabTrigger>community.aws.sns</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Send Amazon Simple Notification Service messages</description>
</snippet>