<snippet>
  <content><![CDATA[- name: ${1:Create, drop, or modify a PostgreSQL table}
  community.postgresql.postgresql_table:
    ca_cert: ${2:# Specifies the name of a file containing SSL certificate authority (CA) certificate(s)}
    cascade: ${3:# Automatically drop objects that depend on the table (such as views)}
    columns: ${4:# Columns that are needed}
    connect_params: ${5:# Any additional parameters to be passed to libpg}
    db: ${6:# Name of database to connect and where the table will be created}
    including: ${7:# Keywords that are used with like parameter, may be DEFAULTS, CONSTRAINTS, INDEXES, STORAGE, COMMENTS or ALL}
    like: ${8:# Create a table like another table (with similar DDL)}
    login_host: ${9:# Host running the database}
    login_password: ${10:# The password this module should use to establish its PostgreSQL session}
    login_unix_socket: ${11:# Path to a Unix domain socket for local connections}
    login_user: ${12:# The username this module should use to establish its PostgreSQL session}
    owner: ${13:# Set a table owner}
    port: ${14:# Database port to connect to}
    rename: ${15:# New table name}
    session_role: ${16:# Switch to session_role after connecting}
    ssl_cert: ${17:# Specifies the file name of the client SSL certificate}
    ssl_key: ${18:# Specifies the location for the secret key used for the client certificate}
    ssl_mode: ${19:# Determines whether or with what priority a secure SSL TCP/IP connection will be negotiated with the server}
    state: ${20:# The table state}
    storage_params: ${21:# Storage parameters like fillfactor, autovacuum_vacuum_treshold, etc}
    table: ${22:# Table name}
    tablespace: ${23:# Set a tablespace for the table}
    truncate: ${24:# Truncate a table}
    trust_input: ${25:# If C(false), check whether values of parameters are potentially dangerous}
    unlogged: ${26:# Create an unlogged table}
  tags:
    - community
    - postgresql
    - postgresql_table]]></content>
  <tabTrigger>community.postgresql.postgresql_table</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create, drop, or modify a PostgreSQL table</description>
</snippet>