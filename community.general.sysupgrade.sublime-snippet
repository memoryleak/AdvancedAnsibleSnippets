<snippet>
  <content><![CDATA[- name: ${1:Manage OpenBSD system upgrades}
  community.general.sysupgrade:
    fetch_only: ${2:# Fetch and verify files and create C(/bsd}
    force: ${3:# Force upgrade (for snapshots only)}
    installurl: ${4:# OpenBSD mirror top-level URL for fetching an upgrade}
    keep_files: ${5:# Keep the files under C(/home/_sysupgrade)}
    snapshot: ${6:# Apply the latest snapshot}
  tags:
    - community
    - general
    - sysupgrade]]></content>
  <tabTrigger>community.general.sysupgrade</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage OpenBSD system upgrades</description>
</snippet>