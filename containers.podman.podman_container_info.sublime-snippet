<snippet>
  <content><![CDATA[- name: ${1:Gather facts about containers using podman}
  containers.podman.podman_container_info:
    name: ${2:# List of container names to gather facts about}
    executable: ${3:# Path to C(podman) executable if it is not in the C($PATH) on the machine running C(podman)}
  tags:
    - containers
    - podman
    - podman_container_info]]></content>
  <tabTrigger>containers.podman.podman_container_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Gather facts about containers using podman</description>
</snippet>