<snippet>
  <content><![CDATA[- name: ${1:Module to manage external providers in oVirt/RHV}
  ovirt.ovirt.ovirt_external_provider:
    name: ${2:# Name of the external provider to manage}
    state: ${3:# Should the external be present or absent}
    description: ${4:# Description of the external provider}
    type: ${5:# Type of the external provider}
    url: ${6:# URL where external provider is hosted}
    username: ${7:# Username to be used for login to external provider}
    password: ${8:# Password of the user specified in C(username) parameter}
    tenant_name: ${9:# Name of the tenant}
    authentication_url: ${10:# Keystone authentication URL of the openstack provider}
    data_center: ${11:# Name of the data center where provider should be attached}
    read_only: ${12:# Specify if the network should be read only}
    network_type: ${13:# Type of the external network provider either external (for example OVN) or neutron}
    authentication_keys: ${14:# List of authentication keys}
    wait: ${15:# C(yes) if the module should wait for the entity to get into desired state}
    fetch_nested: ${16:# If I(True) the module will fetch additional data from the API}
    nested_attributes: ${17:# Specifies list of the attributes which should be fetched from the API}
    auth: ${18:# Dictionary with values needed to create HTTP/HTTPS connection to oVirt:}
    timeout: ${19:# The amount of time in seconds the module should wait for the instance to get into desired state}
    poll_interval: ${20:# Number of the seconds the module waits until another poll request on entity status is sent}
  tags:
    - ovirt
    - ovirt
    - ovirt_external_provider]]></content>
  <tabTrigger>ovirt.ovirt.ovirt_external_provider</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Module to manage external providers in oVirt/RHV</description>
</snippet>