<snippet>
  <content><![CDATA[- name: ${1:Module to manage external providers in oVirt/RHV}
  ovirt.ovirt.ovirt_external_provider:
    auth: ${2:# Dictionary with values needed to create HTTP/HTTPS connection to oVirt:}
    authentication_keys: ${3:# List of authentication keys}
    authentication_url: ${4:# Keystone authentication URL of the openstack provider}
    data_center: ${5:# Name of the data center where provider should be attached}
    description: ${6:# Description of the external provider}
    fetch_nested: ${7:# If I(True) the module will fetch additional data from the API}
    name: ${8:# Name of the external provider to manage}
    nested_attributes: ${9:# Specifies list of the attributes which should be fetched from the API}
    network_type: ${10:# Type of the external network provider either external (for example OVN) or neutron}
    password: ${11:# Password of the user specified in C(username) parameter}
    poll_interval: ${12:# Number of the seconds the module waits until another poll request on entity status is sent}
    read_only: ${13:# Specify if the network should be read only}
    state: ${14:# Should the external be present or absent}
    tenant_name: ${15:# Name of the tenant}
    timeout: ${16:# The amount of time in seconds the module should wait for the instance to get into desired state}
    type: ${17:# Type of the external provider}
    url: ${18:# URL where external provider is hosted}
    username: ${19:# Username to be used for login to external provider}
    wait: ${20:# C(yes) if the module should wait for the entity to get into desired state}
  tags:
    - ovirt
    - ovirt
    - ovirt_external_provider]]></content>
  <tabTrigger>ovirt.ovirt.ovirt_external_provider</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Module to manage external providers in oVirt/RHV</description>
</snippet>