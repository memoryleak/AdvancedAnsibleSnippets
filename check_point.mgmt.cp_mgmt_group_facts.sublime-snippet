<snippet>
  <content><![CDATA[- name: ${1:Get group objects facts on Check Point over Web Services API}
  check_point.mgmt.cp_mgmt_group_facts:
    name: ${2:# Object name}
    show_as_ranges: ${3:# When true, the group's matched content is displayed as ranges of IP addresses rather than network objects}
    details_level: ${4:# The level of detail for some of the fields in the response can vary from showing only the UID value of the object to a fully detailed representation of the object}
    limit: ${5:# No more than that many results will be returned}
    offset: ${6:# Skip that many results before beginning to return them}
    order: ${7:# Sorts results by the given field}
    dereference_group_members: ${8:# Indicates whether to dereference 'members' field by details level for every object in reply}
    show_membership: ${9:# Indicates whether to calculate and show 'groups' field for every object in reply}
    version: ${10:# Version of checkpoint}
  tags:
    - check_point
    - mgmt
    - cp_mgmt_group_facts]]></content>
  <tabTrigger>check_point.mgmt.cp_mgmt_group_facts</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Get group objects facts on Check Point over Web Services API</description>
</snippet>