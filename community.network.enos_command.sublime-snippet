<snippet>
  <content><![CDATA[- name: ${1:Run arbitrary commands on Lenovo ENOS devices}
  community.network.enos_command:
    auth_pass: ${2:# Specifies the password to use if required to enter privileged mode on the remote device}
    authorize: ${3:# Instructs the module to enter privileged mode on the remote device before sending any commands}
    commands: ${4:# List of commands to send to the remote device over the configured provider}
    interval: ${5:# Configures the interval in seconds to wait between retries of the command}
    match: ${6:# The I(match) argument is used in conjunction with the I(wait_for) argument to specify the match policy}
    retries: ${7:# Specifies the number of retries a command should by tried before it is considered failed}
    wait_for: ${8:# List of conditions to evaluate against the output of the command}
  tags:
    - community
    - network
    - enos_command]]></content>
  <tabTrigger>community.network.enos_command</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Run arbitrary commands on Lenovo ENOS devices</description>
</snippet>