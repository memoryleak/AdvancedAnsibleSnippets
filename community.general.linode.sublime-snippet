<snippet>
  <content><![CDATA[- name: ${1:Manage instances on the Linode Public Cloud}
  community.general.linode:
    state: ${2:# Indicate desired state of the resource}
    api_key: ${3:# Linode API key}
    name: ${4:# Name to give the instance (alphanumeric, dashes, underscore)}
    displaygroup: ${5:# Add the instance to a Display Group in Linode Manager}
    linode_id: ${6:# Unique ID of a linode server}
    additional_disks: ${7:# List of dictionaries for creating additional disks that are added to the Linode configuration settings}
    alert_bwin_enabled: ${8:# Set status of bandwidth in alerts}
    alert_bwin_threshold: ${9:# Set threshold in MB of bandwidth in alerts}
    alert_bwout_enabled: ${10:# Set status of bandwidth out alerts}
    alert_bwout_threshold: ${11:# Set threshold in MB of bandwidth out alerts}
    alert_bwquota_enabled: ${12:# Set status of bandwidth quota alerts as percentage of network transfer quota}
    alert_bwquota_threshold: ${13:# Set threshold in MB of bandwidth quota alerts}
    alert_cpu_enabled: ${14:# Set status of receiving CPU usage alerts}
    alert_cpu_threshold: ${15:# Set percentage threshold for receiving CPU usage alerts}
    alert_diskio_enabled: ${16:# Set status of receiving disk IO alerts}
    alert_diskio_threshold: ${17:# Set threshold for average IO ops/sec over 2 hour period}
    backupweeklyday: ${18:# Day of the week to take backups}
    backupwindow: ${19:# The time window in which backups will be taken}
    plan: ${20:# plan to use for the instance (Linode plan)}
    payment_term: ${21:# payment term to use for the instance (payment term in months)}
    password: ${22:# root password to apply to a new server (auto generated if missing)}
    private_ip: ${23:# Add private IPv4 address when Linode is created}
    ssh_pub_key: ${24:# SSH public key applied to root user}
    swap: ${25:# swap size in MB}
    distribution: ${26:# distribution to use for the instance (Linode Distribution)}
    datacenter: ${27:# datacenter to create an instance in (Linode Datacenter)}
    kernel_id: ${28:# kernel to use for the instance (Linode Kernel)}
    wait: ${29:# wait for the instance to be in state C(running) before returning}
    wait_timeout: ${30:# how long before wait gives up, in seconds}
    watchdog: ${31:# Set status of Lassie watchdog}
  tags:
    - community
    - general
    - linode]]></content>
  <tabTrigger>community.general.linode</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage instances on the Linode Public Cloud</description>
</snippet>