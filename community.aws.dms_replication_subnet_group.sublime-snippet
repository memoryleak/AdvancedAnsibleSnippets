<snippet>
  <content><![CDATA[- name: ${1:creates or destroys a data migration services subnet group}
  community.aws.dms_replication_subnet_group:
    access_key: ${2:# AWS access key ID}
    aws_ca_bundle: ${3:# The location of a CA Bundle to use when validating SSL certificates}
    aws_config: ${4:# A dictionary to modify the botocore configuration}
    debug_botocore_endpoint_logs: ${5:# Use a C(botocore}
    description: ${6:# The description for the subnet group}
    endpoint_url: ${7:# URL to connect to instead of the default AWS endpoints}
    identifier: ${8:# The name for the replication subnet group}
    profile: ${9:# A named AWS profile to use for authentication}
    region: ${10:# The AWS region to use}
    secret_key: ${11:# AWS secret access key}
    session_token: ${12:# AWS STS session token for use with temporary credentials}
    state: ${13:# State of the subnet group}
    subnet_ids: ${14:# A list containing the subnet ids for the replication subnet group, needs to be at least 2 items in the list}
    validate_certs: ${15:# When set to C(false), SSL certificates will not be validated for communication with the AWS APIs}
  tags:
    - community
    - aws
    - dms_replication_subnet_group]]></content>
  <tabTrigger>community.aws.dms_replication_subnet_group</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>creates or destroys a data migration services subnet group</description>
</snippet>