<snippet>
  <content><![CDATA[- name: ${1:Send Slack notifications}
  community.general.slack:
    attachments: ${2:# Define a list of attachments}
    blocks: ${3:# Define a list of blocks}
    channel: ${4:# Channel to send the message to}
    color: ${5:# Allow text to use default colors - use the default of 'normal' to not send a custom color bar at the start of the message}
    domain: ${6:# Slack (sub)domain for your environment without protocol}
    icon_emoji: ${7:# Emoji for the message sender}
    icon_url: ${8:# URL for the message sender's icon}
    link_names: ${9:# Automatically create links for channels and usernames in O(msg)}
    message_id: ${10:# Optional}
    msg: ${11:# Message to send}
    parse: ${12:# Setting for the message parser at Slack}
    prepend_hash: ${13:# Setting for automatically prepending a V(#) symbol on the passed in O(channel)}
    thread_id: ${14:# Optional}
    token: ${15:# Slack integration token}
    username: ${16:# This is the sender of the message}
    validate_certs: ${17:# If V(false), SSL certificates will not be validated}
  tags:
    - community
    - general
    - slack]]></content>
  <tabTrigger>community.general.slack</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Send Slack notifications</description>
</snippet>