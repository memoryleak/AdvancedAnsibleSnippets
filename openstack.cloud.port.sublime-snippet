<snippet>
  <content><![CDATA[- name: ${1:Add/Update/Delete ports from an OpenStack cloud}
  openstack.cloud.port:
    allowed_address_pairs: ${2:# Allowed address pairs list}
    api_timeout: ${3:# How long should the socket layer wait before timing out for API calls}
    auth: ${4:# Dictionary containing auth information as needed by the cloud's auth plugin strategy}
    auth_type: ${5:# Name of the auth plugin to use}
    binding_profile: ${6:# Binding profile dict that the port should be created with}
    binding_vnic_type: ${7:# The type of the port that should be created}
    ca_cert: ${8:# A path to a CA Cert bundle that can be used as part of verifying SSL API requests}
    client_cert: ${9:# A path to a client certificate to use as part of the SSL transaction}
    client_key: ${10:# A path to a client key to use as part of the SSL transaction}
    cloud: ${11:# Named cloud or cloud config to operate against}
    description: ${12:# Description of the port}
    device_id: ${13:# Device ID of device using this port}
    device_owner: ${14:# The ID of the entity that uses this port}
    dns_domain: ${15:# The dns domain of the port ( only with dns-integration enabled )}
    dns_name: ${16:# The dns name of the port ( only with dns-integration enabled )}
    extra_dhcp_opts: ${17:# Extra dhcp options to be assigned to this port}
    fixed_ips: ${18:# Desired IP and/or subnet for this port}
    interface: ${19:# Endpoint URL type to fetch from the service catalog}
    is_admin_state_up: ${20:# Sets admin state}
    is_port_security_enabled: ${21:# Whether to enable or disable the port security on the network}
    mac_address: ${22:# MAC address of this port}
    name: ${23:# Name that has to be given to the port}
    network: ${24:# ID or name of the network this port belongs to}
    no_security_groups: ${25:# Do not associate a security group with this port}
    region_name: ${26:# Name of the region}
    sdk_log_level: ${27:# Log level of the OpenStackSDK}
    sdk_log_path: ${28:# Path to the logfile of the OpenStackSDK}
    security_groups: ${29:# Security group(s) ID(s) or name(s) associated with the port}
    state: ${30:# Should the resource be present or absent}
    timeout: ${31:# How long should ansible wait for the requested resource}
    validate_certs: ${32:# Whether or not SSL API requests should be verified}
    wait: ${33:# Should ansible wait until the requested resource is complete}
  tags:
    - openstack
    - cloud
    - port]]></content>
  <tabTrigger>openstack.cloud.port</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Add/Update/Delete ports from an OpenStack cloud</description>
</snippet>