<snippet>
  <content><![CDATA[- name: ${1:Manage Logstash plugins}
  community.general.logstash_plugin:
    name: ${2:# Install plugin with that name}
    plugin_bin: ${3:# Specify logstash-plugin to use for plugin management}
    proxy_host: ${4:# Proxy host to use during plugin installation}
    proxy_port: ${5:# Proxy port to use during plugin installation}
    state: ${6:# Apply plugin state}
    version: ${7:# Specify plugin Version of the plugin to install}
  tags:
    - community
    - general
    - logstash_plugin]]></content>
  <tabTrigger>community.general.logstash_plugin</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Logstash plugins</description>
</snippet>