<snippet>
  <content><![CDATA[- name: ${1:Configures a virtual directory in IIS}
  community.windows.win_iis_virtualdirectory:
    application: ${2:# The application under which the virtual directory is created or exists}
    connect_as: ${3:# The type of authentication to use for the virtual directory}
    name: ${4:# The name of the virtual directory to create or remove}
    password: ${5:# The password associated with I(username)}
    physical_path: ${6:# The physical path to the folder in which the new virtual directory is created}
    site: ${7:# The site name under which the virtual directory is created or exists}
    state: ${8:# Whether to add or remove the specified virtual directory}
    username: ${9:# Specifies the user name of an account that can access configuration files and content for the virtual directory}
  tags:
    - community
    - windows
    - win_iis_virtualdirectory]]></content>
  <tabTrigger>community.windows.win_iis_virtualdirectory</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configures a virtual directory in IIS</description>
</snippet>