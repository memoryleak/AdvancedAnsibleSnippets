<snippet>
  <content><![CDATA[- name: ${1:Configures the physical disk, virtual disk, and storage controller settings}
  dellemc.openmanage.idrac_redfish_storage_controller:
    apply_time: ${2:# Apply time of the I(attributes)}
    attributes: ${3:# Dictionary of controller attributes and value pair}
    baseuri: ${4:# IP address of the target out-of-band controller}
    ca_path: ${5:# The Privacy Enhanced Mail (PEM) file that contains a CA certificate to be used for the validation}
    command: ${6:# These actions may require a system reset, depending on the capabilities of the controller}
    controller_id: ${7:# Fully Qualified Device Descriptor (FQDD) of the storage controller}
    job_wait: ${8:# Provides the option if the module has to wait for the job to be completed}
    job_wait_timeout: ${9:# The maximum wait time of job completion in seconds before the job tracking is stopped}
    key: ${10:# A new security key passphrase that the encryption-capable controller uses to create the encryption key}
    key_id: ${11:# This is a user supplied text label associated with the passphrase}
    maintenance_window: ${12:# Option to schedule the maintenance window}
    mode: ${13:# Encryption mode of the encryption capable controller}
    old_key: ${14:# Security key passphrase used by the encryption-capable controller}
    password: ${15:# Password of the target out-of-band controller}
    size: ${16:# Capacity of the virtual disk to be expanded in MB}
    target: ${17:# Fully Qualified Device Descriptor (FQDD) of the target physical drive}
    timeout: ${18:# The socket level timeout in seconds}
    username: ${19:# Username of the target out-of-band controller}
    validate_certs: ${20:# If C(false), the SSL certificates will not be validated}
    volume_id: ${21:# Fully Qualified Device Descriptor (FQDD) of the volume}
    x_auth_token: ${22:# Authentication token}
  tags:
    - dellemc
    - openmanage
    - idrac_redfish_storage_controller]]></content>
  <tabTrigger>dellemc.openmanage.idrac_redfish_storage_controller</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configures the physical disk, virtual disk, and storage controller settings</description>
</snippet>