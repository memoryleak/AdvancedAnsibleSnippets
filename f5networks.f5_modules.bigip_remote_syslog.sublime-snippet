<snippet>
  <content><![CDATA[- name: ${1:Manipulate remote syslog settings on a BIG-IP}
  f5networks.f5_modules.bigip_remote_syslog:
    local_ip: ${2:# Specifies the local IP address of the system that is logging}
    name: ${3:# Specifies the name of the syslog object}
    provider: ${4:# A dict object containing connection details}
    remote_host: ${5:# Specifies the IP address or hostname for the remote system, to which the system sends log messages}
    remote_port: ${6:# Specifies the port the system uses to send messages to the remote logging server}
    state: ${7:# When C(present), guarantees the remote syslog exists with the provided attributes}
  tags:
    - f5networks
    - f5_modules
    - bigip_remote_syslog]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_remote_syslog</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manipulate remote syslog settings on a BIG-IP</description>
</snippet>