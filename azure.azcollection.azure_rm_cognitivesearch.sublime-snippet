<snippet>
  <content><![CDATA[- name: ${1:Manage Azure Cognitive Search service}
  azure.azcollection.azure_rm_cognitivesearch:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${7:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${8:# Azure client ID}
    cloud_environment: ${9:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    hosting_mode: ${10:# Applicable only for the standard3 SKU}
    identity: ${11:# The identity for the resource}
    location: ${12:# Valid azure location}
    log_mode: ${13:# Parent argument}
    log_path: ${14:# Parent argument}
    name: ${15:# The name of the Azure Cognitive Search service}
    network_rule_set: ${16:# Network specific rules that determine how the Azure Cognitive Search service may be reached}
    partition_count: ${17:# The number of partitions in the search service}
    password: ${18:# Active Directory user password}
    profile: ${19:# Security profile found in ~/}
    public_network_access: ${20:# This value can be set to C(enabled) to avoid breaking changes on existing customer resources and templates}
    replica_count: ${21:# The number of replicas in the search service}
    resource_group: ${22:# The name of the resource group within the current subscription}
    secret: ${23:# Azure client secret}
    sku: ${24:# The SKU of the Search Service, which determines price tier and capacity limits}
    state: ${25:# Assert the state of the search instance}
    subscription_id: ${26:# Your Azure subscription Id}
    tags: ${27:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${28:# Azure tenant ID}
    thumbprint: ${29:# The thumbprint of the private key specified in I(x509_certificate_path)}
    x509_certificate_path: ${30:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_cognitivesearch]]></content>
  <tabTrigger>azure.azcollection.azure_rm_cognitivesearch</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure Cognitive Search service</description>
</snippet>