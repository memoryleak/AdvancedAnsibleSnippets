<snippet>
  <content><![CDATA[- name: ${1:Boot Order policy configuration for Cisco Intersight}
  cisco.intersight.intersight_boot_order_policy:
    api_key_id: ${2:# Public API Key ID associated with the private key}
    api_private_key: ${3:# Filename (absolute path) or string of PEM formatted private key data to be used for Intersight API authentication}
    api_uri: ${4:# URI used to access the Intersight API}
    boot_devices: ${5:# List of Boot Devices configured on the endpoint}
    configured_boot_mode: ${6:# Sets the BIOS boot mode}
    description: ${7:# The user-defined description of the Boot Order policy}
    name: ${8:# The name assigned to the Boot Order policy}
    organization: ${9:# The name of the Organization this resource is assigned to}
    state: ${10:# If C(present), will verify the resource is present and will create if needed}
    tags: ${11:# List of tags in Key:<user-defined key> Value:<user-defined value> format}
    uefi_enable_secure_boot: ${12:# Secure boot enforces that device boots using only software that is trusted by the Original Equipment Manufacturer (OEM)}
    use_proxy: ${13:# If C(no), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    validate_certs: ${14:# Boolean control for verifying the api_uri TLS certificate}
  tags:
    - cisco
    - intersight
    - intersight_boot_order_policy]]></content>
  <tabTrigger>cisco.intersight.intersight_boot_order_policy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Boot Order policy configuration for Cisco Intersight</description>
</snippet>