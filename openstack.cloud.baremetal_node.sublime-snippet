<snippet>
  <content><![CDATA[- name: ${1:Create/Delete Bare Metal Resources from OpenStack}
  openstack.cloud.baremetal_node:
    api_timeout: ${2:# How long should the socket layer wait before timing out for API calls}
    auth: ${3:# Dictionary containing auth information as needed by the cloud's auth plugin strategy}
    auth_type: ${4:# Name of the auth plugin to use}
    bios_interface: ${5:# The bios interface for this node, e}
    boot_interface: ${6:# The boot interface for this node, e}
    ca_cert: ${7:# A path to a CA Cert bundle that can be used as part of verifying SSL API requests}
    chassis_id: ${8:# Associate the node with a pre-defined chassis}
    client_cert: ${9:# A path to a client certificate to use as part of the SSL transaction}
    client_key: ${10:# A path to a client key to use as part of the SSL transaction}
    cloud: ${11:# Named cloud or cloud config to operate against}
    console_interface: ${12:# The console interface for this node, e}
    deploy_interface: ${13:# The deploy interface for this node, e}
    driver: ${14:# The name of the Ironic Driver to use with this node}
    driver_info: ${15:# Information for this node's driver}
    id: ${16:# ID to be given to the baremetal node}
    inspect_interface: ${17:# The interface used for node inspection, e}
    interface: ${18:# Endpoint URL type to fetch from the service catalog}
    management_interface: ${19:# The interface for out-of-band management of this node, e}
    name: ${20:# unique name identifier to be given to the resource}
    network_interface: ${21:# The network interface provider to use when describing connections for this node}
    nics: ${22:# A list of network interface cards, eg, C( - mac: aa:bb:cc:aa:bb:cc)}
    power_interface: ${23:# The interface used to manage power actions on this node, e}
    properties: ${24:# Definition of the physical characteristics of this node}
    raid_interface: ${25:# Interface used for configuring raid on this node}
    region_name: ${26:# Name of the region}
    rescue_interface: ${27:# Interface used for node rescue, e}
    resource_class: ${28:# The specific resource type to which this node belongs}
    sdk_log_level: ${29:# Log level of the OpenStackSDK}
    sdk_log_path: ${30:# Path to the logfile of the OpenStackSDK}
    skip_update_of_masked_password: ${31:# Deprecated, no longer used}
    state: ${32:# Indicates desired state of the resource}
    storage_interface: ${33:# Interface used for attaching and detaching volumes on this node, e}
    timeout: ${34:# Number of seconds to wait for the newly created node to reach the available state}
    validate_certs: ${35:# Whether or not SSL API requests should be verified}
    vendor_interface: ${36:# Interface for all vendor-specific actions on this node, e}
    wait: ${37:# Should ansible wait until the requested resource is complete}
  tags:
    - openstack
    - cloud
    - baremetal_node]]></content>
  <tabTrigger>openstack.cloud.baremetal_node</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create/Delete Bare Metal Resources from OpenStack</description>
</snippet>