<snippet>
  <content><![CDATA[- name: ${1:Manages subnets on the cloudscale}
  cloudscale_ch.cloud.subnet:
    api_timeout: ${2:# Timeout in seconds for calls to the cloudscale}
    api_token: ${3:# cloudscale}
    api_url: ${4:# cloudscale}
    cidr: ${5:# The cidr of the subnet}
    dns_servers: ${6:# A list of DNS resolver IP addresses, that act as DNS servers}
    gateway_address: ${7:# The gateway address of the subnet}
    network: ${8:# The name of the network the subnet is related to}
    reset: ${9:# Resets I(gateway_address) and I(dns_servers) to default values by the API}
    state: ${10:# State of the subnet}
    tags: ${11:# Tags associated with the subnet}
    uuid: ${12:# UUID of the subnet}
  tags:
    - cloudscale_ch
    - cloud
    - subnet]]></content>
  <tabTrigger>cloudscale_ch.cloud.subnet</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages subnets on the cloudscale</description>
</snippet>