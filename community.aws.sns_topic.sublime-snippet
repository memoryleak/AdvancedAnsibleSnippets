<snippet>
  <content><![CDATA[- name: ${1:Manages AWS SNS topics and subscriptions}
  community.aws.sns_topic:
    access_key: ${2:# AWS access key ID}
    aws_ca_bundle: ${3:# The location of a CA Bundle to use when validating SSL certificates}
    aws_config: ${4:# A dictionary to modify the botocore configuration}
    content_based_deduplication: ${5:# Whether to enable content-based deduplication for this topic}
    debug_botocore_endpoint_logs: ${6:# Use a C(botocore}
    delivery_policy: ${7:# Delivery policy to apply to the SNS topic}
    display_name: ${8:# Display name of the topic}
    endpoint_url: ${9:# URL to connect to instead of the default AWS endpoints}
    name: ${10:# The name or ARN of the SNS topic to manage}
    policy: ${11:# Policy to apply to the SNS topic}
    profile: ${12:# A named AWS profile to use for authentication}
    purge_subscriptions: ${13:# Whether to purge any subscriptions not listed here}
    purge_tags: ${14:# If I(purge_tags=true) and I(tags) is set, existing tags will be purged from the resource to match exactly what is defined by I(tags) parameter}
    region: ${15:# The AWS region to use}
    secret_key: ${16:# AWS secret access key}
    session_token: ${17:# AWS STS session token for use with temporary credentials}
    state: ${18:# Whether to create or destroy an SNS topic}
    subscriptions: ${19:# List of subscriptions to apply to the topic}
    tags: ${20:# A dictionary representing the tags to be applied to the resource}
    topic_type: ${21:# The type of topic that should be created}
    validate_certs: ${22:# When set to C(false), SSL certificates will not be validated for communication with the AWS APIs}
  tags:
    - community
    - aws
    - sns_topic]]></content>
  <tabTrigger>community.aws.sns_topic</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages AWS SNS topics and subscriptions</description>
</snippet>