<snippet>
  <content><![CDATA[- name: ${1:Create, update, or destroy custom Automation Platform Controller credential type}
  awx.awx.tower_credential_type:
    name: ${2:# The name of the credential type}
    new_name: ${3:# Setting this option will change the existing name (looked up via the name field}
    description: ${4:# The description of the credential type to give more detail about it}
    kind: ${5:# The type of credential type being added}
    inputs: ${6:# Enter inputs using either JSON or YAML syntax}
    injectors: ${7:# Enter injectors using either JSON or YAML syntax}
    state: ${8:# Desired state of the resource}
    controller_host: ${9:# URL to your Automation Platform Controller instance}
    controller_username: ${10:# Username for your controller instance}
    controller_password: ${11:# Password for your controller instance}
    controller_oauthtoken: ${12:# The OAuth token to use}
    validate_certs: ${13:# Whether to allow insecure connections to AWX}
    controller_config_file: ${14:# Path to the controller config file}
  tags:
    - awx
    - awx
    - tower_credential_type]]></content>
  <tabTrigger>awx.awx.tower_credential_type</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create, update, or destroy custom Automation Platform Controller credential type</description>
</snippet>