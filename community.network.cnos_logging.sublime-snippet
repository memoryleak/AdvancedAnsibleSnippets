<snippet>
  <content><![CDATA[- name: ${1:Manage logging on network devices}
  community.network.cnos_logging:
    dest: ${2:# Destination of the logs}
    name: ${3:# If value of C(dest) is I(file) it indicates file-name and for I(server) indicates the server name to be notified}
    size: ${4:# Size of buffer}
    facility: ${5:# Set logging facility}
    level: ${6:# Set logging severity levels}
    aggregate: ${7:# List of logging definitions}
    state: ${8:# State of the logging configuration}
  tags:
    - community
    - network
    - cnos_logging]]></content>
  <tabTrigger>community.network.cnos_logging</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage logging on network devices</description>
</snippet>