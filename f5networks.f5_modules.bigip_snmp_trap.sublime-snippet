<snippet>
  <content><![CDATA[- name: ${1:Manipulate SNMP trap information on a BIG-IP}
  f5networks.f5_modules.bigip_snmp_trap:
    auth_password: ${2:# Specifies the Authentication protocol password to be used for snmp v3 traps}
    auth_protocol: ${3:# Specifies the Authentication protocol to be used for snmp v3 traps}
    community: ${4:# Specifies the community name for the trap destination}
    destination: ${5:# Specifies the address for the trap destination}
    name: ${6:# Name of the SNMP configuration endpoint}
    network: ${7:# Specifies the name of the trap network}
    partition: ${8:# Device partition to manage resources on}
    port: ${9:# Specifies the port for the trap destination}
    privacy_password: ${10:# Specifies the Privacy protocol password to be used for snmp v3 traps}
    privacy_protocol: ${11:# Specifies the Privacy protocol to be used for snmp v3 traps}
    provider: ${12:# A dict object containing connection details}
    security_level: ${13:# Specifies the port for the trap destination}
    security_name: ${14:# Specifies the security name to used for v3 snmp trap}
    snmp_version: ${15:# Specifies to which Simple Network Management Protocol (SNMP) version the trap destination applies}
    state: ${16:# When C(present), ensures the resource exists}
  tags:
    - f5networks
    - f5_modules
    - bigip_snmp_trap]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_snmp_trap</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manipulate SNMP trap information on a BIG-IP</description>
</snippet>