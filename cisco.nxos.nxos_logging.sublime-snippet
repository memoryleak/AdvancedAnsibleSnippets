<snippet>
  <content><![CDATA[- name: ${1:Manage logging on network devices}
  cisco.nxos.nxos_logging:
    aggregate: ${2:# List of logging definitions}
    dest: ${3:# Destination of the logs}
    dest_level: ${4:# Set logging severity levels}
    event: ${5:# Link/trunk enable/default interface configuration logging}
    facility: ${6:# Facility name for logging}
    facility_level: ${7:# Set logging severity levels for facility based log messages}
    facility_link_status: ${8:# Set logging facility ethpm link status}
    file_size: ${9:# Set logfile size}
    interface: ${10:# Interface to be used while configuring source-interface for logging (e}
    interface_message: ${11:# Add interface description to interface syslogs}
    name: ${12:# If value of C(dest) is I(logfile) it indicates file-name}
    purge: ${13:# Remove any switch logging configuration that does not match what has been configured Not supported for ansible_connection local}
    remote_server: ${14:# Hostname or IP Address for remote logging (when dest is 'server')}
    state: ${15:# State of the logging configuration}
    timestamp: ${16:# Set logging timestamp format}
    use_vrf: ${17:# VRF to be used while configuring remote logging (when dest is 'server')}
  tags:
    - cisco
    - nxos
    - nxos_logging]]></content>
  <tabTrigger>cisco.nxos.nxos_logging</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage logging on network devices</description>
</snippet>