<snippet>
  <content><![CDATA[- name: ${1:Manages service offerings on Apache CloudStack based clouds}
  ngine_io.cloudstack.cs_service_offering:
    disk_bytes_read_rate: ${2:# Bytes read rate of the disk offering}
    disk_bytes_write_rate: ${3:# Bytes write rate of the disk offering}
    cpu_number: ${4:# The number of CPUs of the service offering}
    cpu_speed: ${5:# The CPU speed of the service offering in MHz}
    limit_cpu_usage: ${6:# Restrict the CPU usage to committed service offering}
    deployment_planner: ${7:# The deployment planner heuristics used to deploy a VM of this offering}
    display_text: ${8:# Display text of the service offering}
    domain: ${9:# Domain the service offering is related to}
    host_tags: ${10:# The host tags for this service offering}
    hypervisor_snapshot_reserve: ${11:# Hypervisor snapshot reserve space as a percent of a volume}
    is_iops_customized: ${12:# Whether compute offering iops is custom or not}
    disk_iops_read_rate: ${13:# IO requests read rate of the disk offering}
    disk_iops_write_rate: ${14:# IO requests write rate of the disk offering}
    disk_iops_max: ${15:# Max}
    disk_iops_min: ${16:# Min}
    is_system: ${17:# Whether it is a system VM offering or not}
    is_volatile: ${18:# Whether the virtual machine needs to be volatile or not}
    memory: ${19:# The total memory of the service offering in MB}
    name: ${20:# Name of the service offering}
    network_rate: ${21:# Data transfer rate in Mb/s allowed}
    offer_ha: ${22:# Whether HA is set for the service offering}
    provisioning_type: ${23:# Provisioning type used to create volumes}
    service_offering_details: ${24:# Details for planner, used to store specific parameters}
    state: ${25:# State of the service offering}
    storage_type: ${26:# The storage type of the service offering}
    system_vm_type: ${27:# The system VM type}
    storage_tags: ${28:# The storage tags for this service offering}
    is_customized: ${29:# Whether the offering is customizable or not}
    api_key: ${30:# API key of the CloudStack API}
    api_secret: ${31:# Secret key of the CloudStack API}
    api_url: ${32:# URL of the CloudStack API e}
    api_http_method: ${33:# HTTP method used to query the API endpoint}
    api_timeout: ${34:# HTTP timeout in seconds}
    api_verify_ssl_cert: ${35:# Verify CA authority cert file}
  tags:
    - ngine_io
    - cloudstack
    - cs_service_offering]]></content>
  <tabTrigger>ngine_io.cloudstack.cs_service_offering</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages service offerings on Apache CloudStack based clouds</description>
</snippet>