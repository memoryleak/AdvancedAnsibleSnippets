<snippet>
  <content><![CDATA[- name: ${1:Manage NXAPI configuration on an NXOS device}
  cisco.nxos.nxos_nxapi:
    http_port: ${2:# Configure the port with which the HTTP server will listen on for requests}
    http: ${3:# Controls the operating state of the HTTP protocol as one of the underlying transports for NXAPI}
    https_port: ${4:# Configure the port with which the HTTPS server will listen on for requests}
    https: ${5:# Controls the operating state of the HTTPS protocol as one of the underlying transports for NXAPI}
    sandbox: ${6:# The NXAPI feature provides a web base UI for developers for entering commands}
    state: ${7:# The C(state) argument controls whether or not the NXAPI feature is configured on the remote device}
    ssl_strong_ciphers: ${8:# Controls the use of whether strong or weak ciphers are configured}
    tlsv1_0: ${9:# Controls the use of the Transport Layer Security version 1}
    tlsv1_1: ${10:# Controls the use of the Transport Layer Security version 1}
    tlsv1_2: ${11:# Controls the use of the Transport Layer Security version 1}
  tags:
    - cisco
    - nxos
    - nxos_nxapi]]></content>
  <tabTrigger>cisco.nxos.nxos_nxapi</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage NXAPI configuration on an NXOS device</description>
</snippet>