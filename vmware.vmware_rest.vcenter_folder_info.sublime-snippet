<snippet>
  <content><![CDATA[- name: ${1:Returns information about at most 1000 visible (subject to permission checks) folders in vCenter matching the @link FilterSpec}
  vmware.vmware_rest.vcenter_folder_info:
    datacenters: ${2:# Datacenters that must contain the folder for the folder to match the filter}
    folders: ${3:# Identifiers of folders that can match the filter}
    names: ${4:# Names that folders must have to match the filter (see @link Summary#name)}
    parent_folders: ${5:# Folders that must contain the folder for the folder to match the filter}
    session_timeout: ${6:# Timeout settings for client session}
    type: ${7:# The C(type) defines the type of a vCenter Server folder}
    vcenter_hostname: ${8:# The hostname or IP address of the vSphere vCenter}
    vcenter_password: ${9:# The vSphere vCenter password}
    vcenter_rest_log_file: ${10:# You can use this optional parameter to set the location of a log file}
    vcenter_username: ${11:# The vSphere vCenter username}
    vcenter_validate_certs: ${12:# Allows connection when SSL certificates are not valid}
  tags:
    - vmware
    - vmware_rest
    - vcenter_folder_info]]></content>
  <tabTrigger>vmware.vmware_rest.vcenter_folder_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Returns information about at most 1000 visible (subject to permission checks) folders in vCenter matching the @link FilterSpec</description>
</snippet>