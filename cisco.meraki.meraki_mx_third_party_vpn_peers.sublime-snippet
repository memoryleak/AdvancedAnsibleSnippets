<snippet>
  <content><![CDATA[- name: ${1:Manage third party (IPSec) VPN peers for MX devices}
  cisco.meraki.meraki_mx_third_party_vpn_peers:
    auth_key: ${2:# Authentication key provided by the dashboard}
    host: ${3:# Hostname for Meraki dashboard}
    internal_error_retry_time: ${4:# Number of seconds to retry if server returns an internal server error}
    org_id: ${5:# ID of organization}
    org_name: ${6:# Name of organization}
    output_format: ${7:# Instructs module whether response keys should be snake case (ex}
    output_level: ${8:# Set amount of debug output during module execution}
    peers: ${9:# The list of VPN peers}
    rate_limit_retry_time: ${10:# Number of seconds to retry if rate limiter is triggered}
    state: ${11:# Specifies whether object should be queried, created/modified, or removed}
    timeout: ${12:# Time to timeout for HTTP requests}
    use_https: ${13:# If C(no), it will use HTTP}
    use_proxy: ${14:# If C(no), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    validate_certs: ${15:# Whether to validate HTTP certificates}
  tags:
    - cisco
    - meraki
    - meraki_mx_third_party_vpn_peers]]></content>
  <tabTrigger>cisco.meraki.meraki_mx_third_party_vpn_peers</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage third party (IPSec) VPN peers for MX devices</description>
</snippet>