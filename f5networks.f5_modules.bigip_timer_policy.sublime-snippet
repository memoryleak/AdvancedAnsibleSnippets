<snippet>
  <content><![CDATA[- name: ${1:Manage timer policies on a BIG-IP}
  f5networks.f5_modules.bigip_timer_policy:
    description: ${2:# Specifies descriptive text that identifies the timer policy}
    name: ${3:# Specifies the name of the timer policy}
    partition: ${4:# Device partition to manage resources on}
    provider: ${5:# A dict object containing connection details}
    rules: ${6:# Rules you want assigned to the timer policy}
    state: ${7:# When C(present), ensures the resource exists}
  tags:
    - f5networks
    - f5_modules
    - bigip_timer_policy]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_timer_policy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage timer policies on a BIG-IP</description>
</snippet>