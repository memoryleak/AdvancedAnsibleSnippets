<snippet>
  <content><![CDATA[- name: ${1:Manage volumes on Dell PowerFlex}
  dellemc.powerflex.volume:
    allow_multiple_mappings: ${2:# Specifies whether to allow or not allow multiple mappings}
    auto_snap_remove_type: ${3:# Whether to remove or detach the snapshot policy}
    cap_unit: ${4:# The unit of the volume size}
    compression_type: ${5:# Type of the compression method}
    delete_snapshots: ${6:# If C(true), the volume and all its dependent snapshots will be deleted}
    hostname: ${7:# IP or FQDN of the PowerFlex host}
    password: ${8:# The password of the PowerFlex host}
    port: ${9:# Port number through which communication happens with PowerFlex host}
    protection_domain_id: ${10:# The ID of the protection domain}
    protection_domain_name: ${11:# The name of the protection domain}
    sdc: ${12:# Specifies SDC parameters}
    sdc_state: ${13:# Mapping state of the SDC}
    size: ${14:# The size of the volume}
    snapshot_policy_id: ${15:# ID of the snapshot policy}
    snapshot_policy_name: ${16:# Name of the snapshot policy}
    state: ${17:# State of the volume}
    storage_pool_id: ${18:# The ID of the storage pool}
    storage_pool_name: ${19:# The name of the storage pool}
    timeout: ${20:# Time after which connection will get terminated}
    use_rmcache: ${21:# Whether to use RM Cache or not}
    username: ${22:# The username of the PowerFlex host}
    validate_certs: ${23:# Boolean variable to specify whether or not to validate SSL certificate}
    vol_id: ${24:# The ID of the volume}
    vol_name: ${25:# The name of the volume}
    vol_new_name: ${26:# New name of the volume}
    vol_type: ${27:# Type of volume provisioning}
  tags:
    - dellemc
    - powerflex
    - volume]]></content>
  <tabTrigger>dellemc.powerflex.volume</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage volumes on Dell PowerFlex</description>
</snippet>