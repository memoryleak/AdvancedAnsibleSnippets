<snippet>
  <content><![CDATA[- name: ${1:Backup the current running or startup configuration to a remote server on devices running Lenovo CNOS}
  community.network.cnos_backup:
    configType: ${2:# This specifies what type of configuration will be backed up}
    deviceType: ${3:# This specifies the type of device where the method is executed}
    enablePassword: ${4:# Configures the password used to enter Global Configuration command mode on the switch}
    host: ${5:# This is the variable used to search the hosts file at /etc/ansible/hosts and identify the IP address of the device on which the template is going to be applied}
    outputfile: ${6:# This specifies the file path where the output of each command execution is saved}
    password: ${7:# Configures the password used to authenticate the connection to the remote device}
    protocol: ${8:# This refers to the protocol used by the network device to interact with the remote server to where to upload the backup configuration}
    rcpath: ${9:# This specifies the full file path where the configuration file will be copied on the remote server}
    rcserverip: ${10:# -This specifies the IP Address of the remote server to where the configuration will be backed up}
    serverpassword: ${11:# Specify the password for the server relating to the protocol used}
    serverusername: ${12:# Specify the username for the server relating to the protocol used}
    username: ${13:# Configures the username used to authenticate the connection to the remote device}
  tags:
    - community
    - network
    - cnos_backup]]></content>
  <tabTrigger>community.network.cnos_backup</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Backup the current running or startup configuration to a remote server on devices running Lenovo CNOS</description>
</snippet>