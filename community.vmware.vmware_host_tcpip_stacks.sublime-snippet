<snippet>
  <content><![CDATA[- name: ${1:Manage the TCP/IP Stacks configuration of ESXi host}
  community.vmware.vmware_host_tcpip_stacks:
    default: ${2:# The TCP/IP stacks configuration of the I(default)}
    esxi_hostname: ${3:# Name of the ESXi host}
    hostname: ${4:# The hostname or IP address of the vSphere vCenter or ESXi server}
    password: ${5:# The password of the vSphere vCenter or ESXi server}
    port: ${6:# The port number of the vSphere vCenter or ESXi server}
    provisioning: ${7:# The TCP/IP stacks configuration of the I(provisioning)}
    proxy_host: ${8:# Address of a proxy that will receive all HTTPS requests and relay them}
    proxy_port: ${9:# Port of the HTTP proxy that will receive all HTTPS requests and relay them}
    username: ${10:# The username of the vSphere vCenter or ESXi server}
    validate_certs: ${11:# Allows connection when SSL certificates are not valid}
    vmotion: ${12:# The TCP/IP stacks configuration of the I(vmotion)}
    vxlan: ${13:# The TCP/IP stacks configuration of the I(vxlan)}
  tags:
    - community
    - vmware
    - vmware_host_tcpip_stacks]]></content>
  <tabTrigger>community.vmware.vmware_host_tcpip_stacks</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage the TCP/IP Stacks configuration of ESXi host</description>
</snippet>