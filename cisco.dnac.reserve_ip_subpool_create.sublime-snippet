<snippet>
  <content><![CDATA[- name: ${1:Resource module for Reserve Ip Subpool Create}
  cisco.dnac.reserve_ip_subpool_create:
    dnac_debug: ${2:# Flag for Cisco DNA Center SDK to enable debugging}
    dnac_host: ${3:# The Cisco DNA Center hostname}
    dnac_password: ${4:# The Cisco DNA Center password to authenticate}
    dnac_port: ${5:# The Cisco DNA Center port}
    dnac_username: ${6:# The Cisco DNA Center username to authenticate}
    dnac_verify: ${7:# Flag to enable or disable SSL certificate verification}
    dnac_version: ${8:# Informs the SDK which version of Cisco DNA Center to use}
    ipv4DhcpServers: ${9:# IPv4 input for dhcp server ip example 1}
    ipv4DnsServers: ${10:# IPv4 input for dns server ip example 4}
    ipv4GateWay: ${11:# Gateway ip address details, example 175}
    ipv4GlobalPool: ${12:# IP v4 Global pool address with cidr, example 175}
    ipv4Prefix: ${13:# IPv4 prefix value is true, the ip4 prefix length input field is enabled , if it is false ipv4 total Host input is enable}
    ipv4PrefixLength: ${14:# The ipv4 prefix length is required when ipv4prefix value is true}
    ipv4Subnet: ${15:# IPv4 Subnet address, example 175}
    ipv4TotalHost: ${16:# IPv4 total host is required when ipv4prefix value is false}
    ipv6AddressSpace: ${17:# If the value is false only ipv4 input are required, otherwise both ipv6 and ipv4 are required}
    ipv6DhcpServers: ${18:# IPv6 format dhcp server as input example 2001 db8 1234}
    ipv6DnsServers: ${19:# IPv6 format dns server input example 2001 db8 1234}
    ipv6GateWay: ${20:# Gateway ip address details, example 2001 db8 85a3 0 100 1}
    ipv6GlobalPool: ${21:# IPv6 Global pool address with cidr this is required when Ipv6AddressSpace value is true, example 2001 db8 85a3 /64}
    ipv6Prefix: ${22:# Ipv6 prefix value is true, the ip6 prefix length input field is enabled , if it is false ipv6 total Host input is enable}
    ipv6PrefixLength: ${23:# IPv6 prefix length is required when the ipv6prefix value is true}
    ipv6Subnet: ${24:# IPv6 Subnet address, example 2001 db8 85a3 0 100}
    ipv6TotalHost: ${25:# IPv6 total host is required when ipv6prefix value is false}
    name: ${26:# Name of the reserve ip sub pool}
    siteId: ${27:# SiteId path parameter}
    slaacSupport: ${28:# Slaac Support}
    type: ${29:# Type of the reserve ip sub pool}
    validate_response_schema: ${30:# Flag for Cisco DNA Center SDK to enable the validation of request bodies against a JSON schema}
  tags:
    - cisco
    - dnac
    - reserve_ip_subpool_create]]></content>
  <tabTrigger>cisco.dnac.reserve_ip_subpool_create</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for Reserve Ip Subpool Create</description>
</snippet>