<snippet>
  <content><![CDATA[- name: ${1:Resource module for Reserve Ip Subpool Create}
  cisco.dnac.reserve_ip_subpool_create:
    ipv4DhcpServers: ${2:# IPv4 input for dhcp server ip example 1}
    ipv4DnsServers: ${3:# IPv4 input for dns server ip example 4}
    ipv4GateWay: ${4:# Gateway ip address details, example 175}
    ipv4GlobalPool: ${5:# IP v4 Global pool address with cidr, example 175}
    ipv4Prefix: ${6:# IPv4 prefix value is true, the ip4 prefix length input field is enabled , if it is false ipv4 total Host input is enable}
    ipv4PrefixLength: ${7:# The ipv4 prefix length is required when ipv4prefix value is true}
    ipv4Subnet: ${8:# IPv4 Subnet address, example 175}
    ipv4TotalHost: ${9:# IPv4 total host is required when ipv4prefix value is false}
    ipv6AddressSpace: ${10:# If the value is false only ipv4 input are required, otherwise both ipv6 and ipv4 are required}
    ipv6DhcpServers: ${11:# IPv6 format dhcp server as input example 2001 db8 1234}
    ipv6DnsServers: ${12:# IPv6 format dns server input example 2001 db8 1234}
    ipv6GateWay: ${13:# Gateway ip address details, example 2001 db8 85a3 0 100 1}
    ipv6GlobalPool: ${14:# IPv6 Global pool address with cidr this is required when Ipv6AddressSpace value is true, example 2001 db8 85a3 /64}
    ipv6Prefix: ${15:# Ipv6 prefix value is true, the ip6 prefix length input field is enabled , if it is false ipv6 total Host input is enable}
    ipv6PrefixLength: ${16:# IPv6 prefix length is required when the ipv6prefix value is true}
    ipv6Subnet: ${17:# IPv6 Subnet address, example 2001 db8 85a3 0 100}
    ipv6TotalHost: ${18:# IPv6 total host is required when ipv6prefix value is false}
    name: ${19:# Name of the reserve ip sub pool}
    siteId: ${20:# SiteId path parameter}
    slaacSupport: ${21:# Slaac Support}
    type: ${22:# Type of the reserve ip sub pool}
    dnac_host: ${23:# The Cisco DNA Center hostname}
    dnac_port: ${24:# The Cisco DNA Center port}
    dnac_username: ${25:# The Cisco DNA Center username to authenticate}
    dnac_password: ${26:# The Cisco DNA Center password to authenticate}
    dnac_verify: ${27:# Flag to enable or disable SSL certificate verification}
    dnac_version: ${28:# Informs the SDK which version of Cisco DNA Center to use}
    dnac_debug: ${29:# Flag for Cisco DNA Center SDK to enable debugging}
    validate_response_schema: ${30:# Flag for Cisco DNA Center SDK to enable the validation of request bodies against a JSON schema}
  tags:
    - cisco
    - dnac
    - reserve_ip_subpool_create]]></content>
  <tabTrigger>cisco.dnac.reserve_ip_subpool_create</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for Reserve Ip Subpool Create</description>
</snippet>