<snippet>
  <content><![CDATA[- name: ${1:Fetch configuration/state data from gRPC enabled target hosts}
  ansible.netcommon.grpc_config:
    backup: ${2:# This argument will cause the module to create a full backup of the current C(running-config) from the remote device before any changes are made}
    backup_options: ${3:# This is a dict object containing configurable options related to backup file path}
    config: ${4:# This option specifies the string which acts as a filter to restrict the portions of the data to be retrieved from the target host device}
    state: ${5:# action to be performed}
  tags:
    - grpc_config]]></content>
  <tabTrigger>ansible.netcommon.grpc_config</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Fetch configuration/state data from gRPC enabled target hosts</description>
</snippet>