<snippet>
  <content><![CDATA[- name: ${1:Create or remove a Port Mirroring session}
  community.vmware.vmware_vspan_session:
    description: ${2:# The description for the session}
    destination_port: ${3:# Destination port that received the mirrored packets}
    destination_vm: ${4:# With this parameter it is possible, to add a NIC of a VM to a port mirroring session}
    enabled: ${5:# Whether the session is enabled}
    encapsulation_vlan_id: ${6:# VLAN ID used to encapsulate the mirrored traffic}
    hostname: ${7:# The hostname or IP address of the vSphere vCenter or ESXi server}
    mirrored_packet_length: ${8:# An integer that describes how much of each frame to mirror}
    name: ${9:# Name of the session}
    normal_traffic_allowed: ${10:# Whether or not destination ports can send and receive 'normal' traffic}
    password: ${11:# The password of the vSphere vCenter or ESXi server}
    port: ${12:# The port number of the vSphere vCenter or ESXi server}
    proxy_host: ${13:# Address of a proxy that will receive all HTTPS requests and relay them}
    proxy_port: ${14:# Port of the HTTP proxy that will receive all HTTPS requests and relay them}
    sampling_rate: ${15:# Sampling rate of the session}
    session_type: ${16:# Select the mirroring type}
    source_port_received: ${17:# Source port for which received packets are mirrored}
    source_port_transmitted: ${18:# Source port for which transmitted packets are mirrored}
    source_vm_received: ${19:# With this parameter it is possible, to add a NIC of a VM to a port mirroring session}
    source_vm_transmitted: ${20:# With this parameter it is possible, to add a NIC of a VM to a port mirroring session}
    state: ${21:# Create or remove the session}
    strip_original_vlan: ${22:# Whether to strip the original VLAN tag}
    switch: ${23:# The name of the distributed vSwitch on which to add or remove the mirroring session}
    username: ${24:# The username of the vSphere vCenter or ESXi server}
    validate_certs: ${25:# Allows connection when SSL certificates are not valid}
  tags:
    - community
    - vmware
    - vmware_vspan_session]]></content>
  <tabTrigger>community.vmware.vmware_vspan_session</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create or remove a Port Mirroring session</description>
</snippet>