<snippet>
  <content><![CDATA[- name: ${1:Manages VPCs on Apache CloudStack based clouds}
  ngine_io.cloudstack.cs_vpc:
    account: ${2:# Account the VPC is related to}
    api_http_method: ${3:# HTTP method used to query the API endpoint}
    api_key: ${4:# API key of the CloudStack API}
    api_secret: ${5:# Secret key of the CloudStack API}
    api_timeout: ${6:# HTTP timeout in seconds}
    api_url: ${7:# URL of the CloudStack API e}
    api_verify_ssl_cert: ${8:# Verify CA authority cert file}
    cidr: ${9:# CIDR of the VPC, e}
    clean_up: ${10:# Whether to redeploy a VPC router or not when I(state=restarted)}
    display_text: ${11:# Display text of the VPC}
    domain: ${12:# Domain the VPC is related to}
    name: ${13:# Name of the VPC}
    network_domain: ${14:# Network domain for the VPC}
    poll_async: ${15:# Poll async jobs until job has finished}
    project: ${16:# Name of the project the VPC is related to}
    state: ${17:# State of the VPC}
    tags: ${18:# List of tags}
    vpc_offering: ${19:# Name of the VPC offering}
    zone: ${20:# Name of the zone}
  tags:
    - ngine_io
    - cloudstack
    - cs_vpc]]></content>
  <tabTrigger>ngine_io.cloudstack.cs_vpc</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages VPCs on Apache CloudStack based clouds</description>
</snippet>