<snippet>
  <content><![CDATA[- name: ${1:Manage bandwidth resource control and priority for protocols, services and zones on Solaris/illumos systems}
  community.network.flowadm:
    dsfield: ${2:# - Identifies the 8-bit differentiated services field (as defined in RFC 2474)}
    link: ${3:# Specifiies a link to configure flow on}
    local_ip: ${4:# Identifies a network flow by the local IP address}
    local_port: ${5:# Identifies a service specified by the local port}
    maxbw: ${6:# - Sets the full duplex bandwidth for the flow}
    name: ${7:# - A flow is defined as a set of attributes based on Layer 3 and Layer 4 headers, which can be used to identify a protocol, service, or a zone}
    priority: ${8:# Sets the relative priority for the flow}
    remote_ip: ${9:# Identifies a network flow by the remote IP address}
    state: ${10:# Create/delete/enable/disable an IP address on the network interface}
    temporary: ${11:# Specifies that the configured flow is temporary}
    transport: ${12:# - Specifies a Layer 4 protocol to be used}
  tags:
    - community
    - network
    - flowadm]]></content>
  <tabTrigger>community.network.flowadm</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage bandwidth resource control and priority for protocols, services and zones on Solaris/illumos systems</description>
</snippet>