<snippet>
  <content><![CDATA[- name: ${1:Manage Fabric Node Members (fabric:NodeIdentP)}
  cisco.aci.aci_fabric_node:
    annotation: ${2:# User-defined string for annotating an object}
    certificate_name: ${3:# The X}
    description: ${4:# Description for the new Fabric Node Member}
    host: ${5:# IP Address or hostname of APIC resolvable by Ansible control host}
    name_alias: ${6:# The alias for the current object}
    node_id: ${7:# Node ID Number for the new Fabric Node Member}
    node_type: ${8:# Type for the new Fabric Node Member}
    output_level: ${9:# Influence the output of this ACI module}
    output_path: ${10:# Path to a file that will be used to dump the ACI JSON configuration objects generated by the module}
    password: ${11:# The password to use for authentication}
    pod_id: ${12:# The pod id of the new Fabric Node Member}
    port: ${13:# Port number to be used for REST connection}
    private_key: ${14:# Either a PEM-formatted private key file or the private key content used for signature-based authentication}
    remote_leaf_pool_id: ${15:# External Pool Id of the remote leaf}
    role: ${16:# Role for the new Fabric Node Member}
    serial: ${17:# Serial Number for the new Fabric Node Member}
    state: ${18:# Use C(present) or C(absent) for adding or removing}
    switch: ${19:# Switch Name for the new Fabric Node Member}
    timeout: ${20:# The socket level timeout in seconds}
    use_proxy: ${21:# If C(false), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_ssl: ${22:# If C(false), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${23:# The username to use for authentication}
    validate_certs: ${24:# If C(false), SSL certificates will not be validated}
  tags:
    - cisco
    - aci
    - aci_fabric_node]]></content>
  <tabTrigger>cisco.aci.aci_fabric_node</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Fabric Node Members (fabric:NodeIdentP)</description>
</snippet>