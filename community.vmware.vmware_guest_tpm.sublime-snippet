<snippet>
  <content><![CDATA[- name: ${1:Add or remove vTPM device for specified VM}
  community.vmware.vmware_guest_tpm:
    datacenter: ${2:# The vCenter datacenter name used to get specified cluster or host}
    folder: ${3:# VM folder, absolute or relative path to find an existing VM}
    hostname: ${4:# The hostname or IP address of the vSphere vCenter or ESXi server}
    moid: ${5:# Managed Object ID of the instance to manage if known, this is a unique identifier only within a single vCenter instance}
    name: ${6:# Name of the virtual machine}
    password: ${7:# The password of the vSphere vCenter or ESXi server}
    port: ${8:# The port number of the vSphere vCenter or ESXi server}
    proxy_host: ${9:# Address of a proxy that will receive all HTTPS requests and relay them}
    proxy_port: ${10:# Port of the HTTP proxy that will receive all HTTPS requests and relay them}
    state: ${11:# State of vTPM device}
    username: ${12:# The username of the vSphere vCenter or ESXi server}
    uuid: ${13:# UUID of the instance to manage if known, this is VMware's unique identifier}
    validate_certs: ${14:# Allows connection when SSL certificates are not valid}
  tags:
    - community
    - vmware
    - vmware_guest_tpm]]></content>
  <tabTrigger>community.vmware.vmware_guest_tpm</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Add or remove vTPM device for specified VM</description>
</snippet>