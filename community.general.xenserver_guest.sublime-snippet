<snippet>
  <content><![CDATA[- name: ${1:Manages virtual machines running on Citrix Hypervisor/XenServer host or pool}
  community.general.xenserver_guest:
    cdrom: ${2:# A CD-ROM configuration for the VM}
    custom_params: ${3:# Define a list of custom VM params to set on VM}
    disks: ${4:# A list of disks to add to VM}
    folder: ${5:# Destination folder for VM}
    force: ${6:# Ignore warnings and complete the actions}
    hardware: ${7:# Manage VM's hardware parameters}
    home_server: ${8:# Name of a XenServer host that will be a Home Server for the VM}
    hostname: ${9:# The hostname or IP address of the XenServer host or XenServer pool master}
    is_template: ${10:# Convert VM to template}
    linked_clone: ${11:# Whether to create a Linked Clone from the template, existing VM or snapshot}
    name: ${12:# Name of the VM to work with}
    name_desc: ${13:# VM description}
    networks: ${14:# A list of networks (in the order of the NICs)}
    password: ${15:# The password to use for connecting to XenServer}
    state: ${16:# Specify the state VM should be in}
    state_change_timeout: ${17:# By default, module will wait indefinitely for VM to acquire an IP address if O(wait_for_ip_address=true)}
    template: ${18:# Name of a template, an existing VM (must be shut down) or a snapshot that should be used to create VM}
    template_uuid: ${19:# UUID of a template, an existing VM or a snapshot that should be used to create VM}
    username: ${20:# The username to use for connecting to XenServer}
    uuid: ${21:# UUID of the VM to manage if known}
    validate_certs: ${22:# Allows connection when SSL certificates are not valid}
    wait_for_ip_address: ${23:# Wait until XenServer detects an IP address for the VM}
  tags:
    - community
    - general
    - xenserver_guest]]></content>
  <tabTrigger>community.general.xenserver_guest</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages virtual machines running on Citrix Hypervisor/XenServer host or pool</description>
</snippet>