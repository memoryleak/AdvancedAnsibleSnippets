<snippet>
  <content><![CDATA[- name: ${1:Manage Service Graph in schema templates}
  cisco.mso.mso_schema_template_service_graph:
    description: ${2:# The description of Service Graph}
    display_name: ${3:# The name as displayed on the MSO web interface}
    filter_after_first_node: ${4:# The filter applied after the first node}
    host: ${5:# IP Address or hostname of the ACI Multi Site Orchestrator host}
    login_domain: ${6:# The login domain name to use for authentication}
    output_level: ${7:# Influence the output of this MSO module}
    password: ${8:# The password to use for authentication}
    port: ${9:# Port number to be used for the REST connection}
    schema: ${10:# The name of the schema}
    service_graph: ${11:# The name of the Service Graph to manage}
    service_nodes: ${12:# A list of node types to be associated with the Service Graph}
    state: ${13:# Use C(present) or C(absent) for adding or removing}
    template: ${14:# The name of the template}
    timeout: ${15:# The socket level timeout in seconds}
    use_proxy: ${16:# If C(false), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_ssl: ${17:# If C(false), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${18:# The username to use for authentication}
    validate_certs: ${19:# If C(false), SSL certificates will not be validated}
  tags:
    - cisco
    - mso
    - mso_schema_template_service_graph]]></content>
  <tabTrigger>cisco.mso.mso_schema_template_service_graph</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Service Graph in schema templates</description>
</snippet>