<snippet>
  <content><![CDATA[- name: ${1:Manage members in a OpenStack load-balancer pool}
  openstack.cloud.lb_member:
    address: ${2:# The IP address of the member}
    api_timeout: ${3:# How long should the socket layer wait before timing out for API calls}
    auth: ${4:# Dictionary containing auth information as needed by the cloud's auth plugin strategy}
    auth_type: ${5:# Name of the auth plugin to use}
    ca_cert: ${6:# A path to a CA Cert bundle that can be used as part of verifying SSL API requests}
    client_cert: ${7:# A path to a client certificate to use as part of the SSL transaction}
    client_key: ${8:# A path to a client key to use as part of the SSL transaction}
    cloud: ${9:# Named cloud or cloud config to operate against}
    interface: ${10:# Endpoint URL type to fetch from the service catalog}
    monitor_address: ${11:# IP address used to monitor this member}
    monitor_port: ${12:# Port used to monitor this member}
    name: ${13:# Name that has to be given to the member}
    pool: ${14:# The name or id of the pool that this member belongs to}
    protocol_port: ${15:# The protocol port number for the member}
    region_name: ${16:# Name of the region}
    sdk_log_level: ${17:# Log level of the OpenStackSDK}
    sdk_log_path: ${18:# Path to the logfile of the OpenStackSDK}
    state: ${19:# Should the resource be C(present) or C(absent)}
    subnet_id: ${20:# The subnet ID the member service is accessible from}
    timeout: ${21:# How long should ansible wait for the requested resource}
    validate_certs: ${22:# Whether or not SSL API requests should be verified}
    wait: ${23:# Should ansible wait until the requested resource is complete}
    weight: ${24:# The weight of a member determines the portion of requests or connections it services compared to the other members of the pool}
  tags:
    - openstack
    - cloud
    - lb_member]]></content>
  <tabTrigger>openstack.cloud.lb_member</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage members in a OpenStack load-balancer pool</description>
</snippet>