<snippet>
  <content><![CDATA[- name: ${1:Pull images for use by podman}
  containers.podman.podman_image:
    arch: ${2:# CPU architecture for the container image}
    auth_file: ${3:# Path to file containing authorization credentials to the remote registry}
    build: ${4:# Arguments that control image build}
    ca_cert_dir: ${5:# Path to directory containing TLS certificates and keys to use}
    executable: ${6:# Path to C(podman) executable if it is not in the C($PATH) on the machine running C(podman)}
    force: ${7:# Whether or not to force push or pull an image}
    name: ${8:# Name of the image to pull, push, or delete}
    password: ${9:# Password to use when authenticating to remote registries}
    path: ${10:# Path to the build context directory}
    pull: ${11:# Whether or not to pull the image}
    pull_extra_args: ${12:# Extra arguments to pass to the pull command}
    push: ${13:# Whether or not to push an image}
    push_args: ${14:# Arguments that control pushing images}
    quadlet_dir: ${15:# Path to the directory to write quadlet file in}
    quadlet_file_mode: ${16:# The permissions of the quadlet file}
    quadlet_filename: ${17:# Name of quadlet file to write}
    quadlet_options: ${18:# Options for the quadlet file}
    state: ${19:# Whether an image should be present, absent, or built}
    tag: ${20:# Tag of the image to pull, push, or delete}
    username: ${21:# username to use when authenticating to remote registries}
    validate_certs: ${22:# Require HTTPS and validate certificates when pulling or pushing}
  tags:
    - containers
    - podman
    - podman_image]]></content>
  <tabTrigger>containers.podman.podman_image</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Pull images for use by podman</description>
</snippet>