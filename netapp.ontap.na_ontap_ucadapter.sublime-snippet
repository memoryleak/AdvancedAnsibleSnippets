<snippet>
  <content><![CDATA[- name: ${1:NetApp ONTAP UC adapter configuration}
  netapp.ontap.na_ontap_ucadapter:
    adapter_name: ${2:# Specifies the adapter name}
    cert_filepath: ${3:# path to SSL client cert file (}
    feature_flags: ${4:# Enable or disable a new feature}
    force_ontap_version: ${5:# Override the cluster ONTAP version when using REST}
    hostname: ${6:# The hostname or IP address of the ONTAP instance}
    http_port: ${7:# Override the default port (80 or 443) with this port}
    https: ${8:# Enable and disable https}
    key_filepath: ${9:# path to SSL client key file}
    mode: ${10:# Specifies the mode of the adapter}
    node_name: ${11:# Specifies the adapter home node}
    ontapi: ${12:# The ontap api version to use}
    pair_adapters: ${13:# Specifies the list of adapters which also need to be offline along with the current adapter during modifying}
    password: ${14:# Password for the specified user}
    state: ${15:# Whether the specified adapter should exist}
    type: ${16:# Specifies the fc4 type of the adapter}
    use_rest: ${17:# Whether to use REST or ZAPI}
    username: ${18:# This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required}
    validate_certs: ${19:# If set to C(no), the SSL certificates will not be validated}
  tags:
    - netapp
    - ontap
    - na_ontap_ucadapter]]></content>
  <tabTrigger>netapp.ontap.na_ontap_ucadapter</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp ONTAP UC adapter configuration</description>
</snippet>