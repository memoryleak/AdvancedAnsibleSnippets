<snippet>
  <content><![CDATA[- name: ${1:Manages port forwarding rules on Apache CloudStack based clouds}
  ngine_io.cloudstack.cs_portforward:
    account: ${2:# Account the I(vm) is related to}
    api_http_method: ${3:# HTTP method used to query the API endpoint}
    api_key: ${4:# API key of the CloudStack API}
    api_secret: ${5:# Secret key of the CloudStack API}
    api_timeout: ${6:# HTTP timeout in seconds}
    api_url: ${7:# URL of the CloudStack API e}
    api_verify_ssl_cert: ${8:# Verify CA authority cert file}
    domain: ${9:# Domain the I(vm) is related to}
    ip_address: ${10:# Public IP address the rule is assigned to}
    network: ${11:# Name of the network}
    open_firewall: ${12:# Whether the firewall rule for public port should be created, while creating the new rule}
    poll_async: ${13:# Poll async jobs until job has finished}
    private_end_port: ${14:# End private port for this rule}
    private_port: ${15:# Start private port for this rule}
    project: ${16:# Name of the project the I(vm) is located in}
    protocol: ${17:# Protocol of the port forwarding rule}
    public_end_port: ${18:# End public port for this rule}
    public_port: ${19:# Start public port for this rule}
    state: ${20:# State of the port forwarding rule}
    tags: ${21:# List of tags}
    vm: ${22:# Name of virtual machine which we make the port forwarding rule for}
    vm_guest_ip: ${23:# VM guest NIC secondary IP address for the port forwarding rule}
    vpc: ${24:# Name of the VPC}
    zone: ${25:# Name of the zone in which the virtual machine is in}
  tags:
    - ngine_io
    - cloudstack
    - cs_portforward]]></content>
  <tabTrigger>ngine_io.cloudstack.cs_portforward</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages port forwarding rules on Apache CloudStack based clouds</description>
</snippet>