<snippet>
  <content><![CDATA[- name: ${1:Creates a GCP VpnTunnel}
  google.cloud.gcp_compute_vpn_tunnel:
    access_token: ${2:# An OAuth2 access token if credential type is accesstoken}
    auth_kind: ${3:# The type of credential used}
    description: ${4:# An optional description of this resource}
    env_type: ${5:# Specifies which Ansible environment you're running this module within}
    ike_version: ${6:# IKE protocol version to use when establishing the VPN tunnel with peer VPN gateway}
    local_traffic_selector: ${7:# Local traffic selector to use when establishing the VPN tunnel with peer VPN gateway}
    name: ${8:# Name of the resource}
    peer_external_gateway: ${9:# URL of the peer side external VPN gateway to which this VPN tunnel is connected}
    peer_external_gateway_interface: ${10:# The interface ID of the external VPN gateway to which this VPN tunnel is connected}
    peer_gcp_gateway: ${11:# URL of the peer side HA GCP VPN gateway to which this VPN tunnel is connected}
    peer_ip: ${12:# IP address of the peer VPN gateway}
    project: ${13:# The Google Cloud Platform project to use}
    region: ${14:# The region where the tunnel is located}
    remote_traffic_selector: ${15:# Remote traffic selector to use when establishing the VPN tunnel with peer VPN gateway}
    router: ${16:# URL of router resource to be used for dynamic routing}
    scopes: ${17:# Array of scopes to be used}
    service_account_contents: ${18:# The contents of a Service Account JSON file, either in a dictionary or as a JSON string that represents it}
    service_account_email: ${19:# An optional service account email address if machineaccount is selected and the user does not wish to use the default email}
    service_account_file: ${20:# The path of a Service Account JSON file if serviceaccount is selected as type}
    shared_secret: ${21:# Shared secret used to set the secure session between the Cloud VPN gateway and the peer VPN gateway}
    state: ${22:# Whether the given object should exist in GCP}
    target_vpn_gateway: ${23:# URL of the Target VPN gateway with which this VPN tunnel is associated}
    vpn_gateway: ${24:# URL of the VPN gateway with which this VPN tunnel is associated}
    vpn_gateway_interface: ${25:# The interface ID of the VPN gateway with which this VPN tunnel is associated}
  tags:
    - google
    - cloud
    - gcp_compute_vpn_tunnel]]></content>
  <tabTrigger>google.cloud.gcp_compute_vpn_tunnel</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Creates a GCP VpnTunnel</description>
</snippet>