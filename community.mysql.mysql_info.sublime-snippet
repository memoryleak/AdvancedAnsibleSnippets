<snippet>
  <content><![CDATA[- name: ${1:Gather information about MySQL servers}
  community.mysql.mysql_info:
    ca_cert: ${2:# The path to a Certificate Authority (CA) certificate}
    check_hostname: ${3:# Whether to validate the server host name when an SSL connection is required}
    client_cert: ${4:# The path to a client public key certificate}
    client_key: ${5:# The path to the client private key}
    config_file: ${6:# Specify a config file from which user and password are to be read}
    connect_timeout: ${7:# The connection timeout when connecting to the MySQL server}
    exclude_fields: ${8:# List of fields which are not needed to collect}
    filter: ${9:# Limit the collected information by comma separated string or YAML list}
    login_db: ${10:# Database name to connect to}
    login_host: ${11:# Host running the database}
    login_password: ${12:# The password used to authenticate with}
    login_port: ${13:# Port of the MySQL server}
    login_unix_socket: ${14:# The path to a Unix domain socket for local connections}
    login_user: ${15:# The username used to authenticate with}
    return_empty_dbs: ${16:# Includes names of empty databases to returned dictionary}
  tags:
    - community
    - mysql
    - mysql_info]]></content>
  <tabTrigger>community.mysql.mysql_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Gather information about MySQL servers</description>
</snippet>