<snippet>
  <content><![CDATA[- name: ${1:Manages network-feed objects on Checkpoint over Web Services API}
  check_point.mgmt.cp_mgmt_network_feed:
    name: ${2:# Object name}
    feed_url: ${3:# URL of the feed}
    certificate_id: ${4:# Certificate SHA-1 fingerprint to access the feed}
    feed_format: ${5:# Feed file format}
    feed_type: ${6:# Feed type to be enforced}
    password: ${7:# password for authenticating with the URL}
    tags: ${8:# Collection of tag identifiers}
    username: ${9:# username for authenticating with the URL}
    custom_header: ${10:# Headers to allow different authentication methods with the URL}
    update_interval: ${11:# Interval in minutes for updating the feed on the Security Gateway}
    data_column: ${12:# Number of the column that contains the feed's data}
    fields_delimiter: ${13:# The delimiter that separates between the columns in the feed}
    ignore_lines_that_start_with: ${14:# A prefix that will determine which lines to ignore}
    json_query: ${15:# JQ query to be parsed}
    use_gateway_proxy: ${16:# Use the gateway's proxy for retrieving the feed}
    color: ${17:# Color of the object}
    comments: ${18:# Comments string}
    details_level: ${19:# The level of detail for some of the fields in the response can vary from showing only the UID value of the object to a fully detailed representation of the object}
    domains_to_process: ${20:# Indicates which domains to process the commands on}
    ignore_warnings: ${21:# Apply changes ignoring warnings}
    ignore_errors: ${22:# Apply changes ignoring errors}
    state: ${23:# State of the access rule (present or absent)}
    auto_publish_session: ${24:# Publish the current session if changes have been performed after task completes}
    wait_for_task: ${25:# Wait for the task to end}
    wait_for_task_timeout: ${26:# How many minutes to wait until throwing a timeout error}
    version: ${27:# Version of checkpoint}
  tags:
    - check_point
    - mgmt
    - cp_mgmt_network_feed]]></content>
  <tabTrigger>check_point.mgmt.cp_mgmt_network_feed</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages network-feed objects on Checkpoint over Web Services API</description>
</snippet>