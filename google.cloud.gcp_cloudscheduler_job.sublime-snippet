<snippet>
  <content><![CDATA[- name: ${1:Creates a GCP Job}
  google.cloud.gcp_cloudscheduler_job:
    access_token: ${2:# An OAuth2 access token if credential type is accesstoken}
    app_engine_http_target: ${3:# App Engine HTTP target}
    attempt_deadline: ${4:# The deadline for job attempts}
    auth_kind: ${5:# The type of credential used}
    description: ${6:# A human-readable description for the job}
    env_type: ${7:# Specifies which Ansible environment you're running this module within}
    http_target: ${8:# HTTP target}
    name: ${9:# The name of the job}
    project: ${10:# The Google Cloud Platform project to use}
    pubsub_target: ${11:# Pub/Sub target If the job providers a Pub/Sub target the cron will publish a message to the provided topic}
    region: ${12:# Region where the scheduler job resides}
    retry_config: ${13:# By default, if a job does not complete successfully, meaning that an acknowledgement is not received from the handler, then it will be retried with exponential backoff according to the settings}
    schedule: ${14:# Describes the schedule on which the job will be executed}
    scopes: ${15:# Array of scopes to be used}
    service_account_contents: ${16:# The contents of a Service Account JSON file, either in a dictionary or as a JSON string that represents it}
    service_account_email: ${17:# An optional service account email address if machineaccount is selected and the user does not wish to use the default email}
    service_account_file: ${18:# The path of a Service Account JSON file if serviceaccount is selected as type}
    state: ${19:# Whether the given object should exist in GCP}
    time_zone: ${20:# Specifies the time zone to be used in interpreting schedule}
  tags:
    - google
    - cloud
    - gcp_cloudscheduler_job]]></content>
  <tabTrigger>google.cloud.gcp_cloudscheduler_job</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Creates a GCP Job</description>
</snippet>