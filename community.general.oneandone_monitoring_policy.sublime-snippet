<snippet>
  <content><![CDATA[- name: ${1:Configure 1&1 monitoring policy}
  community.general.oneandone_monitoring_policy:
    add_ports: ${2:# Ports to add to the monitoring policy}
    add_processes: ${3:# Processes to add to the monitoring policy}
    add_servers: ${4:# Servers to add to the monitoring policy}
    agent: ${5:# Set true for using agent}
    api_url: ${6:# Custom API URL}
    auth_token: ${7:# Authenticating API token provided by 1&1}
    description: ${8:# Monitoring policy description}
    email: ${9:# User's email}
    monitoring_policy: ${10:# The identifier (id or name) of the monitoring policy used with update state}
    name: ${11:# Monitoring policy name used with present state}
    ports: ${12:# Array of ports that will be monitoring}
    processes: ${13:# Array of processes that will be monitoring}
    remove_ports: ${14:# Ports to remove from the monitoring policy}
    remove_processes: ${15:# Processes to remove from the monitoring policy}
    remove_servers: ${16:# Servers to remove from the monitoring policy}
    state: ${17:# Define a monitoring policy's state to create, remove, update}
    thresholds: ${18:# Monitoring policy thresholds}
    update_ports: ${19:# Ports to be updated on the monitoring policy}
    update_processes: ${20:# Processes to be updated on the monitoring policy}
    wait: ${21:# wait for the instance to be in state 'running' before returning}
    wait_interval: ${22:# Defines the number of seconds to wait when using the _wait_for methods}
    wait_timeout: ${23:# how long before wait gives up, in seconds}
  tags:
    - community
    - general
    - oneandone_monitoring_policy]]></content>
  <tabTrigger>community.general.oneandone_monitoring_policy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configure 1&amp;1 monitoring policy</description>
</snippet>