<snippet>
  <content><![CDATA[- name: ${1:Deploy software (or files) from bzr branches}
  community.general.bzr:
    dest: ${2:# Absolute path of where the branch should be cloned to}
    executable: ${3:# Path to bzr executable to use}
    force: ${4:# If V(true), any modified files in the working tree will be discarded}
    name: ${5:# SSH or HTTP protocol address of the parent branch}
    version: ${6:# What version of the branch to clone}
  tags:
    - community
    - general
    - bzr]]></content>
  <tabTrigger>community.general.bzr</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Deploy software (or files) from bzr branches</description>
</snippet>