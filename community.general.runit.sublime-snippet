<snippet>
  <content><![CDATA[- name: ${1:Manage runit services}
  community.general.runit:
    enabled: ${2:# Whether the service is enabled or not, if disabled it also implies stopped}
    name: ${3:# Name of the service to manage}
    service_dir: ${4:# directory runsv watches for services}
    service_src: ${5:# directory where services are defined, the source of symlinks to service_dir}
    state: ${6:# V(started)/V(stopped) are idempotent actions that will not run commands unless necessary}
  tags:
    - community
    - general
    - runit]]></content>
  <tabTrigger>community.general.runit</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage runit services</description>
</snippet>