<snippet>
  <content><![CDATA[- name: ${1:Manage Azure SIG Image Version instance}
  azure.azcollection.azure_rm_galleryimageversion:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${7:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${8:# Azure client ID}
    cloud_environment: ${9:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    disable_instance_discovery: ${10:# Determines whether or not instance discovery is performed when attempting to authenticate}
    gallery_image_name: ${11:# The name of the gallery Image Definition in which the Image Version is to be created}
    gallery_name: ${12:# The name of the Shared Image Gallery in which the Image Definition resides}
    location: ${13:# Resource location}
    log_mode: ${14:# Parent argument}
    log_path: ${15:# Parent argument}
    name: ${16:# The name of the gallery Image Version to be created}
    password: ${17:# Active Directory user password}
    profile: ${18:# Security profile found in ~/}
    publishing_profile: ${19:# Publishing profile}
    resource_group: ${20:# The name of the resource group}
    secret: ${21:# Azure client secret}
    state: ${22:# Assert the state of the GalleryImageVersion}
    storage_profile: ${23:# Storage profile}
    subscription_id: ${24:# Your Azure subscription Id}
    tags: ${25:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${26:# Azure tenant ID}
    thumbprint: ${27:# The thumbprint of the private key specified in I(x509_certificate_path)}
    x509_certificate_path: ${28:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_galleryimageversion]]></content>
  <tabTrigger>azure.azcollection.azure_rm_galleryimageversion</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure SIG Image Version instance</description>
</snippet>