<snippet>
  <content><![CDATA[- name: ${1:Consolidate fragmented files on local volumes}
  community.windows.win_defrag:
    include_volumes: ${2:# A list of drive letters or mount point paths of the volumes to be defragmented}
    exclude_volumes: ${3:# A list of drive letters or mount point paths to exclude from defragmentation}
    freespace_consolidation: ${4:# Perform free space consolidation on the specified volumes}
    priority: ${5:# Run the operation at low or normal priority}
    parallel: ${6:# Run the operation on each volume in parallel in the background}
  tags:
    - community
    - windows
    - win_defrag]]></content>
  <tabTrigger>community.windows.win_defrag</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Consolidate fragmented files on local volumes</description>
</snippet>