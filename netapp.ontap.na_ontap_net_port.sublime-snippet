<snippet>
  <content><![CDATA[- name: ${1:NetApp ONTAP network ports}
  netapp.ontap.na_ontap_net_port:
    autonegotiate_admin: ${2:# Enables or disables Ethernet auto-negotiation of speed, duplex and flow control}
    cert_filepath: ${3:# path to SSL client cert file (}
    duplex_admin: ${4:# Specifies the user preferred duplex setting of the port}
    feature_flags: ${5:# Enable or disable a new feature}
    flowcontrol_admin: ${6:# Specifies the user preferred flow control setting of the port}
    force_ontap_version: ${7:# Override the cluster ONTAP version when using REST}
    hostname: ${8:# The hostname or IP address of the ONTAP instance}
    http_port: ${9:# Override the default port (80 or 443) with this port}
    https: ${10:# Enable and disable https}
    ipspace: ${11:# Specifies the port's associated IPspace name}
    key_filepath: ${12:# path to SSL client key file}
    mtu: ${13:# Specifies the maximum transmission unit (MTU) reported by the port}
    node: ${14:# Specifies the name of node}
    ontapi: ${15:# The ontap api version to use}
    password: ${16:# Password for the specified user}
    ports: ${17:# Specifies the name of port(s)}
    speed_admin: ${18:# Specifies the user preferred speed setting of the port}
    state: ${19:# Whether the specified net port should exist or not}
    up_admin: ${20:# Enables or disables the port}
    use_rest: ${21:# Whether to use REST or ZAPI}
    username: ${22:# This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required}
    validate_certs: ${23:# If set to C(no), the SSL certificates will not be validated}
  tags:
    - netapp
    - ontap
    - na_ontap_net_port]]></content>
  <tabTrigger>netapp.ontap.na_ontap_net_port</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp ONTAP network ports</description>
</snippet>