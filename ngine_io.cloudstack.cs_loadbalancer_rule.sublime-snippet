<snippet>
  <content><![CDATA[- name: ${1:Manages load balancer rules on Apache CloudStack based clouds}
  ngine_io.cloudstack.cs_loadbalancer_rule:
    name: ${2:# The name of the load balancer rule}
    description: ${3:# The description of the load balancer rule}
    algorithm: ${4:# Load balancer algorithm}
    private_port: ${5:# The private port of the private ip address/virtual machine where the network traffic will be load balanced to}
    public_port: ${6:# The public port from where the network traffic will be load balanced from}
    ip_address: ${7:# Public IP address from where the network traffic will be load balanced from}
    open_firewall: ${8:# Whether the firewall rule for public port should be created, while creating the new rule}
    cidr: ${9:# CIDR (full notation) to be used for firewall rule if required}
    protocol: ${10:# The protocol to be used on the load balancer}
    project: ${11:# Name of the project the load balancer IP address is related to}
    state: ${12:# State of the rule}
    domain: ${13:# Domain the rule is related to}
    account: ${14:# Account the rule is related to}
    zone: ${15:# Name of the zone in which the rule should be created}
    poll_async: ${16:# Poll async jobs until job has finished}
    tags: ${17:# List of tags}
    network: ${18:# Name of the network}
    vpc: ${19:# Name of the VPC}
    api_key: ${20:# API key of the CloudStack API}
    api_secret: ${21:# Secret key of the CloudStack API}
    api_url: ${22:# URL of the CloudStack API e}
    api_http_method: ${23:# HTTP method used to query the API endpoint}
    api_timeout: ${24:# HTTP timeout in seconds}
    api_verify_ssl_cert: ${25:# Verify CA authority cert file}
  tags:
    - ngine_io
    - cloudstack
    - cs_loadbalancer_rule]]></content>
  <tabTrigger>ngine_io.cloudstack.cs_loadbalancer_rule</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages load balancer rules on Apache CloudStack based clouds</description>
</snippet>