<snippet>
  <content><![CDATA[- name: ${1:Module to manage the aggregates of local users}
  cisco.ios.ios_user:
    aggregate: ${2:# The set of username objects to be configured on the remote Cisco IOS device}
    configured_password: ${3:# The password to be configured on the Cisco IOS device}
    hashed_password: ${4:# This option allows configuring hashed passwords on Cisco IOS devices}
    name: ${5:# The username to be configured on the Cisco IOS device}
    nopassword: ${6:# Defines the username without assigning a password}
    password_type: ${7:# This argument determines whether a 'password' or 'secret' will be configured}
    privilege: ${8:# The C(privilege) argument configures the privilege level of the user when logged into the system}
    purge: ${9:# Instructs the module to consider the resource definition absolute}
    sshkey: ${10:# Specifies one or more SSH public key(s) to configure for the given username}
    state: ${11:# Configures the state of the username definition as it relates to the device operational configuration}
    update_password: ${12:# Since passwords are encrypted in the device running config, this argument will instruct the module when to change the password}
    view: ${13:# Configures the view for the username in the device running configuration}
  tags:
    - cisco
    - ios
    - ios_user]]></content>
  <tabTrigger>cisco.ios.ios_user</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Module to manage the aggregates of local users</description>
</snippet>