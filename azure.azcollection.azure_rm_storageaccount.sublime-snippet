<snippet>
  <content><![CDATA[- name: ${1:Manage Azure storage accounts}
  azure.azcollection.azure_rm_storageaccount:
    access_tier: ${2:# The access tier for this storage account}
    account_type: ${3:# Type of storage account}
    ad_user: ${4:# Active Directory username}
    adfs_authority_url: ${5:# Azure AD authority url}
    allow_blob_public_access: ${6:# Allows blob containers in account to be set for anonymous public access}
    api_profile: ${7:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${8:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${9:# Controls the source of the credentials to use for authentication}
    blob_cors: ${10:# Specifies CORS rules for the Blob service}
    cert_validation_mode: ${11:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${12:# Azure client ID}
    cloud_environment: ${13:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    custom_domain: ${14:# User domain assigned to the storage account}
    encryption: ${15:# The encryption settings on the storage account}
    force_delete_nonempty: ${16:# Attempt deletion if resource already exists and cannot be updated}
    https_only: ${17:# Allows https traffic only to storage service when set to C(True)}
    is_hns_enabled: ${18:# Account HierarchicalNamespace enabled if sets to true}
    kind: ${19:# The kind of storage}
    location: ${20:# Valid Azure location}
    log_mode: ${21:# Parent argument}
    log_path: ${22:# Parent argument}
    minimum_tls_version: ${23:# The minimum required version of Transport Layer Security (TLS) for requests to a storage account}
    name: ${24:# Name of the storage account to update or create}
    network_acls: ${25:# Manages the Firewall and virtual networks settings of the storage account}
    password: ${26:# Active Directory user password}
    profile: ${27:# Security profile found in ~/}
    public_network_access: ${28:# Allow or disallow public network access to Storage Account}
    resource_group: ${29:# Name of the resource group to use}
    secret: ${30:# Azure client secret}
    state: ${31:# State of the storage account}
    static_website: ${32:# Manage static website configuration for the storage account}
    subscription_id: ${33:# Your Azure subscription Id}
    tags: ${34:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${35:# Azure tenant ID}
    thumbprint: ${36:# The thumbprint of the private key specified in I(x509_certificate_path)}
    x509_certificate_path: ${37:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_storageaccount]]></content>
  <tabTrigger>azure.azcollection.azure_rm_storageaccount</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure storage accounts</description>
</snippet>