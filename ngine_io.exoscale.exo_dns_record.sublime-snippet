<snippet>
  <content><![CDATA[- name: ${1:Manages DNS records on Exoscale DNS}
  ngine_io.exoscale.exo_dns_record:
    api_key: ${2:# API key of the Exoscale DNS API}
    api_region: ${3:# Name of the ini section in the C(cloustack}
    api_secret: ${4:# Secret key of the Exoscale DNS API}
    api_timeout: ${5:# HTTP timeout to Exoscale DNS API}
    content: ${6:# Content of the record}
    domain: ${7:# Domain the record is related to}
    multiple: ${8:# Whether there are more than one records with similar I(name) and I(record_type)}
    name: ${9:# Name of the record}
    prio: ${10:# Priority of the record}
    record_type: ${11:# Type of the record}
    state: ${12:# State of the record}
    ttl: ${13:# TTL of the record in seconds}
    validate_certs: ${14:# Validate SSL certs of the Exoscale DNS API}
  tags:
    - ngine_io
    - exoscale
    - exo_dns_record]]></content>
  <tabTrigger>ngine_io.exoscale.exo_dns_record</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages DNS records on Exoscale DNS</description>
</snippet>