<snippet>
  <content><![CDATA[- name: ${1:Configure Pure Storage FlashArray NTP settings}
  purestorage.flasharray.purefa_ntp:
    api_token: ${2:# FlashArray API token for admin privileged user}
    disable_warnings: ${3:# Disable insecure certificate warnings in debug logs}
    fa_url: ${4:# FlashArray management IPv4 address or Hostname}
    ntp_key: ${5:# The NTP symmetric key to be used for NTP authentication}
    ntp_servers: ${6:# A list of up to 4 alternate NTP servers}
    state: ${7:# Create, delete or test NTP servers configuration}
  tags:
    - purestorage
    - flasharray
    - purefa_ntp]]></content>
  <tabTrigger>purestorage.flasharray.purefa_ntp</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configure Pure Storage FlashArray NTP settings</description>
</snippet>