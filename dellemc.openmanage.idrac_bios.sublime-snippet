<snippet>
  <content><![CDATA[- name: ${1:Modify and clear BIOS attributes, reset BIOS settings and configure boot sources}
  dellemc.openmanage.idrac_bios:
    apply_time: ${2:# Apply time of the I(attributes)}
    attributes: ${3:# Dictionary of BIOS attributes and value pair}
    boot_sources: ${4:# (deprecated)List of boot devices to set the boot sources settings}
    ca_path: ${5:# The Privacy Enhanced Mail (PEM) file that contains a CA certificate to be used for the validation}
    clear_pending: ${6:# Allows the user to clear all pending BIOS attributes changes}
    idrac_ip: ${7:# iDRAC IP Address}
    idrac_password: ${8:# iDRAC user password}
    idrac_port: ${9:# iDRAC port}
    idrac_user: ${10:# iDRAC username}
    job_wait: ${11:# Provides the option to wait for job completion}
    job_wait_timeout: ${12:# The maximum wait time of I(job_wait) in seconds}
    maintenance_window: ${13:# Option to schedule the maintenance window}
    reset_bios: ${14:# Resets the BIOS to default settings and triggers a reboot of host system}
    reset_type: ${15:# C(force_restart) Forcefully reboot the host system}
    share_mnt: ${16:# (deprecated)Local mount path of the network share with read-write permission for ansible user}
    share_name: ${17:# (deprecated)Network share or a local path}
    share_password: ${18:# (deprecated)Network share user password}
    share_user: ${19:# (deprecated)Network share user name}
    timeout: ${20:# The socket level timeout in seconds}
    validate_certs: ${21:# If C(false), the SSL certificates will not be validated}
  tags:
    - dellemc
    - openmanage
    - idrac_bios]]></content>
  <tabTrigger>dellemc.openmanage.idrac_bios</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Modify and clear BIOS attributes, reset BIOS settings and configure boot sources</description>
</snippet>