<snippet>
  <content><![CDATA[- name: ${1:Manage NetApp Azure Files Volume}
  netapp.azure.azure_rm_netapp_volume:
    account_name: ${2:# The name of the NetApp account}
    ad_user: ${3:# Active Directory username}
    adfs_authority_url: ${4:# Azure AD authority url}
    api_profile: ${5:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${6:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${7:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${8:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${9:# Azure client ID}
    cloud_environment: ${10:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    feature_flags: ${11:# Enable or disable a new feature}
    file_path: ${12:# A unique file path for the volume}
    location: ${13:# Resource location}
    log_mode: ${14:# Parent argument}
    log_path: ${15:# Parent argument}
    name: ${16:# The name of the volume}
    password: ${17:# Active Directory user password}
    pool_name: ${18:# The name of the capacity pool}
    profile: ${19:# Security profile found in ~/}
    protocol_types: ${20:# Protocol types - NFSv3, NFSv4}
    resource_group: ${21:# Name of the resource group}
    secret: ${22:# Azure client secret}
    service_level: ${23:# The service level of the file system}
    size: ${24:# Provisioned size of the volume (in GiB)}
    state: ${25:# State C(present) will check that the volume exists with the requested configuration}
    subnet_name: ${26:# Azure resource name for a delegated subnet}
    subscription_id: ${27:# Your Azure subscription Id}
    tags: ${28:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${29:# Azure tenant ID}
    virtual_network: ${30:# The name of the virtual network required for the subnet to create a volume}
    vnet_resource_group_for_subnet: ${31:# Only required if virtual_network to be used is of different resource_group}
  tags:
    - netapp
    - azure
    - azure_rm_netapp_volume]]></content>
  <tabTrigger>netapp.azure.azure_rm_netapp_volume</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage NetApp Azure Files Volume</description>
</snippet>