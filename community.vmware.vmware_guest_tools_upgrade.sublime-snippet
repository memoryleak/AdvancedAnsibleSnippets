<snippet>
  <content><![CDATA[- name: ${1:Module to upgrade VMTools}
  community.vmware.vmware_guest_tools_upgrade:
    datacenter: ${2:# Destination datacenter where the virtual machine exists}
    folder: ${3:# Destination folder, absolute or relative path to find an existing guest}
    force_upgrade: ${4:# This flag overrides the guest operating system detection and forcibly upgrade VMware tools or open-vm-tools}
    hostname: ${5:# The hostname or IP address of the vSphere vCenter or ESXi server}
    installer_options: ${6:# Command line options passed to the installer to modify the installation procedure for tools}
    moid: ${7:# Managed Object ID of the instance to manage if known, this is a unique identifier only within a single vCenter instance}
    name: ${8:# Name of the virtual machine to work with}
    name_match: ${9:# If multiple virtual machines matching the name, use the first or last found}
    password: ${10:# The password of the vSphere vCenter or ESXi server}
    port: ${11:# The port number of the vSphere vCenter or ESXi server}
    proxy_host: ${12:# Address of a proxy that will receive all HTTPS requests and relay them}
    proxy_port: ${13:# Port of the HTTP proxy that will receive all HTTPS requests and relay them}
    username: ${14:# The username of the vSphere vCenter or ESXi server}
    uuid: ${15:# UUID of the instance to manage if known, this is VMware's unique identifier}
    validate_certs: ${16:# Allows connection when SSL certificates are not valid}
  tags:
    - community
    - vmware
    - vmware_guest_tools_upgrade]]></content>
  <tabTrigger>community.vmware.vmware_guest_tools_upgrade</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Module to upgrade VMTools</description>
</snippet>