<snippet>
  <content><![CDATA[- name: ${1:Manage Interfaces on Unity storage system}
  dellemc.unity.interface:
    ethernet_port_id: ${2:# ID of the ethernet port}
    ethernet_port_name: ${3:# Name of the ethernet port}
    gateway: ${4:# Gateway of network interface}
    interface_ip: ${5:# IP of network interface}
    nas_server_id: ${6:# ID of the NAS server for which interface will be configured}
    nas_server_name: ${7:# Name of the NAS server for which interface will be configured}
    netmask: ${8:# Netmask of network interface}
    password: ${9:# The password of the Unity management server}
    port: ${10:# Port number through which communication happens with Unity management server}
    prefix_length: ${11:# Prefix length is mutually exclusive with I(netmask)}
    role: ${12:# Indicates whether interface is configured as production or backup}
    state: ${13:# Define whether the interface should exist or not}
    unispherehost: ${14:# IP or FQDN of the Unity management server}
    username: ${15:# The username of the Unity management server}
    validate_certs: ${16:# Boolean variable to specify whether or not to validate SSL certificate}
    vlan_id: ${17:# Vlan id of the interface}
  tags:
    - dellemc
    - unity
    - interface]]></content>
  <tabTrigger>dellemc.unity.interface</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Interfaces on Unity storage system</description>
</snippet>