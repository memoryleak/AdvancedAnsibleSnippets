<snippet>
  <content><![CDATA[- name: ${1:Manages NTP options}
  cisco.nxos.nxos_ntp_options:
    logging: ${2:# Sets whether NTP logging is enabled on the device}
    master: ${3:# Sets whether the device is an authoritative NTP server}
    state: ${4:# Manage the state of the resource}
    stratum: ${5:# If C(master=true), an optional stratum can be supplied (1-15)}
  tags:
    - cisco
    - nxos
    - nxos_ntp_options]]></content>
  <tabTrigger>cisco.nxos.nxos_ntp_options</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages NTP options</description>
</snippet>