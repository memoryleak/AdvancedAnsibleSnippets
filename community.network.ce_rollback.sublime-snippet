<snippet>
  <content><![CDATA[- name: ${1:Set a checkpoint or rollback to a checkpoint on HUAWEI CloudEngine switches}
  community.network.ce_rollback:
    commit_id: ${2:# Specifies the label of the configuration rollback point to which system configurations are expected to roll back}
    label: ${3:# Specifies a user label for a configuration rollback point}
    filename: ${4:# Specifies a configuration file for configuration rollback}
    last: ${5:# Specifies the number of configuration rollback points}
    oldest: ${6:# Specifies the number of configuration rollback points}
    action: ${7:# The operation of configuration rollback}
  tags:
    - community
    - network
    - ce_rollback]]></content>
  <tabTrigger>community.network.ce_rollback</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Set a checkpoint or rollback to a checkpoint on HUAWEI CloudEngine switches</description>
</snippet>