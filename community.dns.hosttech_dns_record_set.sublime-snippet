<snippet>
  <content><![CDATA[- name: ${1:Add or delete record sets in Hosttech DNS service}
  community.dns.hosttech_dns_record_set:
    hosttech_password: ${2:# The password for the Hosttech API user}
    hosttech_token: ${3:# The password for the Hosttech API user}
    hosttech_username: ${4:# The username for the Hosttech API user}
    on_existing: ${5:# This option defines the behavior if the record set already exists, but differs from the specified record set}
    prefix: ${6:# The prefix of the DNS record}
    record: ${7:# The full DNS record to create or delete}
    state: ${8:# Specifies the state of the resource record}
    ttl: ${9:# The TTL to give the new record, in seconds}
    txt_character_encoding: ${10:# Whether to treat numeric escape sequences (V(\\\\xyz)) as octal or decimal numbers}
    txt_transformation: ${11:# Determines how TXT entry values are converted between the API and this module's input and output}
    type: ${12:# The type of DNS record to create or delete}
    value: ${13:# The new value when creating a DNS record}
    zone_id: ${14:# The ID of the DNS zone to modify}
    zone_name: ${15:# The DNS zone to modify}
  tags:
    - community
    - dns
    - hosttech_dns_record_set]]></content>
  <tabTrigger>community.dns.hosttech_dns_record_set</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Add or delete record sets in Hosttech DNS service</description>
</snippet>