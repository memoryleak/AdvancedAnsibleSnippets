<snippet>
  <content><![CDATA[- name: ${1:Manage Azure data lake store}
  azure.azcollection.azure_rm_datalakestore:
    default_group: ${2:# The default owner group for all new folders and files created in the Data Lake Store account}
    encryption_config: ${3:# The Key Vault encryption configuration}
    encryption_state: ${4:# The current state of encryption for this Data Lake Store account}
    firewall_allow_azure_ips: ${5:# The current state of allowing or disallowing IPs originating within Azure through the firewall}
    firewall_rules: ${6:# The list of firewall rules associated with this Data Lake Store account}
    firewall_state: ${7:# The current state of the IP address firewall for this Data Lake Store account}
    identity: ${8:# The Key Vault encryption identity, if any}
    location: ${9:# The resource location}
    name: ${10:# The name of the Data Lake Store account}
    new_tier: ${11:# The commitment tier to use for next month}
    resource_group: ${12:# The name of the Azure resource group to use}
    state: ${13:# State of the data lake store}
    virtual_network_rules: ${14:# The list of virtual network rules associated with this Data Lake Store account}
    ad_user: ${15:# Active Directory username}
    password: ${16:# Active Directory user password}
    profile: ${17:# Security profile found in ~/}
    subscription_id: ${18:# Your Azure subscription Id}
    client_id: ${19:# Azure client ID}
    secret: ${20:# Azure client secret}
    tenant: ${21:# Azure tenant ID}
    cloud_environment: ${22:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    adfs_authority_url: ${23:# Azure AD authority url}
    cert_validation_mode: ${24:# Controls the certificate validation behavior for Azure endpoints}
    auth_source: ${25:# Controls the source of the credentials to use for authentication}
    api_profile: ${26:# Selects an API profile to use when communicating with Azure services}
    log_path: ${27:# Parent argument}
    log_mode: ${28:# Parent argument}
    x509_certificate_path: ${29:# Path to the X509 certificate used to create the service principal in PEM format}
    thumbprint: ${30:# The thumbprint of the private key specified in I(x509_certificate_path)}
    tags: ${31:# Dictionary of string:string pairs to assign as metadata to the object}
    append_tags: ${32:# Use to control if tags field is canonical or just appends to existing tags}
  tags:
    - azure
    - azcollection
    - azure_rm_datalakestore]]></content>
  <tabTrigger>azure.azcollection.azure_rm_datalakestore</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure data lake store</description>
</snippet>