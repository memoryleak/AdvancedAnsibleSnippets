<snippet>
  <content><![CDATA[- name: ${1:Management of provider in ManageIQ}
  community.general.manageiq_provider:
    alerts: ${2:# Alerts endpoint connection information}
    api_version: ${3:# The OpenStack Keystone API version}
    azure_tenant_id: ${4:# Tenant ID}
    host_default_vnc_port_end: ${5:# The last port in the host VNC range}
    host_default_vnc_port_start: ${6:# The first port in the host VNC range}
    manageiq_connection: ${7:# ManageIQ connection configuration information}
    metrics: ${8:# Metrics endpoint connection information}
    name: ${9:# The provider's name}
    project: ${10:# Google Compute Engine Project ID}
    provider: ${11:# Default endpoint connection information, required if state is true}
    provider_region: ${12:# The provider region name to connect to (e}
    ssh_keypair: ${13:# SSH key pair used for SSH connections to all hosts in this provider}
    state: ${14:# absent - provider should not exist, present - provider should be present, refresh - provider will be refreshed}
    subscription: ${15:# Microsoft Azure subscription ID}
    tenant_mapping_enabled: ${16:# Whether to enable mapping of existing tenants}
    type: ${17:# The provider's type}
    zone: ${18:# The ManageIQ zone name that will manage the provider}
  tags:
    - community
    - general
    - manageiq_provider]]></content>
  <tabTrigger>community.general.manageiq_provider</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Management of provider in ManageIQ</description>
</snippet>