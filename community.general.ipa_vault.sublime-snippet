<snippet>
  <content><![CDATA[- name: ${1:Manage FreeIPA vaults}
  community.general.ipa_vault:
    cn: ${2:# Vault name}
    description: ${3:# Description}
    ipa_host: ${4:# IP or hostname of IPA server}
    ipa_pass: ${5:# Password of administrative user}
    ipa_port: ${6:# Port of FreeIPA / IPA server}
    ipa_prot: ${7:# Protocol used by IPA server}
    ipa_timeout: ${8:# Specifies idle timeout (in seconds) for the connection}
    ipa_user: ${9:# Administrative account used on IPA server}
    ipavaultpublickey: ${10:# Public key}
    ipavaultsalt: ${11:# Vault Salt}
    ipavaulttype: ${12:# Vault types are based on security level}
    replace: ${13:# Force replace the existent vault on IPA server}
    service: ${14:# Any service can own one or more service vaults}
    state: ${15:# State to ensure}
    username: ${16:# Any user can own one or more user vaults}
    validate_certs: ${17:# Validate IPA server certificates}
  tags:
    - community
    - general
    - ipa_vault]]></content>
  <tabTrigger>community.general.ipa_vault</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage FreeIPA vaults</description>
</snippet>