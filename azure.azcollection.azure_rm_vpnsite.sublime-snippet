<snippet>
  <content><![CDATA[- name: ${1:Manage Azure VpnSite instance}
  azure.azcollection.azure_rm_vpnsite:
    ad_user: ${2:# Active Directory username}
    address_space: ${3:# The AddressSpace that contains an array of IP address ranges}
    adfs_authority_url: ${4:# Azure AD authority url}
    api_profile: ${5:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${6:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${7:# Controls the source of the credentials to use for authentication}
    bgp_properties: ${8:# The set of bgp properties}
    cert_validation_mode: ${9:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${10:# Azure client ID}
    cloud_environment: ${11:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    device_properties: ${12:# The device properties}
    disable_instance_discovery: ${13:# Determines whether or not instance discovery is performed when attempting to authenticate}
    ip_address: ${14:# The ip-address for the vpn-site}
    is_security_site: ${15:# IsSecuritySite flag}
    location: ${16:# The location of the VpnSite}
    log_mode: ${17:# Parent argument}
    log_path: ${18:# Parent argument}
    name: ${19:# The name of the VpnSite}
    o365_policy: ${20:# Office365 Policy}
    password: ${21:# Active Directory user password}
    profile: ${22:# Security profile found in ~/}
    resource_group: ${23:# The resource group name of the VpnSite}
    secret: ${24:# Azure client secret}
    site_key: ${25:# The key for vpn-site that can be used for connections}
    state: ${26:# Assert the state of the VpnSite}
    subscription_id: ${27:# Your Azure subscription Id}
    tags: ${28:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${29:# Azure tenant ID}
    thumbprint: ${30:# The thumbprint of the private key specified in I(x509_certificate_path)}
    virtual_wan: ${31:# The VirtualWAN to which the vpnSite belongs}
    vpn_site_links: ${32:# List of all vpn site links}
    x509_certificate_path: ${33:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_vpnsite]]></content>
  <tabTrigger>azure.azcollection.azure_rm_vpnsite</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure VpnSite instance</description>
</snippet>