<snippet>
  <content><![CDATA[- name: ${1:Interacts with webservices}
  ansible.windows.win_uri:
    body: ${2:# The body of the HTTP request/response to the web service}
    client_cert: ${3:# The path to the client certificate (}
    client_cert_password: ${4:# The password for I(client_cert) if the cert is password protected}
    content_type: ${5:# Sets the 'Content-Type' header}
    creates: ${6:# A filename, when it already exists, this step will be skipped}
    dest: ${7:# Output the response body to a file}
    follow_redirects: ${8:# Whether or the module should follow redirects}
    force_basic_auth: ${9:# By default the authentication header is only sent when a webservice responses to an initial request with a 401 status}
    headers: ${10:# Extra headers to set on the request}
    http_agent: ${11:# Header to identify as, generally appears in web server logs}
    maximum_redirection: ${12:# Specify how many times the module will redirect a connection to an alternative URI before the connection fails}
    proxy_password: ${13:# The password for I(proxy_username)}
    proxy_url: ${14:# An explicit proxy to use for the request}
    proxy_use_default_credential: ${15:# Uses the current user's credentials when authenticating with a proxy host protected with C(NTLM), C(Kerberos), or C(Negotiate) authentication}
    proxy_username: ${16:# The username to use for proxy authentication}
    removes: ${17:# A filename, when it does not exist, this step will be skipped}
    return_content: ${18:# Whether or not to return the body of the response as a 'content' key in the dictionary result}
    status_code: ${19:# A valid, numeric, HTTP status code that signifies success of the request}
    url: ${20:# Supports FTP, HTTP or HTTPS URLs in the form of (ftp|http|https)://host}
    url_method: ${21:# The HTTP Method of the request}
    url_password: ${22:# The password for I(url_username)}
    url_timeout: ${23:# Specifies how long the request can be pending before it times out (in seconds)}
    url_username: ${24:# The username to use for authentication}
    use_default_credential: ${25:# Uses the current user's credentials when authenticating with a server protected with C(NTLM), C(Kerberos), or C(Negotiate) authentication}
    use_proxy: ${26:# If C(no), it will not use the proxy defined in IE for the current user}
    validate_certs: ${27:# If C(no), SSL certificates will not be validated}
  tags:
    - win_uri]]></content>
  <tabTrigger>ansible.windows.win_uri</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Interacts with webservices</description>
</snippet>