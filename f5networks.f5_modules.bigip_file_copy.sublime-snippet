<snippet>
  <content><![CDATA[- name: ${1:Manage files in datastores on a BIG-IP}
  f5networks.f5_modules.bigip_file_copy:
    name: ${2:# The name of the file as it should reside on the BIG-IP}
    source: ${3:# Specifies the path of the file to upload}
    datastore: ${4:# Specifies the datastore to put the file in}
    force: ${5:# Force overwriting a file}
    partition: ${6:# Device partition to manage resources on}
    state: ${7:# When C(present), ensures the resource exists}
    provider: ${8:# A dict object containing connection details}
  tags:
    - f5networks
    - f5_modules
    - bigip_file_copy]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_file_copy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage files in datastores on a BIG-IP</description>
</snippet>