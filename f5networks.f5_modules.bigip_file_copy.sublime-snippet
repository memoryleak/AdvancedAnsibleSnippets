<snippet>
  <content><![CDATA[- name: ${1:Manage files in datastores on a BIG-IP}
  f5networks.f5_modules.bigip_file_copy:
    datastore: ${2:# Specifies the datastore to put the file in}
    force: ${3:# Force overwriting a file}
    name: ${4:# The name of the file as it should reside on the BIG-IP}
    partition: ${5:# Device partition to manage resources on}
    provider: ${6:# A dict object containing connection details}
    source: ${7:# Specifies the path of the file to upload}
    state: ${8:# When C(present), ensures the resource exists}
  tags:
    - f5networks
    - f5_modules
    - bigip_file_copy]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_file_copy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage files in datastores on a BIG-IP</description>
</snippet>