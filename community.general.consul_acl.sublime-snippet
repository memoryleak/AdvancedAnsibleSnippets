<snippet>
  <content><![CDATA[- name: ${1:Manipulate Consul ACL keys and rules}
  community.general.consul_acl:
    host: ${2:# host of the consul agent defaults to localhost}
    mgmt_token: ${3:# a management token is required to manipulate the acl lists}
    name: ${4:# the name that should be associated with the acl key, this is opaque to Consul}
    port: ${5:# the port on which the consul agent is running}
    rules: ${6:# rules that should be associated with a given token}
    scheme: ${7:# the protocol scheme on which the consul agent is running}
    state: ${8:# whether the ACL pair should be present or absent}
    token: ${9:# the token key identifying an ACL rule set}
    token_type: ${10:# the type of token that should be created}
    validate_certs: ${11:# whether to verify the tls certificate of the consul agent}
  tags:
    - community
    - general
    - consul_acl]]></content>
  <tabTrigger>community.general.consul_acl</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manipulate Consul ACL keys and rules</description>
</snippet>