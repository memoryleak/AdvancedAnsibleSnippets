<snippet>
  <content><![CDATA[- name: ${1:Manage Azure Availability Set}
  azure.azcollection.azure_rm_availabilityset:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${7:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${8:# Azure client ID}
    cloud_environment: ${9:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    location: ${10:# Valid Azure location}
    log_mode: ${11:# Parent argument}
    log_path: ${12:# Parent argument}
    name: ${13:# Name of the availability set}
    password: ${14:# Active Directory user password}
    platform_fault_domain_count: ${15:# Fault domains define the group of virtual machines that share a common power source and network switch}
    platform_update_domain_count: ${16:# Update domains indicate groups of virtual machines and underlying physical hardware that can be rebooted at the same time}
    profile: ${17:# Security profile found in ~/}
    proximity_placement_group: ${18:# The proximity placement group that the availability set should be assigned to}
    resource_group: ${19:# Name of a resource group where the availability set exists or will be created}
    secret: ${20:# Azure client secret}
    sku: ${21:# Define if the availability set supports managed disks}
    state: ${22:# Assert the state of the availability set}
    subscription_id: ${23:# Your Azure subscription Id}
    tags: ${24:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${25:# Azure tenant ID}
    thumbprint: ${26:# The thumbprint of the private key specified in I(x509_certificate_path)}
    x509_certificate_path: ${27:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_availabilityset]]></content>
  <tabTrigger>azure.azcollection.azure_rm_availabilityset</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure Availability Set</description>
</snippet>