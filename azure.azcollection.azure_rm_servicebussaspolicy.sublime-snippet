<snippet>
  <content><![CDATA[- name: ${1:Manage Azure Service Bus SAS policy}
  azure.azcollection.azure_rm_servicebussaspolicy:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    auth_source: ${5:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${6:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${7:# Azure client ID}
    cloud_environment: ${8:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    disable_instance_discovery: ${9:# Determines whether or not instance discovery is performed when attempting to authenticate}
    log_mode: ${10:# Parent argument}
    log_path: ${11:# Parent argument}
    name: ${12:# Name of the SAS policy}
    namespace: ${13:# Manage SAS policy for a namespace without C(queue) or C(topic) set}
    password: ${14:# Active Directory user password}
    profile: ${15:# Security profile found in ~/}
    queue: ${16:# Type of the messaging queue}
    regenerate_primary_key: ${17:# Regenerate the SAS policy primary key}
    regenerate_secondary_key: ${18:# Regenerate the SAS policy secondary key}
    resource_group: ${19:# Name of resource group}
    rights: ${20:# Claim rights of the SAS policy}
    secret: ${21:# Azure client secret}
    state: ${22:# Assert the state of the route}
    subscription_id: ${23:# Your Azure subscription Id}
    tenant: ${24:# Azure tenant ID}
    thumbprint: ${25:# The thumbprint of the private key specified in I(x509_certificate_path)}
    topic: ${26:# Name of the messaging topic}
    x509_certificate_path: ${27:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_servicebussaspolicy]]></content>
  <tabTrigger>azure.azcollection.azure_rm_servicebussaspolicy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure Service Bus SAS policy</description>
</snippet>