<snippet>
  <content><![CDATA[- name: ${1:Manage Azure VirtualWan instance}
  azure.azcollection.azure_rm_virtualwan:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    allow_branch_to_branch_traffic: ${4:# True if branch to branch traffic is allowed}
    allow_vnet_to_vnet_traffic: ${5:# C(True) if Vnet to Vnet traffic is allowed}
    api_profile: ${6:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${7:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${8:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${9:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${10:# Azure client ID}
    cloud_environment: ${11:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    disable_vpn_encryption: ${12:# Vpn encryption to be disabled or not}
    location: ${13:# The virtual wan location}
    log_mode: ${14:# Parent argument}
    log_path: ${15:# Parent argument}
    name: ${16:# The name of the VirtualWAN being retrieved}
    office365_local_breakout_category: ${17:# Specifies the Office365 local breakout category}
    password: ${18:# Active Directory user password}
    profile: ${19:# Security profile found in ~/}
    resource_group: ${20:# The resource group name of the VirtualWan}
    secret: ${21:# Azure client secret}
    state: ${22:# Assert the state of the VirtualWan}
    subscription_id: ${23:# Your Azure subscription Id}
    tags: ${24:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${25:# Azure tenant ID}
    thumbprint: ${26:# The thumbprint of the private key specified in I(x509_certificate_path)}
    virtual_hubs: ${27:# List of VirtualHubs in the VirtualWAN}
    virtual_wan_type: ${28:# The type of the VirtualWAN}
    vpn_sites: ${29:# List of VpnSites in the VirtualWAN}
    x509_certificate_path: ${30:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_virtualwan]]></content>
  <tabTrigger>azure.azcollection.azure_rm_virtualwan</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure VirtualWan instance</description>
</snippet>