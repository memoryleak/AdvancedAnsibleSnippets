<snippet>
  <content><![CDATA[- name: ${1:NetApp information gatherer}
  netapp.ontap.na_ontap_info:
    cert_filepath: ${2:# path to SSL client cert file (}
    continue_on_error: ${3:# By default, this module fails on the first error}
    desired_attributes: ${4:# Advanced feature requiring to understand ZAPI internals}
    feature_flags: ${5:# Enable or disable a new feature}
    gather_subset: ${6:# When supplied, this argument will restrict the information collected to a given subset}
    hostname: ${7:# The hostname or IP address of the ONTAP instance}
    http_port: ${8:# Override the default port (80 or 443) with this port}
    https: ${9:# Enable and disable https}
    key_filepath: ${10:# path to SSL client key file}
    max_records: ${11:# Maximum number of records returned in a single ZAPI call}
    ontapi: ${12:# The ontap api version to use}
    password: ${13:# Password for the specified user}
    query: ${14:# Advanced feature requiring to understand ZAPI internals}
    state: ${15:# deprecated as of 21}
    summary: ${16:# Boolean flag to control return all attributes of the module info or only the names}
    use_native_zapi_tags: ${17:# By default, I(-) in the returned dictionary keys are translated to I(_)}
    use_rest: ${18:# This module only support ZAPI and will can not be swtich to REST}
    username: ${19:# This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required}
    validate_certs: ${20:# If set to C(no), the SSL certificates will not be validated}
    volume_move_target_aggr_info: ${21:# Required options for volume_move_target_aggr_info}
    vserver: ${22:# If present, 'vserver tunneling' will limit the output to the vserver scope}
  tags:
    - netapp
    - ontap
    - na_ontap_info]]></content>
  <tabTrigger>netapp.ontap.na_ontap_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp information gatherer</description>
</snippet>