<snippet>
  <content><![CDATA[- name: ${1:Manage Layer 3 Outside (L3Out) BGP Peers (bgp:PeerP)}
  cisco.aci.aci_l3out_bgp_peer:
    address_type_controls: ${2:# Address Type Controls}
    admin_state: ${3:# Admin state for the BGP session}
    allow_self_as_count: ${4:# Number of allowed self AS}
    annotation: ${5:# User-defined string for annotating an object}
    bgp_controls: ${6:# BGP Controls}
    certificate_name: ${7:# The X}
    host: ${8:# IP Address or hostname of APIC resolvable by Ansible control host}
    interface_profile: ${9:# Name of the interface profile}
    l3out: ${10:# Name of an existing L3Out}
    local_as_number: ${11:# The local Autonomous System Number (ASN) of the L3Out BGP Peer}
    local_as_number_config: ${12:# The local Autonomous System Number (ASN) configuration of the L3Out BGP Peer}
    node_id: ${13:# Node to build the interface on for Port-channels and single ports}
    node_profile: ${14:# Name of the node profile}
    output_level: ${15:# Influence the output of this ACI module}
    output_path: ${16:# Path to a file that will be used to dump the ACI JSON configuration objects generated by the module}
    password: ${17:# The password to use for authentication}
    path_ep: ${18:# Path to interface}
    peer_controls: ${19:# Peer Controls}
    peer_ip: ${20:# IP address of the BGP peer}
    pod_id: ${21:# Pod to build the interface on}
    port: ${22:# Port number to be used for REST connection}
    private_asn_controls: ${23:# Private AS Controls}
    private_key: ${24:# Either a PEM-formatted private key file or the private key content used for signature-based authentication}
    remote_asn: ${25:# Autonomous System Number of the BGP peer}
    route_control_profiles: ${26:# List of dictionaries objects, which is used to bind the BGP Peer Connectivity Profile to Route Control Profile}
    state: ${27:# Use C(present) or C(absent) for adding or removing}
    tenant: ${28:# Name of an existing tenant}
    timeout: ${29:# The socket level timeout in seconds}
    ttl: ${30:# eBGP Multihop Time To Live}
    use_proxy: ${31:# If C(false), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_ssl: ${32:# If C(false), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${33:# The username to use for authentication}
    validate_certs: ${34:# If C(false), SSL certificates will not be validated}
    weight: ${35:# Weight for BGP routes from this neighbor}
  tags:
    - cisco
    - aci
    - aci_l3out_bgp_peer]]></content>
  <tabTrigger>cisco.aci.aci_l3out_bgp_peer</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Layer 3 Outside (L3Out) BGP Peers (bgp:PeerP)</description>
</snippet>