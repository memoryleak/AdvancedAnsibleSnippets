<snippet>
  <content><![CDATA[- name: ${1:Creates/updates/deletes/blocks/unblocks GitLab Users}
  community.general.gitlab_user:
    access_level: ${2:# The access level to the group}
    api_job_token: ${3:# GitLab CI job token for logging in}
    api_oauth_token: ${4:# GitLab OAuth token for logging in}
    api_password: ${5:# The password to use for authentication against the API}
    api_token: ${6:# GitLab access token with API permissions}
    api_url: ${7:# The resolvable endpoint for the API}
    api_username: ${8:# The username to use for authentication against the API}
    ca_path: ${9:# The CA certificates bundle to use to verify GitLab server certificate}
    confirm: ${10:# Require confirmation}
    email: ${11:# The email that belongs to the user}
    external: ${12:# Define external parameter for this user}
    group: ${13:# Id or Full path of parent group in the form of group/name}
    identities: ${14:# List of identities to be added/updated for this user}
    isadmin: ${15:# Grant admin privileges to the user}
    name: ${16:# Name of the user you want to create}
    overwrite_identities: ${17:# Overwrite identities with identities added in this module}
    password: ${18:# The password of the user}
    reset_password: ${19:# Whether the user can change its password or not}
    sshkey_expires_at: ${20:# The expiration date of the SSH public key in ISO 8601 format C(YYYY-MM-DDTHH:MM:SSZ)}
    sshkey_file: ${21:# The SSH public key itself}
    sshkey_name: ${22:# The name of the SSH public key}
    state: ${23:# Create, delete or block a user}
    username: ${24:# The username of the user}
    validate_certs: ${25:# Whether or not to validate SSL certs when supplying a HTTPS endpoint}
  tags:
    - community
    - general
    - gitlab_user]]></content>
  <tabTrigger>community.general.gitlab_user</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Creates/updates/deletes/blocks/unblocks GitLab Users</description>
</snippet>