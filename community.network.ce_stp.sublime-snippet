<snippet>
  <content><![CDATA[- name: ${1:Manages STP configuration on HUAWEI CloudEngine switches}
  community.network.ce_stp:
    bpdu_filter: ${2:# Specify a port as a BPDU filter port}
    bpdu_protection: ${3:# Configure BPDU protection on an edge port}
    cost: ${4:# Set the path cost of the current port}
    edged_port: ${5:# Set the current port as an edge port}
    interface: ${6:# Interface name}
    loop_protection: ${7:# Enable loop protection on the current port}
    root_protection: ${8:# Enable root protection on the current port}
    state: ${9:# Specify desired state of the resource}
    stp_converge: ${10:# STP convergence mode}
    stp_enable: ${11:# Enable or disable STP on a switch}
    stp_mode: ${12:# Set an operation mode for the current MSTP process}
    tc_protection: ${13:# Configure the TC BPDU protection function for an MSTP process}
    tc_protection_interval: ${14:# Set the time the MSTP device takes to handle the maximum number of TC BPDUs and immediately refresh forwarding entries}
    tc_protection_threshold: ${15:# Set the maximum number of TC BPDUs that the MSTP can handle}
  tags:
    - community
    - network
    - ce_stp]]></content>
  <tabTrigger>community.network.ce_stp</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages STP configuration on HUAWEI CloudEngine switches</description>
</snippet>