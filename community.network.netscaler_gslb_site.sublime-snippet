<snippet>
  <content><![CDATA[- name: ${1:Manage gslb site entities in Netscaler}
  community.network.netscaler_gslb_site:
    clip: ${2:# Cluster IP address}
    metricexchange: ${3:# Exchange metrics with other sites}
    naptrreplacementsuffix: ${4:# The naptr replacement suffix configured here will be used to construct the naptr replacement field in NAPTR record}
    nitro_pass: ${5:# The password with which to authenticate to the netscaler node}
    nitro_protocol: ${6:# Which protocol to use when accessing the nitro API objects}
    nitro_timeout: ${7:# Time in seconds until a timeout error is thrown when establishing a new session with Netscaler}
    nitro_user: ${8:# The username with which to authenticate to the netscaler node}
    nsip: ${9:# The ip address of the netscaler appliance where the nitro API calls will be made}
    nwmetricexchange: ${10:# Exchange, with other GSLB sites, network metrics such as round-trip time (RTT), learned from communications with various local DNS (LDNS) servers used by clients}
    parentsite: ${11:# Parent site of the GSLB site, in a parent-child topology}
    publicclip: ${12:# IP address to be used to globally access the remote cluster when it is deployed behind a NAT}
    publicip: ${13:# Public IP address for the local site}
    save_config: ${14:# If C(yes) the module will save the configuration on the netscaler node if it makes any changes}
    sessionexchange: ${15:# Exchange persistent session entries with other GSLB sites every five seconds}
    siteipaddress: ${16:# IP address for the GSLB site}
    sitename: ${17:# Name for the GSLB site}
    sitetype: ${18:# Type of site to create}
    state: ${19:# The state of the resource being configured by the module on the netscaler node}
    triggermonitor: ${20:# Specify the conditions under which the GSLB service must be monitored by a monitor, if one is bound}
    validate_certs: ${21:# If C(no), SSL certificates will not be validated}
  tags:
    - community
    - network
    - netscaler_gslb_site]]></content>
  <tabTrigger>community.network.netscaler_gslb_site</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage gslb site entities in Netscaler</description>
</snippet>