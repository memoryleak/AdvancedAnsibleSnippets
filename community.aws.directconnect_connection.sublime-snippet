<snippet>
  <content><![CDATA[- name: ${1:Creates, deletes, modifies a DirectConnect connection}
  community.aws.directconnect_connection:
    access_key: ${2:# AWS access key ID}
    aws_ca_bundle: ${3:# The location of a CA Bundle to use when validating SSL certificates}
    aws_config: ${4:# A dictionary to modify the botocore configuration}
    bandwidth: ${5:# The bandwidth of the Direct Connect connection}
    connection_id: ${6:# The ID of the Direct Connect connection}
    debug_botocore_endpoint_logs: ${7:# Use a C(botocore}
    endpoint_url: ${8:# URL to connect to instead of the default AWS endpoints}
    forced_update: ${9:# To modify I(bandwidth) or I(location) the connection needs to be deleted and recreated}
    link_aggregation_group: ${10:# The ID of the link aggregation group you want to associate with the connection}
    location: ${11:# Where the Direct Connect connection is located}
    name: ${12:# The name of the Direct Connect connection}
    profile: ${13:# A named AWS profile to use for authentication}
    region: ${14:# The AWS region to use}
    secret_key: ${15:# AWS secret access key}
    session_token: ${16:# AWS STS session token for use with temporary credentials}
    state: ${17:# The state of the Direct Connect connection}
    validate_certs: ${18:# When set to C(false), SSL certificates will not be validated for communication with the AWS APIs}
  tags:
    - community
    - aws
    - directconnect_connection]]></content>
  <tabTrigger>community.aws.directconnect_connection</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Creates, deletes, modifies a DirectConnect connection</description>
</snippet>