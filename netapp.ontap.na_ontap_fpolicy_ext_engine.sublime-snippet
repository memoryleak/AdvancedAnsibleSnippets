<snippet>
  <content><![CDATA[- name: ${1:NetApp ONTAP fPolicy external engine configuration}
  netapp.ontap.na_ontap_fpolicy_ext_engine:
    cert_filepath: ${2:# path to SSL client cert file (}
    certificate_ca: ${3:# Certificate authority name}
    certificate_common_name: ${4:# FQDN or custom common name of certificate}
    certificate_serial: ${5:# Serial number of certificate}
    extern_engine_type: ${6:# External engine type}
    feature_flags: ${7:# Enable or disable a new feature}
    force_ontap_version: ${8:# Override the cluster ONTAP version when using REST}
    hostname: ${9:# The hostname or IP address of the ONTAP instance}
    http_port: ${10:# Override the default port (80 or 443) with this port}
    https: ${11:# Enable and disable https}
    is_resiliency_enabled: ${12:# Indicates if the resiliency with this engine is required}
    key_filepath: ${13:# path to SSL client key file}
    max_connection_retries: ${14:# Number of times storage appliance will attempt to establish a broken connection to FPolicy server}
    max_server_reqs: ${15:# Maximum number of outstanding screen requests that will be queued for an FPolicy Server}
    name: ${16:# Name of the external engine}
    ontapi: ${17:# The ontap api version to use}
    password: ${18:# Password for the specified user}
    port: ${19:# Port number of the FPolicy server application}
    primary_servers: ${20:# Primary FPolicy servers}
    recv_buffer_size: ${21:# Receive buffer size of connected socket for FPolicy Server}
    resiliency_directory_path: ${22:# Directory path under Vserver for storing file access notifications}
    secondary_servers: ${23:# Secondary FPolicy servers}
    send_buffer_size: ${24:# Send buffer size of connected socket for FPolicy Server}
    ssl_option: ${25:# SSL option for external communication}
    state: ${26:# Whether the fPolicy external engine is present or not}
    use_rest: ${27:# Whether to use REST or ZAPI}
    username: ${28:# This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required}
    validate_certs: ${29:# If set to C(no), the SSL certificates will not be validated}
    vserver: ${30:# the name of the vserver to create the external engine on}
  tags:
    - netapp
    - ontap
    - na_ontap_fpolicy_ext_engine]]></content>
  <tabTrigger>netapp.ontap.na_ontap_fpolicy_ext_engine</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp ONTAP fPolicy external engine configuration</description>
</snippet>