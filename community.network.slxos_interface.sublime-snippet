<snippet>
  <content><![CDATA[- name: ${1:Manage Interfaces on Extreme SLX-OS network devices}
  community.network.slxos_interface:
    aggregate: ${2:# List of Interfaces definitions}
    delay: ${3:# Time in seconds to wait before checking for the operational state on remote device}
    description: ${4:# Description of Interface}
    enabled: ${5:# Interface link status}
    mtu: ${6:# Maximum size of transmit packet}
    name: ${7:# Name of the Interface}
    neighbors: ${8:# Check the operational state of given interface C(name) for LLDP neighbor}
    rx_rate: ${9:# Receiver rate in bits per second (bps)}
    speed: ${10:# Interface link speed}
    state: ${11:# State of the Interface configuration, C(up) means present and operationally up and C(down) means present and operationally C(down)}
    tx_rate: ${12:# Transmit rate in bits per second (bps)}
  tags:
    - community
    - network
    - slxos_interface]]></content>
  <tabTrigger>community.network.slxos_interface</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Interfaces on Extreme SLX-OS network devices</description>
</snippet>