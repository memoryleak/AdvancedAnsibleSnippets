<snippet>
  <content><![CDATA[- name: ${1:NetApp ONTAP LDAP client}
  netapp.ontap.na_ontap_ldap_client:
    ad_domain: ${2:# Active Directory Domain Name}
    base_dn: ${3:# LDAP base DN}
    base_scope: ${4:# LDAP search scope}
    bind_as_cifs_server: ${5:# The cluster uses the CIFS server's credentials to bind to the LDAP server}
    bind_dn: ${6:# LDAP bind user DN}
    bind_password: ${7:# LDAP bind user password}
    cert_filepath: ${8:# path to SSL client cert file (}
    feature_flags: ${9:# Enable or disable a new feature}
    force_ontap_version: ${10:# Override the cluster ONTAP version when using REST}
    hostname: ${11:# The hostname or IP address of the ONTAP instance}
    http_port: ${12:# Override the default port (80 or 443) with this port}
    https: ${13:# Enable and disable https}
    key_filepath: ${14:# path to SSL client key file}
    ldaps_enabled: ${15:# Specifies whether or not LDAPS is enabled}
    min_bind_level: ${16:# Minimal LDAP server bind level}
    name: ${17:# The name of LDAP client configuration}
    ontapi: ${18:# The ontap api version to use}
    password: ${19:# Password for the specified user}
    port: ${20:# LDAP server TCP port}
    preferred_ad_servers: ${21:# Preferred Active Directory (AD) Domain Controllers}
    query_timeout: ${22:# LDAP server query timeout}
    referral_enabled: ${23:# LDAP Referral Chasing}
    schema: ${24:# LDAP schema}
    servers: ${25:# Comma separated list of LDAP servers}
    session_security: ${26:# Client Session Security}
    skip_config_validation: ${27:# Indicates whether or not the validation for the specified LDAP configuration is disabled}
    state: ${28:# Whether the specified LDAP client configuration exist or not}
    use_rest: ${29:# Whether to use REST or ZAPI}
    use_start_tls: ${30:# Start TLS on LDAP connection}
    username: ${31:# This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required}
    validate_certs: ${32:# If set to C(no), the SSL certificates will not be validated}
    vserver: ${33:# vserver/svm that holds LDAP client configuration}
  tags:
    - netapp
    - ontap
    - na_ontap_ldap_client]]></content>
  <tabTrigger>netapp.ontap.na_ontap_ldap_client</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp ONTAP LDAP client</description>
</snippet>