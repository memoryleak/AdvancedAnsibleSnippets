<snippet>
  <content><![CDATA[- name: ${1:Manage tenants with cloud sites}
  cisco.mso.mso_tenant_site:
    aws_access_key: ${2:# AWS account's access key id}
    aws_account_org: ${3:# AWS account for organization}
    aws_trusted: ${4:# AWS account's access in trusted mode}
    azure_access_type: ${5:# Managed mode for Azure}
    azure_active_directory_id: ${6:# Azure account's active directory id}
    azure_active_directory_name: ${7:# Azure account's active directory name}
    azure_application_id: ${8:# Azure account's application id}
    azure_credential_name: ${9:# Azure account's credential name}
    azure_subscription_id: ${10:# Azure account's subscription id}
    cloud_account: ${11:# Required for cloud site}
    host: ${12:# IP Address or hostname of the ACI Multi Site Orchestrator host}
    login_domain: ${13:# The login domain name to use for authentication}
    output_level: ${14:# Influence the output of this MSO module}
    password: ${15:# The password to use for authentication}
    port: ${16:# Port number to be used for the REST connection}
    secret_key: ${17:# secret key of AWS for untrusted account}
    security_domains: ${18:# List of security domains for sites}
    site: ${19:# The name of the site}
    state: ${20:# Use C(present) or C(absent) for adding or removing}
    tenant: ${21:# The name of the tenant}
    timeout: ${22:# The socket level timeout in seconds}
    use_proxy: ${23:# If C(false), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_ssl: ${24:# If C(false), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${25:# The username to use for authentication}
    validate_certs: ${26:# If C(false), SSL certificates will not be validated}
  tags:
    - cisco
    - mso
    - mso_tenant_site]]></content>
  <tabTrigger>cisco.mso.mso_tenant_site</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage tenants with cloud sites</description>
</snippet>