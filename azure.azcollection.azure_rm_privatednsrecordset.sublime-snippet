<snippet>
  <content><![CDATA[- name: ${1:Create, delete and update Private DNS record sets and records}
  azure.azcollection.azure_rm_privatednsrecordset:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${7:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${8:# Azure client ID}
    cloud_environment: ${9:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    disable_instance_discovery: ${10:# Determines whether or not instance discovery is performed when attempting to authenticate}
    log_mode: ${11:# Parent argument}
    log_path: ${12:# Parent argument}
    password: ${13:# Active Directory user password}
    profile: ${14:# Security profile found in ~/}
    record_mode: ${15:# Whether existing record values not sent to the module should be purged}
    record_type: ${16:# The type of record set to create or delete}
    records: ${17:# List of records to be created depending on the type of record (set)}
    relative_name: ${18:# Relative name of the record set}
    resource_group: ${19:# Name of resource group}
    secret: ${20:# Azure client secret}
    state: ${21:# Assert the state of the record set}
    subscription_id: ${22:# Your Azure subscription Id}
    tags: ${23:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${24:# Azure tenant ID}
    thumbprint: ${25:# The thumbprint of the private key specified in I(x509_certificate_path)}
    time_to_live: ${26:# Time to live of the record set in seconds}
    x509_certificate_path: ${27:# Path to the X509 certificate used to create the service principal in PEM format}
    zone_name: ${28:# Name of the existing Private DNS zone in which to manage the record set}
  tags:
    - azure
    - azcollection
    - azure_rm_privatednsrecordset]]></content>
  <tabTrigger>azure.azcollection.azure_rm_privatednsrecordset</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create, delete and update Private DNS record sets and records</description>
</snippet>