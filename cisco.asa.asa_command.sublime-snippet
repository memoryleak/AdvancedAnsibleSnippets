<snippet>
  <content><![CDATA[- name: ${1:Run arbitrary commands on Cisco ASA devices}
  cisco.asa.asa_command:
    commands: ${2:# List of commands to send to the remote device over the configured provider}
    wait_for: ${3:# List of conditions to evaluate against the output of the command}
    match: ${4:# The I(match) argument is used in conjunction with the I(wait_for) argument to specify the match policy}
    retries: ${5:# Specifies the number of retries a command should by tried before it is considered failed}
    interval: ${6:# Configures the interval in seconds to wait between retries of the command}
    context: ${7:# Specifies which context to target if you are running in the ASA in multiple context mode}
    passwords: ${8:# Saves running-config passwords in clear-text when set to True}
  tags:
    - cisco
    - asa
    - asa_command]]></content>
  <tabTrigger>cisco.asa.asa_command</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Run arbitrary commands on Cisco ASA devices</description>
</snippet>