<snippet>
  <content><![CDATA[- name: ${1:Manages firewall groups on Vultr}
  vultr.cloud.firewall_group:
    description: ${2:# Description of the firewall group}
    state: ${3:# State of the firewall group}
    api_key: ${4:# API key of the Vultr API}
    api_timeout: ${5:# HTTP timeout to Vultr API}
    api_retries: ${6:# Amount of retries in case of the Vultr API retuns an HTTP 503 code}
    api_retry_max_delay: ${7:# Retry backoff delay in seconds is exponential up to this max}
    api_endpoint: ${8:# URL to API endpint (without trailing slash)}
    validate_certs: ${9:# Validate SSL certs of the Vultr API}
  tags:
    - vultr
    - cloud
    - firewall_group]]></content>
  <tabTrigger>vultr.cloud.firewall_group</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages firewall groups on Vultr</description>
</snippet>