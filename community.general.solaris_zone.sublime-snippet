<snippet>
  <content><![CDATA[- name: ${1:Manage Solaris zones}
  community.general.solaris_zone:
    attach_options: ${2:# Extra options to the zoneadm attach command}
    config: ${3:# The C(zonecfg) configuration commands for this zone}
    create_options: ${4:# Extra options to the zonecfg(1M) create command}
    install_options: ${5:# Extra options to the zoneadm(1M) install command}
    name: ${6:# Zone name}
    path: ${7:# The path where the zone will be created}
    root_password: ${8:# The password hash for the root account}
    sparse: ${9:# Whether to create a sparse (V(true)) or whole root (V(false)) zone}
    state: ${10:# V(present), configure and install the zone}
    timeout: ${11:# Timeout, in seconds, for zone to boot}
  tags:
    - community
    - general
    - solaris_zone]]></content>
  <tabTrigger>community.general.solaris_zone</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Solaris zones</description>
</snippet>