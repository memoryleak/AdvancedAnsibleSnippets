<snippet>
  <content><![CDATA[- name: ${1:List and Export VMware vCenter infra profile configs}
  community.vmware.vmware_vc_infraprofile_info:
    api: ${2:# API which needs to be executed}
    config_path: ${3:# Config file path which contains infra profile config JSON data, supports both relative and absolute path}
    decryption_key: ${4:# decryption_key argument for while doing import profile task as of now its not taken into account form API team}
    description: ${5:# Description of about encryption or decryption key}
    encryption_key: ${6:# encryption_key argument for while doing import profile task as of now its not taken into account form API team}
    hostname: ${7:# The hostname or IP address of the vSphere vCenter server}
    password: ${8:# The password of the vSphere vCenter server}
    port: ${9:# The port number of the vSphere vCenter}
    profiles: ${10:# A list of profile names to be exported, imported, and validated}
    protocol: ${11:# The connection to protocol}
    proxy_host: ${12:# Address of a proxy that will receive all HTTPS requests and relay them}
    proxy_port: ${13:# Port of the HTTP proxy that will receive all HTTPS requests and relay them}
    username: ${14:# The username of the vSphere vCenter server}
    validate_certs: ${15:# Allows connection when SSL certificates are not valid}
  tags:
    - community
    - vmware
    - vmware_vc_infraprofile_info]]></content>
  <tabTrigger>community.vmware.vmware_vc_infraprofile_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>List and Export VMware vCenter infra profile configs</description>
</snippet>