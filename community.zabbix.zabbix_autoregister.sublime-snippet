<snippet>
  <content><![CDATA[- name: ${1:Update Zabbix autoregistration}
  community.zabbix.zabbix_autoregister:
    http_login_password: ${2:# Basic Auth password}
    http_login_user: ${3:# Basic Auth login}
    tls_accept: ${4:# Type of allowed incoming connections for autoregistration}
    tls_psk: ${5:# TLS connection uses this PSK value}
    tls_psk_identity: ${6:# TLS connection uses this PSK identity string}
  tags:
    - community
    - zabbix
    - zabbix_autoregister]]></content>
  <tabTrigger>community.zabbix.zabbix_autoregister</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Update Zabbix autoregistration</description>
</snippet>