<snippet>
  <content><![CDATA[- name: ${1:Hostname resource module}
  cisco.nxos.nxos_hostname:
    config: ${2:# A dictionary of hostname configuration}
    running_config: ${3:# This option is used only with state I(parsed)}
    state: ${4:# The state the configuration should be left in}
  tags:
    - cisco
    - nxos
    - nxos_hostname]]></content>
  <tabTrigger>cisco.nxos.nxos_hostname</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Hostname resource module</description>
</snippet>