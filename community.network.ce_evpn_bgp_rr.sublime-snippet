<snippet>
  <content><![CDATA[- name: ${1:Manages RR for the VXLAN Network on HUAWEI CloudEngine switches}
  community.network.ce_evpn_bgp_rr:
    as_number: ${2:# Specifies the number of the AS, in integer format}
    bgp_instance: ${3:# Specifies the name of a BGP instance}
    bgp_evpn_enable: ${4:# Enable or disable the BGP-EVPN address family}
    peer_type: ${5:# Specify the peer type}
    peer: ${6:# Specifies the IPv4 address or the group name of a peer}
    reflect_client: ${7:# Configure the local device as the route reflector and the peer or peer group as the client of the route reflector}
    policy_vpn_target: ${8:# Enable or disable the VPN-Target filtering}
  tags:
    - community
    - network
    - ce_evpn_bgp_rr]]></content>
  <tabTrigger>community.network.ce_evpn_bgp_rr</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages RR for the VXLAN Network on HUAWEI CloudEngine switches</description>
</snippet>