<snippet>
  <content><![CDATA[- name: ${1:Manages network offerings on Apache CloudStack based clouds}
  ngine_io.cloudstack.cs_network_offering:
    state: ${2:# State of the network offering}
    display_text: ${3:# Display text of the network offerings}
    guest_ip_type: ${4:# Guest type of the network offering}
    name: ${5:# The name of the network offering}
    supported_services: ${6:# Services supported by the network offering}
    traffic_type: ${7:# The traffic type for the network offering}
    availability: ${8:# The availability of network offering}
    conserve_mode: ${9:# Whether the network offering has IP conserve mode enabled}
    details: ${10:# Network offering details in key/value pairs}
    egress_default_policy: ${11:# Whether the default egress policy is allow or to deny}
    persistent: ${12:# True if network offering supports persistent networks}
    keepalive_enabled: ${13:# If true keepalive will be turned on in the loadbalancer}
    max_connections: ${14:# Maximum number of concurrent connections supported by the network offering}
    network_rate: ${15:# Data transfer rate in megabits per second allowed}
    service_capabilities: ${16:# Desired service capabilities as part of network offering}
    service_offering: ${17:# The service offering name or ID used by virtual router provider}
    service_providers: ${18:# Provider to service mapping}
    specify_ip_ranges: ${19:# Whether the network offering supports specifying IP ranges}
    specify_vlan: ${20:# Whether the network offering supports vlans or not}
    for_vpc: ${21:# Whether the offering is meant to be used for VPC or not}
    tags: ${22:# List of tags}
    domains: ${23:# List of domains the network offering is related to}
    zones: ${24:# List of zones the network offering is related to}
    api_key: ${25:# API key of the CloudStack API}
    api_secret: ${26:# Secret key of the CloudStack API}
    api_url: ${27:# URL of the CloudStack API e}
    api_http_method: ${28:# HTTP method used to query the API endpoint}
    api_timeout: ${29:# HTTP timeout in seconds}
    api_verify_ssl_cert: ${30:# Verify CA authority cert file}
  tags:
    - ngine_io
    - cloudstack
    - cs_network_offering]]></content>
  <tabTrigger>ngine_io.cloudstack.cs_network_offering</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages network offerings on Apache CloudStack based clouds</description>
</snippet>