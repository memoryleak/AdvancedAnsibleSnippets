<snippet>
  <content><![CDATA[- name: ${1:Manage FreeIPA service}
  community.general.ipa_service:
    krbcanonicalname: ${2:# Principal of the service}
    hosts: ${3:# Defines the list of 'ManagedBy' hosts}
    force: ${4:# Force principal name even if host is not in DNS}
    skip_host_check: ${5:# Force service to be created even when host object does not exist to manage it}
    state: ${6:# State to ensure}
    ipa_port: ${7:# Port of FreeIPA / IPA server}
    ipa_host: ${8:# IP or hostname of IPA server}
    ipa_user: ${9:# Administrative account used on IPA server}
    ipa_pass: ${10:# Password of administrative user}
    ipa_prot: ${11:# Protocol used by IPA server}
    validate_certs: ${12:# This only applies if C(ipa_prot) is I(https)}
    ipa_timeout: ${13:# Specifies idle timeout (in seconds) for the connection}
  tags:
    - community
    - general
    - ipa_service]]></content>
  <tabTrigger>community.general.ipa_service</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage FreeIPA service</description>
</snippet>