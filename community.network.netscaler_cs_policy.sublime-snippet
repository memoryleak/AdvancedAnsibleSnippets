<snippet>
  <content><![CDATA[- name: ${1:Manage content switching policy}
  community.network.netscaler_cs_policy:
    action: ${2:# Content switching action that names the target load balancing virtual server to which the traffic is switched}
    domain: ${3:# The domain name}
    nitro_pass: ${4:# The password with which to authenticate to the netscaler node}
    nitro_protocol: ${5:# Which protocol to use when accessing the nitro API objects}
    nitro_timeout: ${6:# Time in seconds until a timeout error is thrown when establishing a new session with Netscaler}
    nitro_user: ${7:# The username with which to authenticate to the netscaler node}
    nsip: ${8:# The ip address of the netscaler appliance where the nitro API calls will be made}
    policyname: ${9:# Name for the content switching policy}
    rule: ${10:# Expression, or name of a named expression, against which traffic is evaluated}
    save_config: ${11:# If C(yes) the module will save the configuration on the netscaler node if it makes any changes}
    state: ${12:# The state of the resource being configured by the module on the netscaler node}
    url: ${13:# URL string that is matched with the URL of a request}
    validate_certs: ${14:# If C(no), SSL certificates will not be validated}
  tags:
    - community
    - network
    - netscaler_cs_policy]]></content>
  <tabTrigger>community.network.netscaler_cs_policy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage content switching policy</description>
</snippet>