<snippet>
  <content><![CDATA[- name: ${1:SNMP Server resource module}
  cisco.nxos.nxos_snmp_server:
    running_config: ${2:# This option is used only with state I(parsed)}
    config: ${3:# A dict of SNMP server configuration}
    state: ${4:# The state the configuration should be left in}
  tags:
    - cisco
    - nxos
    - nxos_snmp_server]]></content>
  <tabTrigger>cisco.nxos.nxos_snmp_server</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>SNMP Server resource module</description>
</snippet>