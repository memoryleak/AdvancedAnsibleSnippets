<snippet>
  <content><![CDATA[- name: ${1:Resource module for Sensor Test Template Edit}
  cisco.dnac.sensor_test_template_edit:
    _id: ${2:# The sensor test template unique identifier, generated at test creation time}
    actionInProgress: ${3:# Indication of inprogress action}
    apCoverage: ${4:# Sensor Test Template Edit's apCoverage}
    connection: ${5:# Connection type of test WIRED, WIRELESS, BOTH}
    dnac_debug: ${6:# Flag for Cisco DNA Center SDK to enable debugging}
    dnac_host: ${7:# The Cisco DNA Center hostname}
    dnac_password: ${8:# The Cisco DNA Center password to authenticate}
    dnac_port: ${9:# The Cisco DNA Center port}
    dnac_username: ${10:# The Cisco DNA Center username to authenticate}
    dnac_verify: ${11:# Flag to enable or disable SSL certificate verification}
    dnac_version: ${12:# Informs the SDK which version of Cisco DNA Center to use}
    encryptionMode: ${13:# Encryption mode}
    frequency: ${14:# Sensor Test Template Edit's frequency}
    lastModifiedTime: ${15:# Last modify time}
    location: ${16:# Location string}
    locationInfoList: ${17:# Sensor Test Template Edit's locationInfoList}
    modelVersion: ${18:# Test template object model version (must be 2)}
    name: ${19:# The sensor test template name, which is the same as in 'templateName'}
    numAssociatedSensor: ${20:# Number of associated sensor}
    numNeighborAPThreshold: ${21:# Number of neighboring AP threshold}
    profiles: ${22:# Sensor Test Template Edit's profiles}
    radioAsSensorRemoved: ${23:# Radio as sensor removed}
    rssiThreshold: ${24:# RSSI threshold}
    runNow: ${25:# Run now (YES, NO)}
    scheduleInDays: ${26:# Bit-wise value of scheduled test days}
    sensors: ${27:# Sensor Test Template Edit's sensors}
    showWlcUpgradeBanner: ${28:# Show WLC upgrade banner}
    siteHierarchy: ${29:# Site hierarchy}
    ssids: ${30:# Sensor Test Template Edit's ssids}
    startTime: ${31:# Start time}
    status: ${32:# Status of the test (RUNNING, NOTRUNNING)}
    templateName: ${33:# The test template name that is to be edited}
    testScheduleMode: ${34:# Test schedule mode (ONDEMAND, DEDICATED, SCHEDULED, CONTINUOUS, RUNNOW)}
    validate_response_schema: ${35:# Flag for Cisco DNA Center SDK to enable the validation of request bodies against a JSON schema}
    version: ${36:# The sensor test template version (must be 2)}
    wlans: ${37:# WLANs list}
  tags:
    - cisco
    - dnac
    - sensor_test_template_edit]]></content>
  <tabTrigger>cisco.dnac.sensor_test_template_edit</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for Sensor Test Template Edit</description>
</snippet>