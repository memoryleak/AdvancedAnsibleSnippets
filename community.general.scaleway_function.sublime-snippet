<snippet>
  <content><![CDATA[- name: ${1:Scaleway Function management}
  community.general.scaleway_function:
    api_timeout: ${2:# HTTP timeout to Scaleway API in seconds}
    api_token: ${3:# Scaleway OAuth token}
    api_url: ${4:# Scaleway API URL}
    description: ${5:# Description of the function}
    environment_variables: ${6:# Environment variables of the function}
    function_timeout: ${7:# The length of time your handler can spend processing a request before being stopped}
    handler: ${8:# The C(module-name}
    max_scale: ${9:# Maximum number of replicas for the function}
    memory_limit: ${10:# Resources define performance characteristics of your function}
    min_scale: ${11:# Minimum number of replicas for the function}
    name: ${12:# Name of the function}
    namespace_id: ${13:# Function namespace identifier}
    privacy: ${14:# Privacy policies define whether a function can be executed anonymously}
    query_parameters: ${15:# List of parameters passed to the query string}
    redeploy: ${16:# Redeploy the function if update is required}
    region: ${17:# Scaleway region to use (for example V(fr-par))}
    runtime: ${18:# Runtime of the function}
    secret_environment_variables: ${19:# Secret environment variables of the function}
    state: ${20:# Indicate desired state of the function}
    validate_certs: ${21:# Validate SSL certs of the Scaleway API}
    wait: ${22:# Wait for the resource to reach its desired state before returning}
    wait_sleep_time: ${23:# Time to wait before every attempt to check the state of the resource}
    wait_timeout: ${24:# Time to wait for the resource to reach the expected state}
  tags:
    - community
    - general
    - scaleway_function]]></content>
  <tabTrigger>community.general.scaleway_function</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Scaleway Function management</description>
</snippet>