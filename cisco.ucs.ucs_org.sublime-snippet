<snippet>
  <content><![CDATA[- name: ${1:Manages UCS Organizations for UCS Manager}
  cisco.ucs.ucs_org:
    delegate_to: ${2:# Where the module will be run}
    description: ${3:# A user-defined description of the organization}
    hostname: ${4:# IP address or hostname of Cisco UCS Manager}
    org_name: ${5:# The name of the organization}
    parent_org_path: ${6:# A forward slash / separated hierarchical path from the root organization to the parent of the organization to be added or updated}
    password: ${7:# Password for Cisco UCS Manager authentication}
    port: ${8:# Port number to be used during connection (by default uses 443 for https and 80 for http connection)}
    proxy: ${9:# If use_proxy is no, specfies proxy to be used for connection}
    state: ${10:# If C(absent), will remove organization}
    use_proxy: ${11:# If C(no), will not use the proxy as defined by system environment variable}
    use_ssl: ${12:# If C(no), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${13:# Username for Cisco UCS Manager authentication}
  tags:
    - cisco
    - ucs
    - ucs_org]]></content>
  <tabTrigger>cisco.ucs.ucs_org</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages UCS Organizations for UCS Manager</description>
</snippet>