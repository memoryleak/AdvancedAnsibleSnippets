<snippet>
  <content><![CDATA[- name: ${1:LACP resource module}
  cisco.nxos.nxos_lacp:
    config: ${2:# LACP global options}
    running_config: ${3:# This option is used only with state I(parsed)}
    state: ${4:# The state of the configuration after module completion}
  tags:
    - cisco
    - nxos
    - nxos_lacp]]></content>
  <tabTrigger>cisco.nxos.nxos_lacp</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>LACP resource module</description>
</snippet>