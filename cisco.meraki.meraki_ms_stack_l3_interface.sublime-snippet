<snippet>
  <content><![CDATA[- name: ${1:Manage routed interfaces on MS switches}
  cisco.meraki.meraki_ms_stack_l3_interface:
    auth_key: ${2:# Authentication key provided by the dashboard}
    default_gateway: ${3:# The next hop for any traffic that isn't going to a directly connected subnet or over a static route}
    host: ${4:# Hostname for Meraki dashboard}
    interface_id: ${5:# Uniqiue identification number for layer 3 interface}
    interface_ip: ${6:# The IP address this switch will use for layer 3 routing on this VLAN or subnet}
    internal_error_retry_time: ${7:# Number of seconds to retry if server returns an internal server error}
    multicast_routing: ${8:# Enable multicast support if multicast routing between VLANs is required}
    name: ${9:# A friendly name or description for the interface or VLAN}
    net_id: ${10:# ID of network which configuration is applied to}
    net_name: ${11:# Name of network which configuration is applied to}
    org_id: ${12:# ID of organization}
    org_name: ${13:# Name of organization}
    ospf_settings: ${14:# The OSPF routing settings of the interface}
    output_format: ${15:# Instructs module whether response keys should be snake case (ex}
    output_level: ${16:# Set amount of debug output during module execution}
    rate_limit_retry_time: ${17:# Number of seconds to retry if rate limiter is triggered}
    stack_id: ${18:# The unique identifier of the stack}
    state: ${19:# Create or modify an organization}
    subnet: ${20:# The network that this routed interface is on, in CIDR notation}
    timeout: ${21:# Time to timeout for HTTP requests}
    use_https: ${22:# If C(no), it will use HTTP}
    use_proxy: ${23:# If C(no), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    validate_certs: ${24:# Whether to validate HTTP certificates}
    vlan_id: ${25:# The VLAN this routed interface is on}
  tags:
    - cisco
    - meraki
    - meraki_ms_stack_l3_interface]]></content>
  <tabTrigger>cisco.meraki.meraki_ms_stack_l3_interface</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage routed interfaces on MS switches</description>
</snippet>