<snippet>
  <content><![CDATA[- name: ${1:Manages F5 BIG-IP GTM TCP half-open monitors}
  f5networks.f5_modules.bigip_gtm_monitor_tcp_half_open:
    ignore_down_response: ${2:# Specifies that the monitor allows more than one probe attempt per interval}
    interval: ${3:# Specifies, in seconds, the frequency at which the system issues the monitor check when either the resource is down or the status of the resource is unknown}
    ip: ${4:# IP address part of the IP/port definition}
    name: ${5:# Monitor name}
    parent: ${6:# The parent template of this monitor template}
    partition: ${7:# Device partition to manage resources on}
    port: ${8:# Port address part of the IP/port definition}
    probe_attempts: ${9:# Specifies the number of times the system attempts to probe the host server, after which the system considers the host server down or unavailable}
    probe_interval: ${10:# Specifies the number of seconds the big3d process waits before sending out a subsequent probe attempt when a probe fails and multiple probe attempts have been requested}
    probe_timeout: ${11:# Specifies the number of seconds after which the system times out the probe request to the system}
    provider: ${12:# A dict object containing connection details}
    state: ${13:# When C(present), ensures the monitor exists}
    timeout: ${14:# Specifies the number of seconds the target has in which to respond to the monitor request}
    transparent: ${15:# Specifies whether the monitor operates in transparent mode}
  tags:
    - f5networks
    - f5_modules
    - bigip_gtm_monitor_tcp_half_open]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_gtm_monitor_tcp_half_open</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages F5 BIG-IP GTM TCP half-open monitors</description>
</snippet>