<snippet>
  <content><![CDATA[- name: ${1:Manages IGMP global configuration}
  cisco.nxos.nxos_igmp:
    enforce_rtr_alert: ${2:# Enables or disables the enforce router alert option check for IGMPv2 and IGMPv3 packets}
    flush_routes: ${3:# Removes routes when the IGMP process is restarted}
    restart: ${4:# Restarts the igmp process (using an exec config command)}
    state: ${5:# Manages desired state of the resource}
  tags:
    - cisco
    - nxos
    - nxos_igmp]]></content>
  <tabTrigger>cisco.nxos.nxos_igmp</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages IGMP global configuration</description>
</snippet>