<snippet>
  <content><![CDATA[- name: ${1:Generic facts of Azure resources}
  azure.azcollection.azure_rm_resource_info:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    api_version: ${5:# Specific API version to be used}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${7:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${8:# Azure client ID}
    cloud_environment: ${9:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    log_mode: ${10:# Parent argument}
    log_path: ${11:# Parent argument}
    method: ${12:# The HTTP method of the request or response}
    password: ${13:# Active Directory user password}
    profile: ${14:# Security profile found in ~/}
    provider: ${15:# Provider type, should be specified in no URL is given}
    resource_group: ${16:# Resource group to be used}
    resource_name: ${17:# Resource name}
    resource_type: ${18:# Resource type}
    secret: ${19:# Azure client secret}
    subresource: ${20:# List of subresources}
    subscription_id: ${21:# Your Azure subscription Id}
    tenant: ${22:# Azure tenant ID}
    thumbprint: ${23:# The thumbprint of the private key specified in I(x509_certificate_path)}
    url: ${24:# Azure RM Resource URL}
    x509_certificate_path: ${25:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_resource_info]]></content>
  <tabTrigger>azure.azcollection.azure_rm_resource_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Generic facts of Azure resources</description>
</snippet>