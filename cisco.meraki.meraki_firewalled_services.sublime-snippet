<snippet>
  <content><![CDATA[- name: ${1:Edit firewall policies for administrative network services}
  cisco.meraki.meraki_firewalled_services:
    auth_key: ${2:# Authentication key provided by the dashboard}
    net_name: ${3:# Name of a network}
    net_id: ${4:# ID number of a network}
    org_name: ${5:# Name of organization associated to a network}
    org_id: ${6:# ID of organization associated to a network}
    state: ${7:# States that a policy should be created or modified}
    service: ${8:# Network service to query or modify}
    access: ${9:# Network service to query or modify}
    allowed_ips: ${10:# List of IP addresses allowed to access a service}
    host: ${11:# Hostname for Meraki dashboard}
    use_proxy: ${12:# If C(no), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_https: ${13:# If C(no), it will use HTTP}
    output_format: ${14:# Instructs module whether response keys should be snake case (ex}
    output_level: ${15:# Set amount of debug output during module execution}
    timeout: ${16:# Time to timeout for HTTP requests}
    validate_certs: ${17:# Whether to validate HTTP certificates}
    rate_limit_retry_time: ${18:# Number of seconds to retry if rate limiter is triggered}
    internal_error_retry_time: ${19:# Number of seconds to retry if server returns an internal server error}
  tags:
    - cisco
    - meraki
    - meraki_firewalled_services]]></content>
  <tabTrigger>cisco.meraki.meraki_firewalled_services</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Edit firewall policies for administrative network services</description>
</snippet>