<snippet>
  <content><![CDATA[- name: ${1:Manage lines in text files}
  ansible.builtin.lineinfile:
    path: ${2:# The file to modify}
    regexp: ${3:# The regular expression to look for in every line of the file}
    search_string: ${4:# The literal string to look for in every line of the file}
    state: ${5:# Whether the line should be there or not}
    line: ${6:# The line to insert/replace into the file}
    backrefs: ${7:# Used with C(state=present)}
    insertafter: ${8:# Used with C(state=present)}
    insertbefore: ${9:# Used with C(state=present)}
    create: ${10:# Used with C(state=present)}
    backup: ${11:# Create a backup file including the timestamp information so you can get the original file back if you somehow clobbered it incorrectly}
    firstmatch: ${12:# Used with C(insertafter) or C(insertbefore)}
    others: ${13:# All arguments accepted by the M(ansible}
    mode: ${14:# The permissions the resulting filesystem object should have}
    owner: ${15:# Name of the user that should own the filesystem object, as would be fed to I(chown)}
    group: ${16:# Name of the group that should own the filesystem object, as would be fed to I(chown)}
    seuser: ${17:# The user part of the SELinux filesystem object context}
    serole: ${18:# The role part of the SELinux filesystem object context}
    setype: ${19:# The type part of the SELinux filesystem object context}
    selevel: ${20:# The level part of the SELinux filesystem object context}
    unsafe_writes: ${21:# Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object}
    attributes: ${22:# The attributes the resulting filesystem object should have}
    validate: ${23:# The validation command to run before copying the updated file into the final destination}
  tags:
    - lineinfile]]></content>
  <tabTrigger>ansible.builtin.lineinfile</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage lines in text files</description>
</snippet>