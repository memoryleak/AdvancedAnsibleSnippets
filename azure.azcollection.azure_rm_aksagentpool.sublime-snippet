<snippet>
  <content><![CDATA[- name: ${1:Manage node pools in Kubernetes kubernetes cluster}
  azure.azcollection.azure_rm_aksagentpool:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    availability_zones: ${7:# Availability zones for nodes}
    cert_validation_mode: ${8:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${9:# Azure client ID}
    cloud_environment: ${10:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    cluster_name: ${11:# The name of the kubernetes cluster}
    count: ${12:# Number of agents (VMs) to host docker containers}
    enable_auto_scaling: ${13:# Whether to enable auto-scaler}
    log_mode: ${14:# Parent argument}
    log_path: ${15:# Parent argument}
    max_count: ${16:# Maximum number of nodes for auto-scaling}
    max_pods: ${17:# Maximum number of pods that can run on a node}
    min_count: ${18:# Minimum number of nodes for auto-scaling}
    mode: ${19:# AgentPoolMode represents mode of an agent pool}
    name: ${20:# The name of the node agent pool}
    node_labels: ${21:# Agent pool node labels to be persisted across all nodes in agent pool}
    orchestrator_version: ${22:# Version of orchestrator specified when creating the managed cluster}
    os_disk_size_gb: ${23:# OS Disk Size in GB to be used to specify the disk size for every machine in this master/agent pool}
    os_type: ${24:# OsType to be used to specify os type}
    password: ${25:# Active Directory user password}
    profile: ${26:# Security profile found in ~/}
    resource_group: ${27:# The name of the resource group}
    secret: ${28:# Azure client secret}
    state: ${29:# State of the automation runbook}
    subscription_id: ${30:# Your Azure subscription Id}
    tags: ${31:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${32:# Azure tenant ID}
    thumbprint: ${33:# The thumbprint of the private key specified in I(x509_certificate_path)}
    type_properties_type: ${34:# AgentPoolType represents types of an agent pool}
    vm_size: ${35:# Size of agent VMs}
    vnet_subnet_id: ${36:# VNet SubnetID specifies the VNet's subnet identifier}
    x509_certificate_path: ${37:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_aksagentpool]]></content>
  <tabTrigger>azure.azcollection.azure_rm_aksagentpool</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage node pools in Kubernetes kubernetes cluster</description>
</snippet>