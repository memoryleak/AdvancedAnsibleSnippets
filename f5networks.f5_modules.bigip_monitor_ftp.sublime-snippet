<snippet>
  <content><![CDATA[- name: ${1:Manage FTP monitors on a BIG-IP}
  f5networks.f5_modules.bigip_monitor_ftp:
    app_service: ${2:# The iApp service to be associated with this profile}
    debug: ${3:# Specifies whether the monitor sends error messages and additional information to a log file created and labeled specifically for this monitor}
    description: ${4:# The description of the monitor}
    filename: ${5:# Specifies the full path and file name of the file the system attempts to download}
    interval: ${6:# The interval specifying how frequently the monitor instance of this template will run}
    ip: ${7:# IP address part of the IP/port definition}
    manual_resume: ${8:# Specifies whether the system automatically changes the status of a resource to B(enabled) at the next successful monitor check}
    mode: ${9:# Specifies the data transfer process (DTP) mode}
    name: ${10:# Specifies the name of the monitor}
    parent: ${11:# The parent template of this monitor template}
    partition: ${12:# Device partition to manage resources on}
    port: ${13:# Port address part of the IP/port definition}
    provider: ${14:# A dict object containing connection details}
    state: ${15:# When C(present), ensures the monitor exists}
    target_password: ${16:# Specifies the password, if the monitored target requires authentication}
    target_username: ${17:# Specifies the user name, if the monitored target requires authentication}
    time_until_up: ${18:# Specifies the amount of time in seconds after the first successful response before a node will be marked up}
    timeout: ${19:# The number of seconds in which the node or service must respond to the monitor request}
    up_interval: ${20:# Specifies the interval at which the system performs the health check when a resource is up}
    update_password: ${21:# C(always) will update passwords if the C(target_password) is specified}
  tags:
    - f5networks
    - f5_modules
    - bigip_monitor_ftp]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_monitor_ftp</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage FTP monitors on a BIG-IP</description>
</snippet>