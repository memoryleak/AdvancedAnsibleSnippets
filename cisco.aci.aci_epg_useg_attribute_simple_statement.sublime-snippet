<snippet>
  <content><![CDATA[- name: ${1:Manage EPG useg Attributes Simple Statements (fv:DnsAttr, fv:IdGroupAttr, fv:IpAttr, fv:MacAttr, and fv:VmAttr)}
  cisco.aci.aci_epg_useg_attribute_simple_statement:
    annotation: ${2:# User-defined string for annotating an object}
    ap: ${3:# The name of an existing application network profile}
    category: ${4:# The name of the vmware tag category or vmware custom attribute}
    certificate_name: ${5:# The X}
    epg: ${6:# The name of an existing end point group}
    host: ${7:# IP Address or hostname of APIC resolvable by Ansible control host}
    name: ${8:# The name of the EPG useg attribute}
    operator: ${9:# The operator of the EPG useg attribute}
    output_level: ${10:# Influence the output of this ACI module}
    output_path: ${11:# Path to a file that will be used to dump the ACI JSON configuration objects generated by the module}
    parent_block_statements: ${12:# The list of parent block statements}
    password: ${13:# The password to use for authentication}
    port: ${14:# Port number to be used for REST connection}
    private_key: ${15:# Either a PEM-formatted private key file or the private key content used for signature-based authentication}
    state: ${16:# Use C(present) or C(absent) for adding or removing}
    suppress_previous: ${17:# If C(true), a GET to check previous will not be sent before a POST update to APIC}
    suppress_verification: ${18:# If C(true), a verifying GET will not be sent after a POST update to APIC}
    tenant: ${19:# The name of an existing tenant}
    timeout: ${20:# The socket level timeout in seconds}
    type: ${21:# The type of the EPG useg attribute}
    use_proxy: ${22:# If C(false), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_ssl: ${23:# If C(false), an HTTP connection will be used instead of the default HTTPS connection}
    use_subnet: ${24:# Whether to use the EPG subnet definition for ip}
    username: ${25:# The username to use for authentication}
    validate_certs: ${26:# If C(false), SSL certificates will not be validated}
    value: ${27:# The value of the EPG useg attribute}
  tags:
    - cisco
    - aci
    - aci_epg_useg_attribute_simple_statement]]></content>
  <tabTrigger>cisco.aci.aci_epg_useg_attribute_simple_statement</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage EPG useg Attributes Simple Statements (fv:DnsAttr, fv:IdGroupAttr, fv:IpAttr, fv:MacAttr, and fv:VmAttr)</description>
</snippet>