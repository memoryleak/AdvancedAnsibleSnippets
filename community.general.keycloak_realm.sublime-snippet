<snippet>
  <content><![CDATA[- name: ${1:Allows administration of Keycloak realm via Keycloak API}
  community.general.keycloak_realm:
    access_code_lifespan: ${2:# The realm access code lifespan}
    access_code_lifespan_login: ${3:# The realm access code lifespan login}
    access_code_lifespan_user_action: ${4:# The realm access code lifespan user action}
    access_token_lifespan: ${5:# The realm access token lifespan}
    access_token_lifespan_for_implicit_flow: ${6:# The realm access token lifespan for implicit flow}
    account_theme: ${7:# The realm account theme}
    action_token_generated_by_admin_lifespan: ${8:# The realm action token generated by admin lifespan}
    action_token_generated_by_user_lifespan: ${9:# The realm action token generated by user lifespan}
    admin_events_details_enabled: ${10:# The realm admin events details enabled}
    admin_events_enabled: ${11:# The realm admin events enabled}
    admin_theme: ${12:# The realm admin theme}
    attributes: ${13:# The realm attributes}
    auth_client_id: ${14:# OpenID Connect C(client_id) to authenticate to the API with}
    auth_client_secret: ${15:# Client Secret to use in conjunction with O(auth_client_id) (if required)}
    auth_keycloak_url: ${16:# URL to the Keycloak instance}
    auth_password: ${17:# Password to authenticate for API access with}
    auth_realm: ${18:# Keycloak realm name to authenticate to for API access}
    auth_username: ${19:# Username to authenticate for API access with}
    browser_flow: ${20:# The realm browser flow}
    browser_security_headers: ${21:# The realm browser security headers}
    brute_force_protected: ${22:# The realm brute force protected}
    client_authentication_flow: ${23:# The realm client authentication flow}
    client_scope_mappings: ${24:# The realm client scope mappings}
    connection_timeout: ${25:# Controls the HTTP connections timeout period (in seconds) to Keycloak API}
    default_default_client_scopes: ${26:# The realm default default client scopes}
    default_groups: ${27:# The realm default groups}
    default_locale: ${28:# The realm default locale}
    default_optional_client_scopes: ${29:# The realm default optional client scopes}
    default_roles: ${30:# The realm default roles}
    default_signature_algorithm: ${31:# The realm default signature algorithm}
    direct_grant_flow: ${32:# The realm direct grant flow}
    display_name: ${33:# The realm display name}
    display_name_html: ${34:# The realm display name HTML}
    docker_authentication_flow: ${35:# The realm docker authentication flow}
    duplicate_emails_allowed: ${36:# The realm duplicate emails allowed option}
    edit_username_allowed: ${37:# The realm edit username allowed option}
    email_theme: ${38:# The realm email theme}
    enabled: ${39:# The realm enabled option}
    enabled_event_types: ${40:# The realm enabled event types}
    events_enabled: ${41:# Enables or disables login events for this realm}
    events_expiration: ${42:# The realm events expiration}
    events_listeners: ${43:# The realm events listeners}
    failure_factor: ${44:# The realm failure factor}
    http_agent: ${45:# Configures the HTTP User-Agent header}
    id: ${46:# The realm to create}
    internationalization_enabled: ${47:# The realm internationalization enabled option}
    login_theme: ${48:# The realm login theme}
    login_with_email_allowed: ${49:# The realm login with email allowed option}
    max_delta_time_seconds: ${50:# The realm max delta time in seconds}
    max_failure_wait_seconds: ${51:# The realm max failure wait in seconds}
    minimum_quick_login_wait_seconds: ${52:# The realm minimum quick login wait in seconds}
    not_before: ${53:# The realm not before}
    offline_session_idle_timeout: ${54:# The realm offline session idle timeout}
    offline_session_max_lifespan: ${55:# The realm offline session max lifespan}
    offline_session_max_lifespan_enabled: ${56:# The realm offline session max lifespan enabled option}
    otp_policy_algorithm: ${57:# The realm otp policy algorithm}
    otp_policy_digits: ${58:# The realm otp policy digits}
    otp_policy_initial_counter: ${59:# The realm otp policy initial counter}
    otp_policy_look_ahead_window: ${60:# The realm otp policy look ahead window}
    otp_policy_period: ${61:# The realm otp policy period}
    otp_policy_type: ${62:# The realm otp policy type}
    otp_supported_applications: ${63:# The realm otp supported applications}
    password_policy: ${64:# The realm password policy}
    permanent_lockout: ${65:# The realm permanent lockout}
    quick_login_check_milli_seconds: ${66:# The realm quick login check in milliseconds}
    realm: ${67:# The realm name}
    refresh_token_max_reuse: ${68:# The realm refresh token max reuse}
    registration_allowed: ${69:# The realm registration allowed option}
    registration_email_as_username: ${70:# The realm registration email as username option}
    registration_flow: ${71:# The realm registration flow}
    remember_me: ${72:# The realm remember me option}
    reset_credentials_flow: ${73:# The realm reset credentials flow}
    reset_password_allowed: ${74:# The realm reset password allowed option}
    revoke_refresh_token: ${75:# The realm revoke refresh token option}
    smtp_server: ${76:# The realm smtp server}
    ssl_required: ${77:# The realm ssl required option}
    sso_session_idle_timeout: ${78:# The realm sso session idle timeout}
    sso_session_idle_timeout_remember_me: ${79:# The realm sso session idle timeout remember me}
    sso_session_max_lifespan: ${80:# The realm sso session max lifespan}
    sso_session_max_lifespan_remember_me: ${81:# The realm sso session max lifespan remember me}
    state: ${82:# State of the realm}
    supported_locales: ${83:# The realm supported locales}
    token: ${84:# Authentication token for Keycloak API}
    user_managed_access_allowed: ${85:# The realm user managed access allowed option}
    validate_certs: ${86:# Verify TLS certificates (do not disable this in production)}
    verify_email: ${87:# The realm verify email option}
    wait_increment_seconds: ${88:# The realm wait increment in seconds}
  tags:
    - community
    - general
    - keycloak_realm]]></content>
  <tabTrigger>community.general.keycloak_realm</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Allows administration of Keycloak realm via Keycloak API</description>
</snippet>