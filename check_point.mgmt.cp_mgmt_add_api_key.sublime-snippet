<snippet>
  <content><![CDATA[- name: ${1:Add API key for administrator, to enable login with it}
  check_point.mgmt.cp_mgmt_add_api_key:
    admin_name: ${2:# Administrator name to generate API key for}
    admin_uid: ${3:# Administrator uid to generate API key for}
    auto_publish_session: ${4:# Publish the current session if changes have been performed after task completes}
    version: ${5:# Version of checkpoint}
    wait_for_task: ${6:# Wait for the task to end}
    wait_for_task_timeout: ${7:# How many minutes to wait until throwing a timeout error}
  tags:
    - check_point
    - mgmt
    - cp_mgmt_add_api_key]]></content>
  <tabTrigger>check_point.mgmt.cp_mgmt_add_api_key</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Add API key for administrator, to enable login with it</description>
</snippet>