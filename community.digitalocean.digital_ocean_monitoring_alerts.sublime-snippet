<snippet>
  <content><![CDATA[- name: ${1:Programmatically retrieve metrics as well as configure alert policies based on these metrics}
  community.digitalocean.digital_ocean_monitoring_alerts:
    oauth_token: ${2:# DigitalOcean OAuth token; can be specified in C(DO_API_KEY), C(DO_API_TOKEN), or C(DO_OAUTH_TOKEN) environment variables}
    state: ${3:# The usual, C(present) to create, C(absent) to destroy}
    alerts: ${4:# Alert object, required for C(state=present)}
    compare: ${5:# Alert comparison, required for C(state=present)}
    description: ${6:# Alert description, required for C(state=present)}
    enabled: ${7:# Enabled or not, required for C(state=present)}
    entities: ${8:# Alert entities, required for C(state=present)}
    tags: ${9:# Alert tags, required for C(state=present)}
    type: ${10:# Alert type, required for C(state=present)}
    value: ${11:# Alert threshold, required for C(state=present)}
    window: ${12:# Alert window, required for C(state=present)}
    uuid: ${13:# Alert uuid, required for C(state=absent)}
  tags:
    - community
    - digitalocean
    - digital_ocean_monitoring_alerts]]></content>
  <tabTrigger>community.digitalocean.digital_ocean_monitoring_alerts</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Programmatically retrieve metrics as well as configure alert policies based on these metrics</description>
</snippet>