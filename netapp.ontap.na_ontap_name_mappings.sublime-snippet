<snippet>
  <content><![CDATA[- name: ${1:NetApp ONTAP name mappings}
  netapp.ontap.na_ontap_name_mappings:
    cert_filepath: ${2:# path to SSL client cert file (}
    client_match: ${3:# Client workstation IP Address which is matched when searching for the pattern}
    direction: ${4:# Direction in which the name mapping is applied}
    feature_flags: ${5:# Enable or disable a new feature}
    force_ontap_version: ${6:# Override the cluster ONTAP version when using REST}
    from_index: ${7:# If no entry with index is found, it is created by reindexing the entry for from_index}
    hostname: ${8:# The hostname or IP address of the ONTAP instance}
    http_port: ${9:# Override the default port (80 or 443) with this port}
    https: ${10:# Enable and disable https}
    index: ${11:# Position in the list of name mappings}
    key_filepath: ${12:# path to SSL client key file}
    ontapi: ${13:# The ontap api version to use}
    password: ${14:# Password for the specified user}
    pattern: ${15:# Pattern used to match the name while searching for a name that can be used as a replacement}
    replacement: ${16:# The name that is used as a replacement, if the pattern associated with this entry matches}
    state: ${17:# Whether the specified name mappings should exist or not}
    use_rest: ${18:# Whether to use REST or ZAPI}
    username: ${19:# This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required}
    validate_certs: ${20:# If set to C(no), the SSL certificates will not be validated}
    vserver: ${21:# Name of the vserver to use}
  tags:
    - netapp
    - ontap
    - na_ontap_name_mappings]]></content>
  <tabTrigger>netapp.ontap.na_ontap_name_mappings</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp ONTAP name mappings</description>
</snippet>