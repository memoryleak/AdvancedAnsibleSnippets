<snippet>
  <content><![CDATA[- name: ${1:Manage Cloudflare DNS records}
  community.general.cloudflare_dns:
    account_api_key: ${2:# Account API key}
    account_email: ${3:# Account email}
    algorithm: ${4:# Algorithm number}
    api_token: ${5:# API token}
    cert_usage: ${6:# Certificate usage number}
    flag: ${7:# Issuer Critical Flag}
    hash_type: ${8:# Hash type number}
    key_tag: ${9:# DNSSEC key tag}
    port: ${10:# Service port}
    priority: ${11:# Record priority}
    proto: ${12:# Service protocol}
    proxied: ${13:# Proxy through Cloudflare network or just use DNS}
    record: ${14:# Record to add}
    selector: ${15:# Selector number}
    service: ${16:# Record service}
    solo: ${17:# Whether the record should be the only one for that record type and record name}
    state: ${18:# Whether the record(s) should exist or not}
    tag: ${19:# CAA issue restriction}
    timeout: ${20:# Timeout for Cloudflare API calls}
    ttl: ${21:# The TTL to give the new record}
    type: ${22:# The type of DNS record to create}
    value: ${23:# The record value}
    weight: ${24:# Service weight}
    zone: ${25:# The name of the Zone to work with (e}
  tags:
    - community
    - general
    - cloudflare_dns]]></content>
  <tabTrigger>community.general.cloudflare_dns</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Cloudflare DNS records</description>
</snippet>