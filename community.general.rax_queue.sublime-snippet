<snippet>
  <content><![CDATA[- name: ${1:Create / delete a queue in Rackspace Public Cloud}
  community.general.rax_queue:
    name: ${2:# Name to give the queue}
    state: ${3:# Indicate desired state of the resource}
    api_key: ${4:# Rackspace API key, overrides I(credentials)}
    credentials: ${5:# File to find the Rackspace credentials in}
    env: ${6:# Environment as configured in I(~/}
    region: ${7:# Region to create an instance in}
    username: ${8:# Rackspace username, overrides I(credentials)}
    validate_certs: ${9:# Whether or not to require SSL validation of API endpoints}
    auth_endpoint: ${10:# The URI of the authentication service}
    identity_type: ${11:# Authentication mechanism to use, such as rackspace or keystone}
    tenant_id: ${12:# The tenant ID used for authentication}
    tenant_name: ${13:# The tenant name used for authentication}
  tags:
    - community
    - general
    - rax_queue]]></content>
  <tabTrigger>community.general.rax_queue</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create / delete a queue in Rackspace Public Cloud</description>
</snippet>