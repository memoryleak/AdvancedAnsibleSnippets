<snippet>
  <content><![CDATA[- name: ${1:Manage Azure load balancers}
  azure.azcollection.azure_rm_loadbalancer:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    backend_address_pools: ${7:# List of backend address pools}
    cert_validation_mode: ${8:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${9:# Azure client ID}
    cloud_environment: ${10:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    disable_instance_discovery: ${11:# Determines whether or not instance discovery is performed when attempting to authenticate}
    frontend_ip_configurations: ${12:# List of frontend IPs to be used}
    inbound_nat_pools: ${13:# Defines an external port range for inbound NAT to a single backend port on NICs associated with a load balancer}
    inbound_nat_rules: ${14:# Collection of inbound NAT Rules used by a load balancer}
    load_balancing_rules: ${15:# Object collection representing the load balancing rules Gets the provisioning}
    location: ${16:# Valid Azure location}
    log_mode: ${17:# Parent argument}
    log_path: ${18:# Parent argument}
    name: ${19:# Name of the load balancer}
    password: ${20:# Active Directory user password}
    probes: ${21:# List of probe definitions used to check endpoint health}
    profile: ${22:# Security profile found in ~/}
    resource_group: ${23:# Name of a resource group where the load balancer exists or will be created}
    secret: ${24:# Azure client secret}
    sku: ${25:# The load balancer SKU}
    state: ${26:# Assert the state of the load balancer}
    subscription_id: ${27:# Your Azure subscription Id}
    tags: ${28:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${29:# Azure tenant ID}
    thumbprint: ${30:# The thumbprint of the private key specified in I(x509_certificate_path)}
    x509_certificate_path: ${31:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_loadbalancer]]></content>
  <tabTrigger>azure.azcollection.azure_rm_loadbalancer</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure load balancers</description>
</snippet>