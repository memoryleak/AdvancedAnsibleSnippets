<snippet>
  <content><![CDATA[- name: ${1:Manage IPSec Traffic Selectors on BIG-IP}
  f5networks.f5_modules.bigip_traffic_selector:
    description: ${2:# Description of the traffic selector}
    destination_address: ${3:# Specifies the host or network IP address to which the application traffic is destined}
    ipsec_policy: ${4:# Specifies the IPsec policy that tells the BIG-IP system how to handle the packets}
    name: ${5:# Specifies the name of the traffic selector}
    order: ${6:# Specifies the order in which traffic is matched, if traffic can be matched to multiple traffic selectors}
    partition: ${7:# Device partition to manage resources on}
    provider: ${8:# A dict object containing connection details}
    source_address: ${9:# Specifies the host or network IP address from which the application traffic originates}
    state: ${10:# When C(present), ensures the resource exists}
  tags:
    - f5networks
    - f5_modules
    - bigip_traffic_selector]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_traffic_selector</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage IPSec Traffic Selectors on BIG-IP</description>
</snippet>