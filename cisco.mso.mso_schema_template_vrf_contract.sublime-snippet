<snippet>
  <content><![CDATA[- name: ${1:Manage vrf contracts in schema templates}
  cisco.mso.mso_schema_template_vrf_contract:
    contract: ${2:# A contract associated to this VRF}
    host: ${3:# IP Address or hostname of the ACI Multi Site Orchestrator host}
    login_domain: ${4:# The login domain name to use for authentication}
    output_level: ${5:# Influence the output of this MSO module}
    password: ${6:# The password to use for authentication}
    port: ${7:# Port number to be used for the REST connection}
    schema: ${8:# The name of the schema}
    state: ${9:# Use C(present) or C(absent) for adding or removing}
    template: ${10:# The name of the template to change}
    timeout: ${11:# The socket level timeout in seconds}
    use_proxy: ${12:# If C(false), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_ssl: ${13:# If C(false), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${14:# The username to use for authentication}
    validate_certs: ${15:# If C(false), SSL certificates will not be validated}
    vrf: ${16:# The name of the VRF}
  tags:
    - cisco
    - mso
    - mso_schema_template_vrf_contract]]></content>
  <tabTrigger>cisco.mso.mso_schema_template_vrf_contract</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage vrf contracts in schema templates</description>
</snippet>