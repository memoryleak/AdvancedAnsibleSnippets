<snippet>
  <content><![CDATA[- name: ${1:Add, remove, or move an ESXi host to, from, or within vCenter}
  community.vmware.vmware_host:
    add_connected: ${2:# If set to V(true), then the host should be connected as soon as it is added}
    cluster_name: ${3:# Name of the cluster to add the host}
    datacenter_name: ${4:# Name of the datacenter to add the host}
    esxi_hostname: ${5:# ESXi hostname to manage}
    esxi_password: ${6:# ESXi password}
    esxi_ssl_thumbprint: ${7:# Specifying the hostsystem certificate's thumbprint}
    esxi_username: ${8:# ESXi username}
    fetch_ssl_thumbprint: ${9:# Fetch the thumbprint of the host's SSL certificate}
    folder: ${10:# Name of the folder under which host to add}
    force_connection: ${11:# Force the connection if the host is already being managed by another vCenter server}
    hostname: ${12:# The hostname or IP address of the vSphere vCenter or ESXi server}
    password: ${13:# The password of the vSphere vCenter or ESXi server}
    port: ${14:# The port number of the vSphere vCenter or ESXi server}
    proxy_host: ${15:# Address of a proxy that will receive all HTTPS requests and relay them}
    proxy_port: ${16:# Port of the HTTP proxy that will receive all HTTPS requests and relay them}
    reconnect_disconnected: ${17:# Reconnect disconnected hosts}
    state: ${18:# If set to V(present), add the host if host is absent}
    username: ${19:# The username of the vSphere vCenter or ESXi server}
    validate_certs: ${20:# Allows connection when SSL certificates are not valid}
  tags:
    - community
    - vmware
    - vmware_host]]></content>
  <tabTrigger>community.vmware.vmware_host</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Add, remove, or move an ESXi host to, from, or within vCenter</description>
</snippet>