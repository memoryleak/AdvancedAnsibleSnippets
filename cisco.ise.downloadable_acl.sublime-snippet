<snippet>
  <content><![CDATA[- name: ${1:Resource module for Downloadable ACL}
  cisco.ise.downloadable_acl:
    dacl: ${2:# The DACL Content}
    daclType: ${3:# Allowed values - IPV4, - IPV6, - IP_AGNOSTIC}
    description: ${4:# Use the string \\\\n for a newline}
    id: ${5:# Downloadable ACL's id}
    name: ${6:# Resource Name}
    ise_hostname: ${7:# The Identity Services Engine hostname}
    ise_username: ${8:# The Identity Services Engine username to authenticate}
    ise_password: ${9:# The Identity Services Engine password to authenticate}
    ise_verify: ${10:# Flag to enable or disable SSL certificate verification}
    ise_version: ${11:# Informs the SDK which version of Identity Services Engine to use}
    ise_wait_on_rate_limit: ${12:# Flag for Identity Services Engine SDK to enable automatic rate-limit handling}
    ise_debug: ${13:# Flag for Identity Services Engine SDK to enable debugging}
    ise_uses_api_gateway: ${14:# Flag that informs the SDK whether to use the Identity Services Engine's API Gateway to send requests}
    ise_uses_csrf_token: ${15:# Flag that informs the SDK whether we send the CSRF token to ISE's ERS APIs}
  tags:
    - cisco
    - ise
    - downloadable_acl]]></content>
  <tabTrigger>cisco.ise.downloadable_acl</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for Downloadable ACL</description>
</snippet>