<snippet>
  <content><![CDATA[- name: ${1:register a task definition in ecs}
  community.aws.ecs_taskdefinition:
    access_key: ${2:# AWS access key ID}
    arn: ${3:# The ARN of the task description to delete}
    aws_ca_bundle: ${4:# The location of a CA Bundle to use when validating SSL certificates}
    aws_config: ${5:# A dictionary to modify the botocore configuration}
    containers: ${6:# A list of containers definitions}
    cpu: ${7:# The number of cpu units used by the task}
    debug_botocore_endpoint_logs: ${8:# Use a C(botocore}
    endpoint_url: ${9:# URL to connect to instead of the default AWS endpoints}
    execution_role_arn: ${10:# The Amazon Resource Name (ARN) of the task execution role that the Amazon ECS container agent and the Docker daemon can assume}
    family: ${11:# A Name that would be given to the task definition}
    force_create: ${12:# Always create new task definition}
    launch_type: ${13:# The launch type on which to run your task}
    memory: ${14:# The amount (in MiB) of memory used by the task}
    network_mode: ${15:# The Docker networking mode to use for the containers in the task}
    placement_constraints: ${16:# Placement constraint objects to use for the task}
    profile: ${17:# A named AWS profile to use for authentication}
    region: ${18:# The AWS region to use}
    revision: ${19:# A revision number for the task definition}
    runtime_platform: ${20:# runtime platform configuration for the task}
    secret_key: ${21:# AWS secret access key}
    session_token: ${22:# AWS STS session token for use with temporary credentials}
    state: ${23:# State whether the task definition should exist or be deleted}
    task_role_arn: ${24:# The Amazon Resource Name (ARN) of the IAM role that containers in this task can assume}
    validate_certs: ${25:# When set to C(false), SSL certificates will not be validated for communication with the AWS APIs}
    volumes: ${26:# A list of names of volumes to be attached}
  tags:
    - community
    - aws
    - ecs_taskdefinition]]></content>
  <tabTrigger>community.aws.ecs_taskdefinition</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>register a task definition in ecs</description>
</snippet>