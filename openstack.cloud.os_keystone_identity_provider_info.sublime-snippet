<snippet>
  <content><![CDATA[- name: ${1:Get the information about the available federation identity providers}
  openstack.cloud.os_keystone_identity_provider_info:
    name: ${2:# The name of the identity provider to fetch}
    cloud: ${3:# Named cloud or cloud config to operate against}
    auth: ${4:# Dictionary containing auth information as needed by the cloud's auth plugin strategy}
    auth_type: ${5:# Name of the auth plugin to use}
    region_name: ${6:# Name of the region}
    wait: ${7:# Should ansible wait until the requested resource is complete}
    timeout: ${8:# How long should ansible wait for the requested resource}
    api_timeout: ${9:# How long should the socket layer wait before timing out for API calls}
    validate_certs: ${10:# Whether or not SSL API requests should be verified}
    ca_cert: ${11:# A path to a CA Cert bundle that can be used as part of verifying SSL API requests}
    client_cert: ${12:# A path to a client certificate to use as part of the SSL transaction}
    client_key: ${13:# A path to a client key to use as part of the SSL transaction}
    interface: ${14:# Endpoint URL type to fetch from the service catalog}
    availability_zone: ${15:# Ignored}
    sdk_log_path: ${16:# Path to the logfile of the OpenStackSDK}
    sdk_log_level: ${17:# Log level of the OpenStackSDK}
  tags:
    - openstack
    - cloud
    - os_keystone_identity_provider_info]]></content>
  <tabTrigger>openstack.cloud.os_keystone_identity_provider_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Get the information about the available federation identity providers</description>
</snippet>