<snippet>
  <content><![CDATA[- name: ${1:Manage bits and pieces of XML files or strings}
  community.general.xml:
    add_children: ${2:# Add additional child-element(s) to a selected element for a given O(xpath)}
    attribute: ${3:# The attribute to select when using parameter O(value)}
    backup: ${4:# Create a backup file including the timestamp information so you can get the original file back if you somehow clobbered it incorrectly}
    content: ${5:# Search for a given O(xpath) and get content}
    count: ${6:# Search for a given O(xpath) and provide the count of any matches}
    input_type: ${7:# Type of input for O(add_children) and O(set_children)}
    insertafter: ${8:# Add additional child-element(s) after the last selected element for a given O(xpath)}
    insertbefore: ${9:# Add additional child-element(s) before the first selected element for a given O(xpath)}
    namespaces: ${10:# The namespace C(prefix:uri) mapping for the XPath expression}
    path: ${11:# Path to the file to operate on}
    pretty_print: ${12:# Pretty print XML output}
    print_match: ${13:# Search for a given O(xpath) and print out any matches}
    set_children: ${14:# Set the child-element(s) of a selected element for a given O(xpath)}
    state: ${15:# Set or remove an xpath selection (node(s), attribute(s))}
    strip_cdata_tags: ${16:# Remove CDATA tags surrounding text values}
    value: ${17:# Desired state of the selected attribute}
    xmlstring: ${18:# A string containing XML on which to operate}
    xpath: ${19:# A valid XPath expression describing the item(s) you want to manipulate}
  tags:
    - community
    - general
    - xml]]></content>
  <tabTrigger>community.general.xml</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage bits and pieces of XML files or strings</description>
</snippet>