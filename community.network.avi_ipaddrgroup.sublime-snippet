<snippet>
  <content><![CDATA[- name: ${1:Module for setup of IpAddrGroup Avi RESTful Object}
  community.network.avi_ipaddrgroup:
    addrs: ${2:# Configure ip address(es)}
    api_context: ${3:# Avi API context that includes current session ID and CSRF Token}
    api_version: ${4:# Avi API version of to use for Avi API and objects}
    apic_epg_name: ${5:# Populate ip addresses from members of this cisco apic epg}
    avi_api_patch_op: ${6:# Patch operation to use when using avi_api_update_method as patch}
    avi_api_update_method: ${7:# Default method for object update is HTTP PUT}
    avi_credentials: ${8:# Avi Credentials dictionary which can be used in lieu of enumerating Avi Controller login details}
    avi_disable_session_cache_as_fact: ${9:# It disables avi session information to be cached as a fact}
    controller: ${10:# IP address or hostname of the controller}
    country_codes: ${11:# Populate the ip address ranges from the geo database for this country}
    description: ${12:# User defined description for the object}
    ip_ports: ${13:# Configure (ip address, port) tuple(s)}
    marathon_app_name: ${14:# Populate ip addresses from tasks of this marathon app}
    marathon_service_port: ${15:# Task port associated with marathon service port}
    name: ${16:# Name of the ip address group}
    password: ${17:# Password of Avi user in Avi controller}
    prefixes: ${18:# Configure ip address prefix(es)}
    ranges: ${19:# Configure ip address range(s)}
    state: ${20:# The state that should be applied on the entity}
    tenant: ${21:# Name of tenant used for all Avi API calls and context of object}
    tenant_ref: ${22:# It is a reference to an object of type tenant}
    tenant_uuid: ${23:# UUID of tenant used for all Avi API calls and context of object}
    url: ${24:# Avi controller URL of the object}
    username: ${25:# Username used for accessing Avi controller}
    uuid: ${26:# Uuid of the ip address group}
  tags:
    - community
    - network
    - avi_ipaddrgroup]]></content>
  <tabTrigger>community.network.avi_ipaddrgroup</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Module for setup of IpAddrGroup Avi RESTful Object</description>
</snippet>