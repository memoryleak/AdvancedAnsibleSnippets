<snippet>
  <content><![CDATA[- name: ${1:Executes a command on a remote Windows node}
  ansible.windows.win_command:
    _raw_params: ${2:# The C(win_command) module takes a free form command to run}
    argv: ${3:# A list that contains the executable and arguments to run}
    chdir: ${4:# Set the specified path as the current working directory before executing a command}
    cmd: ${5:# The command and arguments to run}
    creates: ${6:# A path or path filter pattern; when the referenced path exists on the target host, the task will be skipped}
    output_encoding_override: ${7:# This option overrides the encoding of stdout/stderr output}
    removes: ${8:# A path or path filter pattern; when the referenced path B(does not) exist on the target host, the task will be skipped}
    stdin: ${9:# Set the stdin of the command directly to the specified value}
  tags:
    - win_command]]></content>
  <tabTrigger>ansible.windows.win_command</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Executes a command on a remote Windows node</description>
</snippet>