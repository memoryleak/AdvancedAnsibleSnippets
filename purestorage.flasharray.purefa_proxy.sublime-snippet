<snippet>
  <content><![CDATA[- name: ${1:Configure FlashArray phonehome HTTPs proxy settings}
  purestorage.flasharray.purefa_proxy:
    api_token: ${2:# FlashArray API token for admin privileged user}
    disable_warnings: ${3:# Disable insecure certificate warnings in debug logs}
    fa_url: ${4:# FlashArray management IPv4 address or Hostname}
    host: ${5:# The proxy host name}
    port: ${6:# The proxy TCP/IP port number}
    protocol: ${7:# The proxy protocol}
    state: ${8:# Set or delete proxy configuration}
  tags:
    - purestorage
    - flasharray
    - purefa_proxy]]></content>
  <tabTrigger>purestorage.flasharray.purefa_proxy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configure FlashArray phonehome HTTPs proxy settings</description>
</snippet>