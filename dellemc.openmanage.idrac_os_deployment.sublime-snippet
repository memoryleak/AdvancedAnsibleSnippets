<snippet>
  <content><![CDATA[- name: ${1:Boot to a network ISO image}
  dellemc.openmanage.idrac_os_deployment:
    ca_path: ${2:# The Privacy Enhanced Mail (PEM) file that contains a CA certificate to be used for the validation}
    expose_duration: ${3:# It is the time taken in minutes for the ISO image file to be exposed as a local CD-ROM device to the host server}
    idrac_ip: ${4:# iDRAC IP Address}
    idrac_password: ${5:# iDRAC user password}
    idrac_port: ${6:# iDRAC port}
    idrac_user: ${7:# iDRAC username}
    iso_image: ${8:# Network ISO name}
    share_name: ${9:# CIFS or NFS Network share}
    share_password: ${10:# Network share user password}
    share_user: ${11:# Network share user in the format 'user@domain' or 'domain\\\\user' if user is part of a domain else 'user'}
    timeout: ${12:# The socket level timeout in seconds}
    validate_certs: ${13:# If C(false), the SSL certificates will not be validated}
  tags:
    - dellemc
    - openmanage
    - idrac_os_deployment]]></content>
  <tabTrigger>dellemc.openmanage.idrac_os_deployment</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Boot to a network ISO image</description>
</snippet>