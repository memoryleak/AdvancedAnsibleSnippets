<snippet>
  <content><![CDATA[- name: ${1:Create and manage AWS Datapipelines}
  community.aws.data_pipeline:
    access_key: ${2:# AWS access key ID}
    aws_ca_bundle: ${3:# The location of a CA Bundle to use when validating SSL certificates}
    aws_config: ${4:# A dictionary to modify the botocore configuration}
    debug_botocore_endpoint_logs: ${5:# Use a C(botocore}
    description: ${6:# An optional description for the pipeline being created}
    endpoint_url: ${7:# URL to connect to instead of the default AWS endpoints}
    name: ${8:# The name of the Datapipeline to create/modify/delete}
    objects: ${9:# A list of pipeline object definitions, each of which is a dict that takes the keys I(id), I(name) and I(fields)}
    parameters: ${10:# A list of parameter objects (dicts) in the pipeline definition}
    profile: ${11:# A named AWS profile to use for authentication}
    region: ${12:# The AWS region to use}
    secret_key: ${13:# AWS secret access key}
    session_token: ${14:# AWS STS session token for use with temporary credentials}
    state: ${15:# The requested state of the pipeline}
    tags: ${16:# A dict of key:value pair(s) to add to the pipeline}
    timeout: ${17:# Time in seconds to wait for the pipeline to transition to the requested state, fail otherwise}
    validate_certs: ${18:# When set to C(false), SSL certificates will not be validated for communication with the AWS APIs}
    values: ${19:# A list of parameter values (dicts) in the pipeline definition}
  tags:
    - community
    - aws
    - data_pipeline]]></content>
  <tabTrigger>community.aws.data_pipeline</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create and manage AWS Datapipelines</description>
</snippet>