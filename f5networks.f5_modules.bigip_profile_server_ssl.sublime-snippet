<snippet>
  <content><![CDATA[- name: ${1:Manages server SSL profiles on a BIG-IP}
  f5networks.f5_modules.bigip_profile_server_ssl:
    authenticate_name: ${2:# Specifies a Common Name (CN) that is embedded in a server certificate}
    ca_file: ${3:# Specifies a server CA the system trusts}
    certificate: ${4:# Specifies the name of the certificate the system uses for server-side SSL processing}
    chain: ${5:# Specifies the certificates-key chain to associate with the SSL profile}
    cipher_group: ${6:# Specifies the cipher group to assign to this profile}
    ciphers: ${7:# Specifies the list of ciphers the system supports}
    key: ${8:# Specifies the file name of the SSL key}
    name: ${9:# Specifies the name of the profile}
    ocsp_profile: ${10:# Specifies the name of the OCSP profile for purpose of validating the status of server certificate}
    options: ${11:# Options the system uses for SSL processing in the form of a list}
    parent: ${12:# The parent template of this monitor template}
    partition: ${13:# Device partition to manage resources on}
    passphrase: ${14:# Specifies a passphrase used to encrypt the key}
    provider: ${15:# A dict object containing connection details}
    renegotiation: ${16:# Enables or disables SSL renegotiation}
    secure_renegotiation: ${17:# Specifies the method of secure renegotiations for SSL connections}
    server_certificate: ${18:# Specifies the way the system handles server certificates}
    server_name: ${19:# Specifies the fully qualified DNS hostname of the server used in Server Name Indication communications}
    sni_default: ${20:# Indicates the system uses this profile as the default SSL profile when there is no match to the server name, or when the client provides no SNI extension support}
    sni_require: ${21:# Requires the network peers also provide SNI support}
    state: ${22:# When C(present), ensures the profile exists}
    update_password: ${23:# C(always) allows users to update passwords if they choose to do so}
  tags:
    - f5networks
    - f5_modules
    - bigip_profile_server_ssl]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_profile_server_ssl</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages server SSL profiles on a BIG-IP</description>
</snippet>