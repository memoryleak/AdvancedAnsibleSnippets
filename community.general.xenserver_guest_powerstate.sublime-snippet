<snippet>
  <content><![CDATA[- name: ${1:Manages power states of virtual machines running on Citrix Hypervisor/XenServer host or pool}
  community.general.xenserver_guest_powerstate:
    hostname: ${2:# The hostname or IP address of the XenServer host or XenServer pool master}
    name: ${3:# Name of the VM to manage}
    password: ${4:# The password to use for connecting to XenServer}
    state: ${5:# Specify the state VM should be in}
    state_change_timeout: ${6:# By default, module will wait indefinitely for VM to change state or acquire an IP address if O(wait_for_ip_address=true)}
    username: ${7:# The username to use for connecting to XenServer}
    uuid: ${8:# UUID of the VM to manage if known}
    validate_certs: ${9:# Allows connection when SSL certificates are not valid}
    wait_for_ip_address: ${10:# Wait until XenServer detects an IP address for the VM}
  tags:
    - community
    - general
    - xenserver_guest_powerstate]]></content>
  <tabTrigger>community.general.xenserver_guest_powerstate</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages power states of virtual machines running on Citrix Hypervisor/XenServer host or pool</description>
</snippet>