<snippet>
  <content><![CDATA[- name: ${1:Configure FlashBlade Directory Service}
  purestorage.flashblade.purefb_ds:
    api_token: ${2:# FlashBlade API token for admin privileged user}
    base_dn: ${3:# Sets the base of the Distinguished Name (DN) of the directory service groups}
    bind_password: ${4:# Sets the password of the bind_user user name account}
    bind_user: ${5:# Sets the user name that can be used to bind to and query the directory}
    disable_warnings: ${6:# Disable insecure certificate warnings}
    dstype: ${7:# The type of directory service to work on}
    enable: ${8:# Whether to enable or disable directory service support}
    fb_url: ${9:# FlashBlade management IP address or Hostname}
    force_bind_password: ${10:# Will force the bind password to be reset even if the bind user password is unchanged}
    join_ou: ${11:# The optional organizational unit (OU) where the machine account for the directory service will be created}
    nis_domain: ${12:# The NIS domain to search}
    nis_servers: ${13:# A list of up to 30 IP addresses or FQDNs for NIS servers}
    state: ${14:# Create or delete directory service configuration}
    uri: ${15:# A list of up to 30 URIs of the directory servers}
  tags:
    - purestorage
    - flashblade
    - purefb_ds]]></content>
  <tabTrigger>purestorage.flashblade.purefb_ds</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configure FlashBlade Directory Service</description>
</snippet>