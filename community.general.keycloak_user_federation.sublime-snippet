<snippet>
  <content><![CDATA[- name: ${1:Allows administration of Keycloak user federations via Keycloak API}
  community.general.keycloak_user_federation:
    state: ${2:# State of the user federation}
    realm: ${3:# The Keycloak realm under which this user federation resides}
    id: ${4:# The unique ID for this user federation}
    name: ${5:# Display name of provider when linked in admin console}
    provider_id: ${6:# Provider for this user federation}
    provider_type: ${7:# Component type for user federation (only supported value is C(org}
    parent_id: ${8:# Unique ID for the parent of this user federation}
    config: ${9:# Dict specifying the configuration options for the provider; the contents differ depending on the value of I(provider_id)}
    mappers: ${10:# A list of dicts defining mappers associated with this Identity Provider}
    auth_keycloak_url: ${11:# URL to the Keycloak instance}
    auth_client_id: ${12:# OpenID Connect I(client_id) to authenticate to the API with}
    auth_realm: ${13:# Keycloak realm name to authenticate to for API access}
    auth_client_secret: ${14:# Client Secret to use in conjunction with I(auth_client_id) (if required)}
    auth_username: ${15:# Username to authenticate for API access with}
    auth_password: ${16:# Password to authenticate for API access with}
    token: ${17:# Authentication token for Keycloak API}
    validate_certs: ${18:# Verify TLS certificates (do not disable this in production)}
    connection_timeout: ${19:# Controls the HTTP connections timeout period (in seconds) to Keycloak API}
    http_agent: ${20:# Configures the HTTP User-Agent header}
  tags:
    - community
    - general
    - keycloak_user_federation]]></content>
  <tabTrigger>community.general.keycloak_user_federation</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Allows administration of Keycloak user federations via Keycloak API</description>
</snippet>