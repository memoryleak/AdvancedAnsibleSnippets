<snippet>
  <content><![CDATA[- name: ${1:Resource module for Reserve Ip Subpool Update}
  cisco.dnac.reserve_ip_subpool_update:
    id: ${2:# Id query parameter}
    ipv4DhcpServers: ${3:# IPv4 input for dhcp server ip example 1}
    ipv4DnsServers: ${4:# IPv4 input for dns server ip example 4}
    ipv4GateWay: ${5:# Ipv4 Gate Way}
    ipv6AddressSpace: ${6:# If the value is false only ipv4 input are required, otherwise both ipv6 and ipv4 are required}
    ipv6DhcpServers: ${7:# IPv6 format dhcp server as input example 2001 db8 1234}
    ipv6DnsServers: ${8:# IPv6 format dns server input example 2001 db8 1234}
    ipv6GateWay: ${9:# Gateway ip address details, example 2001 db8 85a3 0 100 1}
    ipv6GlobalPool: ${10:# IP v6 Global pool address with cidr this is required when Ipv6AddressSpace value is true, example 2001 db8 85a3 /64}
    ipv6Prefix: ${11:# IPv6 prefix value is true, the ip6 prefix length input field is enabled , if it is false ipv6 total Host input is enable}
    ipv6PrefixLength: ${12:# IPv6 prefix length is required when the ipv6prefix value is true}
    ipv6Subnet: ${13:# IPv6 Subnet address, example 2001 db8 85a3 0 100}
    ipv6TotalHost: ${14:# IPv6 total host is required when ipv6prefix value is false}
    name: ${15:# Name of the reserve ip sub pool}
    siteId: ${16:# SiteId path parameter}
    slaacSupport: ${17:# Slaac Support}
    dnac_host: ${18:# The Cisco DNA Center hostname}
    dnac_port: ${19:# The Cisco DNA Center port}
    dnac_username: ${20:# The Cisco DNA Center username to authenticate}
    dnac_password: ${21:# The Cisco DNA Center password to authenticate}
    dnac_verify: ${22:# Flag to enable or disable SSL certificate verification}
    dnac_version: ${23:# Informs the SDK which version of Cisco DNA Center to use}
    dnac_debug: ${24:# Flag for Cisco DNA Center SDK to enable debugging}
    validate_response_schema: ${25:# Flag for Cisco DNA Center SDK to enable the validation of request bodies against a JSON schema}
  tags:
    - cisco
    - dnac
    - reserve_ip_subpool_update]]></content>
  <tabTrigger>cisco.dnac.reserve_ip_subpool_update</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for Reserve Ip Subpool Update</description>
</snippet>