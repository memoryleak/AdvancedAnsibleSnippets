<snippet>
  <content><![CDATA[- name: ${1:Module to configure VRF definitions}
  cisco.ios.ios_vrf:
    address_family: ${2:# The list of address families with MDT parameters to be configured on the remote IOS device}
    associated_interfaces: ${3:# This is a intent option and checks the operational state of the for given vrf C(name) for associated interfaces}
    delay: ${4:# Time in seconds to wait before checking for the operational state on remote device}
    description: ${5:# Provides a short description of the VRF definition in the current active configuration}
    interfaces: ${6:# Identifies the set of interfaces that should be configured in the VRF}
    name: ${7:# The name of the VRF definition to be managed on the remote IOS device}
    purge: ${8:# Instructs the module to consider the VRF definition absolute}
    rd: ${9:# The router-distinguisher value uniquely identifies the VRF to routing processes on the remote IOS system}
    route_both: ${10:# Adds an export and import list of extended route target communities to the VRF}
    route_both_ipv4: ${11:# Adds an export and import list of extended route target communities in address-family configuration submode to the VRF}
    route_both_ipv6: ${12:# Adds an export and import list of extended route target communities in address-family configuration submode to the VRF}
    route_export: ${13:# Adds an export list of extended route target communities to the VRF}
    route_export_ipv4: ${14:# Adds an export list of extended route target communities in address-family configuration submode to the VRF}
    route_export_ipv6: ${15:# Adds an export list of extended route target communities in address-family configuration submode to the VRF}
    route_import: ${16:# Adds an import list of extended route target communities to the VRF}
    route_import_ipv4: ${17:# Adds an import list of extended route target communities in address-family configuration submode to the VRF}
    route_import_ipv6: ${18:# Adds an import list of extended route target communities in address-family configuration submode to the VRF}
    state: ${19:# Configures the state of the VRF definition as it relates to the device operational configuration}
    vrfs: ${20:# The set of VRF definition objects to be configured on the remote IOS device}
  tags:
    - cisco
    - ios
    - ios_vrf]]></content>
  <tabTrigger>cisco.ios.ios_vrf</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Module to configure VRF definitions</description>
</snippet>