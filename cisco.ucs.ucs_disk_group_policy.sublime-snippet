<snippet>
  <content><![CDATA[- name: ${1:Configures disk group policies on Cisco UCS Manager}
  cisco.ucs.ucs_disk_group_policy:
    configuration_mode: ${2:# Disk group configuration mode}
    description: ${3:# The user-defined description of the storage profile}
    drive_type: ${4:# Specify the drive type to use in the drive group}
    hostname: ${5:# IP address or hostname of Cisco UCS Manager}
    manual_disks: ${6:# List of manually configured disks}
    min_drive_size: ${7:# Specify the minimum drive size or unspecified to allow all drive sizes}
    name: ${8:# The name of the disk group policy}
    num_ded_hot_spares: ${9:# Specify the number of hot spares for the disk group}
    num_drives: ${10:# Specify the number of drives for the disk group}
    num_glob_hot_spares: ${11:# Specify the number of global hot spares for the disk group}
    org_dn: ${12:# The distinguished name (dn) of the organization where the resource is assigned}
    password: ${13:# Password for Cisco UCS Manager authentication}
    port: ${14:# Port number to be used during connection (by default uses 443 for https and 80 for http connection)}
    proxy: ${15:# If use_proxy is no, specfies proxy to be used for connection}
    raid_level: ${16:# The RAID level for the disk group policy}
    state: ${17:# Desired state of the disk group policy}
    use_proxy: ${18:# If C(no), will not use the proxy as defined by system environment variable}
    use_remaining_disks: ${19:# Specifies whether you can use all the remaining disks in the disk group or not}
    use_ssl: ${20:# If C(no), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${21:# Username for Cisco UCS Manager authentication}
    virtual_drive: ${22:# Configuration of virtual drive options}
  tags:
    - cisco
    - ucs
    - ucs_disk_group_policy]]></content>
  <tabTrigger>cisco.ucs.ucs_disk_group_policy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configures disk group policies on Cisco UCS Manager</description>
</snippet>