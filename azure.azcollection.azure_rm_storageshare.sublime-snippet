<snippet>
  <content><![CDATA[- name: ${1:Manage Azure storage file share}
  azure.azcollection.azure_rm_storageshare:
    access_tier: ${2:# The access tier determines the price and in some cases also the performance of a file share}
    account_name: ${3:# Name of the parent storage account for the storage file share}
    ad_user: ${4:# Active Directory username}
    adfs_authority_url: ${5:# Azure AD authority url}
    api_profile: ${6:# Selects an API profile to use when communicating with Azure services}
    auth_source: ${7:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${8:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${9:# Azure client ID}
    cloud_environment: ${10:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    disable_instance_discovery: ${11:# Determines whether or not instance discovery is performed when attempting to authenticate}
    enabled_protocols: ${12:# The authentication protocol that is used for the file share}
    log_mode: ${13:# Parent argument}
    log_path: ${14:# Parent argument}
    metadata: ${15:# A name-value pair to associate with the container as metadata}
    name: ${16:# Name of the storage file share to delete or create}
    password: ${17:# Active Directory user password}
    profile: ${18:# Security profile found in ~/}
    quota: ${19:# The maximum size of the file share, in gigabytes}
    resource_group: ${20:# Name of the resource group to use}
    root_squash: ${21:# The property is for NFS share only}
    secret: ${22:# Azure client secret}
    state: ${23:# State of the storage file share}
    subscription_id: ${24:# Your Azure subscription Id}
    tenant: ${25:# Azure tenant ID}
    thumbprint: ${26:# The thumbprint of the private key specified in I(x509_certificate_path)}
    x509_certificate_path: ${27:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_storageshare]]></content>
  <tabTrigger>azure.azcollection.azure_rm_storageshare</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure storage file share</description>
</snippet>