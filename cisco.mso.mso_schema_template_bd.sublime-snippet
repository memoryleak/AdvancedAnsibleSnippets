<snippet>
  <content><![CDATA[- name: ${1:Manage Bridge Domains (BDs) in schema templates}
  cisco.mso.mso_schema_template_bd:
    arp_flooding: ${2:# ARP Flooding}
    bd: ${3:# The name of the BD to manage}
    description: ${4:# The description of BD is supported on versions of MSO that are 3}
    dhcp_policies: ${5:# A list DHCP Policies to be assciated with the BD}
    dhcp_policy: ${6:# The DHCP Policy}
    display_name: ${7:# The name as displayed on the MSO web interface}
    host: ${8:# IP Address or hostname of the ACI Multi Site Orchestrator host}
    intersite_bum_traffic: ${9:# Whether to allow intersite BUM traffic}
    ipv6_unknown_multicast_flooding: ${10:# IPv6 Unknown Multicast Flooding can either be Flood or Optimized Flooding}
    layer2_stretch: ${11:# Whether to enable L2 stretch}
    layer2_unknown_unicast: ${12:# Layer2 unknown unicast}
    layer3_multicast: ${13:# Whether to enable L3 multicast}
    login_domain: ${14:# The login domain name to use for authentication}
    multi_destination_flooding: ${15:# Multi-Destination Flooding can either be Flood in BD, Drop or Flood in Encapsulation}
    optimize_wan_bandwidth: ${16:# Whether to optimize WAN bandwidth}
    output_level: ${17:# Influence the output of this MSO module}
    password: ${18:# The password to use for authentication}
    port: ${19:# Port number to be used for the REST connection}
    schema: ${20:# The name of the schema}
    state: ${21:# Use C(present) or C(absent) for adding or removing}
    subnets: ${22:# The subnets associated to this BD}
    template: ${23:# The name of the template}
    timeout: ${24:# The socket level timeout in seconds}
    unicast_routing: ${25:# Unicast Routing}
    unknown_multicast_flooding: ${26:# Unknown Multicast Flooding can either be Flood or Optimized Flooding}
    use_proxy: ${27:# If C(false), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_ssl: ${28:# If C(false), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${29:# The username to use for authentication}
    validate_certs: ${30:# If C(false), SSL certificates will not be validated}
    virtual_mac_address: ${31:# Virtual MAC Address}
    vrf: ${32:# The VRF associated to this BD}
  tags:
    - cisco
    - mso
    - mso_schema_template_bd]]></content>
  <tabTrigger>cisco.mso.mso_schema_template_bd</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Bridge Domains (BDs) in schema templates</description>
</snippet>