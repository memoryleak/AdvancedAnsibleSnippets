<snippet>
  <content><![CDATA[- name: ${1:Manage Elasticsearch plugins}
  community.general.elasticsearch_plugin:
    force: ${2:# Force batch mode when installing plugins}
    name: ${3:# Name of the plugin to install}
    plugin_bin: ${4:# Location of the plugin binary}
    plugin_dir: ${5:# Your configured plugin directory specified in Elasticsearch}
    proxy_host: ${6:# Proxy host to use during plugin installation}
    proxy_port: ${7:# Proxy port to use during plugin installation}
    src: ${8:# Optionally set the source location to retrieve the plugin from}
    state: ${9:# Desired state of a plugin}
    timeout: ${10:# Timeout setting: 30s, 1m, 1h}
    url: ${11:# Set exact URL to download the plugin from (Only works for ES 1}
    version: ${12:# Version of the plugin to be installed}
  tags:
    - community
    - general
    - elasticsearch_plugin]]></content>
  <tabTrigger>community.general.elasticsearch_plugin</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Elasticsearch plugins</description>
</snippet>