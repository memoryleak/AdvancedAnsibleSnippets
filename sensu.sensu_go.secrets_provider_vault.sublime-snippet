<snippet>
  <content><![CDATA[- name: ${1:Manage Sensu VaultProvider secrets providers}
  sensu.sensu_go.secrets_provider_vault:
    address: ${2:# Address of the Vault server}
    token: ${3:# Authentication token to use with Vault}
    version: ${4:# Version of the Vault key/value store}
    timeout: ${5:# Timeout (in seconds) for connection to Vault server}
    max_retries: ${6:# Maximum number of times to retry failed connections to Vault server}
    rate_limit: ${7:# Maximum number of secrets requests for per second}
    burst_limit: ${8:# Maximum allowed number of secrets requests in a rate interval}
    tls: ${9:# TLS configuration for establishing connection with Vault server}
    auth: ${10:# Authentication parameters}
    name: ${11:# The Sensu resource's name}
    state: ${12:# Target state of the Sensu object}
  tags:
    - sensu
    - sensu_go
    - secrets_provider_vault]]></content>
  <tabTrigger>sensu.sensu_go.secrets_provider_vault</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Sensu VaultProvider secrets providers</description>
</snippet>