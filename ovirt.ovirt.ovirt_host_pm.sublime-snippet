<snippet>
  <content><![CDATA[- name: ${1:Module to manage power management of hosts in oVirt/RHV}
  ovirt.ovirt.ovirt_host_pm:
    address: ${2:# Address of the power management interface}
    auth: ${3:# Dictionary with values needed to create HTTP/HTTPS connection to oVirt:}
    encrypt_options: ${4:# If I(true) options will be encrypted when send to agent}
    fetch_nested: ${5:# If I(True) the module will fetch additional data from the API}
    name: ${6:# Name of the host to manage}
    nested_attributes: ${7:# Specifies list of the attributes which should be fetched from the API}
    options: ${8:# Dictionary of additional fence agent options (including Power Management slot)}
    order: ${9:# Integer value specifying, by default it's added at the end}
    password: ${10:# Password of the user specified in C(username) parameter}
    poll_interval: ${11:# Number of the seconds the module waits until another poll request on entity status is sent}
    port: ${12:# Power management interface port}
    state: ${13:# Should the host be present/absent}
    timeout: ${14:# The amount of time in seconds the module should wait for the instance to get into desired state}
    type: ${15:# Type of the power management}
    username: ${16:# Username to be used to connect to power management interface}
    wait: ${17:# C(yes) if the module should wait for the entity to get into desired state}
  tags:
    - ovirt
    - ovirt
    - ovirt_host_pm]]></content>
  <tabTrigger>ovirt.ovirt.ovirt_host_pm</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Module to manage power management of hosts in oVirt/RHV</description>
</snippet>