<snippet>
  <content><![CDATA[- name: ${1:Add, remove or update SSH key}
  community.hrobot.ssh_key:
    fingerprint: ${2:# The MD5 fingerprint of the public SSH key to remove}
    hetzner_password: ${3:# The password for the Robot webservice user}
    hetzner_user: ${4:# The username for the Robot webservice user}
    name: ${5:# The public key's name}
    public_key: ${6:# The public key data in OpenSSH format}
    state: ${7:# Whether to make sure a public SSH key is present or absent}
  tags:
    - community
    - hrobot
    - ssh_key]]></content>
  <tabTrigger>community.hrobot.ssh_key</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Add, remove or update SSH key</description>
</snippet>