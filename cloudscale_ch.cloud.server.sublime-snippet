<snippet>
  <content><![CDATA[- name: ${1:Manages servers on the cloudscale}
  cloudscale_ch.cloud.server:
    api_timeout: ${2:# Timeout in seconds for calls to the cloudscale}
    api_token: ${3:# cloudscale}
    api_url: ${4:# cloudscale}
    bulk_volume_size_gb: ${5:# Size of the bulk storage volume in GB}
    flavor: ${6:# Flavor of the server}
    force: ${7:# Allow to stop the running server for updating if necessary}
    image: ${8:# Image used to create the server}
    interfaces: ${9:# List of network interface objects specifying the interfaces to be attached to the server}
    name: ${10:# Name of the Server}
    password: ${11:# Password for the server}
    server_groups: ${12:# List of UUID or names of server groups}
    ssh_keys: ${13:# List of SSH public keys}
    state: ${14:# State of the server}
    tags: ${15:# Tags assosiated with the servers}
    use_ipv6: ${16:# Enable IPv6 on the public network interface}
    use_private_network: ${17:# Attach a private network interface to the server}
    use_public_network: ${18:# Attach a public network interface to the server}
    user_data: ${19:# Cloud-init configuration (cloud-config) data to use for the server}
    uuid: ${20:# UUID of the server}
    volume_size_gb: ${21:# Size of the root volume in GB}
    zone: ${22:# Zone in which the server resides (e}
  tags:
    - cloudscale_ch
    - cloud
    - server]]></content>
  <tabTrigger>cloudscale_ch.cloud.server</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages servers on the cloudscale</description>
</snippet>