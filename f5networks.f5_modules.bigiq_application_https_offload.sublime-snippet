<snippet>
  <content><![CDATA[- name: ${1:Manages BIG-IQ HTTPS offload applications}
  f5networks.f5_modules.bigiq_application_https_offload:
    add_analytics: ${2:# Collects statistics of the BIG-IP to which the application is deployed}
    client_ssl_profile: ${3:# Specifies the SSL profile for managing client-side SSL traffic}
    description: ${4:# Description of the application}
    inbound_virtual: ${5:# Settings to configure the virtual which receives the inbound connection}
    name: ${6:# Name of the new application}
    provider: ${7:# A dict object containing connection details}
    redirect_virtual: ${8:# Settings to configure the virtual which receives the connection to be redirected}
    servers: ${9:# A list of servers on which the application is hosted}
    service_environment: ${10:# Specifies the name of service environment or the hostname of the BIG-IP to which the application will be deployed}
    state: ${11:# The state of the resource on the system}
    wait: ${12:# If the module should wait for the application to be created, deleted or updated}
  tags:
    - f5networks
    - f5_modules
    - bigiq_application_https_offload]]></content>
  <tabTrigger>f5networks.f5_modules.bigiq_application_https_offload</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages BIG-IQ HTTPS offload applications</description>
</snippet>