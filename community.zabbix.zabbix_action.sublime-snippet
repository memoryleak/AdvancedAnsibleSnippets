<snippet>
  <content><![CDATA[- name: ${1:Create/Delete/Update Zabbix actions}
  community.zabbix.zabbix_action:
    acknowledge_operations: ${2:# List of acknowledge operations}
    conditions: ${3:# List of conditions to use for filtering results}
    esc_period: ${4:# Default operation step duration}
    eval_type: ${5:# Filter condition evaluation method}
    event_source: ${6:# Type of events that the action will handle}
    formula: ${7:# User-defined expression to be used for evaluating conditions with a custom expression}
    http_login_password: ${8:# Basic Auth password}
    http_login_user: ${9:# Basic Auth login}
    name: ${10:# Name of the action}
    notify_if_canceled: ${11:# Weather to notify when escalation is canceled}
    operations: ${12:# List of action operations}
    pause_in_maintenance: ${13:# Whether to pause escalation during maintenance periods or not}
    pause_symptoms: ${14:# Whether to pause escalation if event is a symptom event}
    recovery_operations: ${15:# List of recovery operations}
    state: ${16:# State of the action}
    status: ${17:# Status of the action}
  tags:
    - community
    - zabbix
    - zabbix_action]]></content>
  <tabTrigger>community.zabbix.zabbix_action</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create/Delete/Update Zabbix actions</description>
</snippet>