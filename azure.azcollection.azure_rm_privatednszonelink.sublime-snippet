<snippet>
  <content><![CDATA[- name: ${1:Create, delete and update Virtual network link for Private DNS zone}
  azure.azcollection.azure_rm_privatednszonelink:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${7:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${8:# Azure client ID}
    cloud_environment: ${9:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    log_mode: ${10:# Parent argument}
    log_path: ${11:# Parent argument}
    name: ${12:# The name of the virtual network link}
    password: ${13:# Active Directory user password}
    profile: ${14:# Security profile found in ~/}
    registration_enabled: ${15:# Is auto-registration of virtual machine records in the virtual network in the Private DNS zone enabled}
    resource_group: ${16:# Name of resource group}
    secret: ${17:# Azure client secret}
    state: ${18:# Assert the state of the virtual network link}
    subscription_id: ${19:# Your Azure subscription Id}
    tags: ${20:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${21:# Azure tenant ID}
    thumbprint: ${22:# The thumbprint of the private key specified in I(x509_certificate_path)}
    virtual_network: ${23:# The reference of the virtual network}
    x509_certificate_path: ${24:# Path to the X509 certificate used to create the service principal in PEM format}
    zone_name: ${25:# The name of the Private DNS zone}
  tags:
    - azure
    - azcollection
    - azure_rm_privatednszonelink]]></content>
  <tabTrigger>azure.azcollection.azure_rm_privatednszonelink</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create, delete and update Virtual network link for Private DNS zone</description>
</snippet>