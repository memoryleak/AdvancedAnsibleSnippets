<snippet>
  <content><![CDATA[- name: ${1:Read a value from CloudFormation Exports}
  community.aws.cloudformation_exports_info:
    access_key: ${2:# AWS access key ID}
    secret_key: ${3:# AWS secret access key}
    session_token: ${4:# AWS STS session token for use with temporary credentials}
    profile: ${5:# A named AWS profile to use for authentication}
    endpoint_url: ${6:# URL to connect to instead of the default AWS endpoints}
    aws_ca_bundle: ${7:# The location of a CA Bundle to use when validating SSL certificates}
    validate_certs: ${8:# When set to C(false), SSL certificates will not be validated for communication with the AWS APIs}
    aws_config: ${9:# A dictionary to modify the botocore configuration}
    debug_botocore_endpoint_logs: ${10:# Use a C(botocore}
    region: ${11:# The AWS region to use}
  tags:
    - community
    - aws
    - cloudformation_exports_info]]></content>
  <tabTrigger>community.aws.cloudformation_exports_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Read a value from CloudFormation Exports</description>
</snippet>