<snippet>
  <content><![CDATA[- name: ${1:Parse cli output or text using a variety of parsers}
  ansible.utils.cli_parse:
    command: ${2:# The command to run on the host}
    parser: ${3:# Parser specific parameters}
    set_fact: ${4:# Set the resulting parsed data as a fact}
    text: ${5:# Text to be parsed}
  tags:
    - cli_parse]]></content>
  <tabTrigger>ansible.utils.cli_parse</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Parse cli output or text using a variety of parsers</description>
</snippet>