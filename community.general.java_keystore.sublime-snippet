<snippet>
  <content><![CDATA[- name: ${1:Create a Java keystore in JKS format}
  community.general.java_keystore:
    attributes: ${2:# The attributes the resulting filesystem object should have}
    certificate: ${3:# Content of the certificate used to create the keystore}
    certificate_path: ${4:# Location of the certificate used to create the keystore}
    dest: ${5:# Absolute path of the generated keystore}
    force: ${6:# Keystore is created even if it already exists}
    group: ${7:# Name of the group that should own jks file}
    keystore_type: ${8:# Type of the Java keystore}
    mode: ${9:# Mode the file should be}
    name: ${10:# Name of the certificate in the keystore}
    owner: ${11:# Name of the user that should own jks file}
    password: ${12:# Password that should be used to secure the keystore}
    private_key: ${13:# Content of the private key used to create the keystore}
    private_key_passphrase: ${14:# Passphrase used to read the private key, if required}
    private_key_path: ${15:# Location of the private key used to create the keystore}
    selevel: ${16:# The level part of the SELinux filesystem object context}
    serole: ${17:# The role part of the SELinux filesystem object context}
    setype: ${18:# The type part of the SELinux filesystem object context}
    seuser: ${19:# The user part of the SELinux filesystem object context}
    ssl_backend: ${20:# Backend for loading private keys and certificates}
    unsafe_writes: ${21:# Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object}
  tags:
    - community
    - general
    - java_keystore]]></content>
  <tabTrigger>community.general.java_keystore</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create a Java keystore in JKS format</description>
</snippet>