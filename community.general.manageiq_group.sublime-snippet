<snippet>
  <content><![CDATA[- name: ${1:Management of groups in ManageIQ}
  community.general.manageiq_group:
    belongsto_filters: ${2:# A list of strings with a reference to the allowed host, cluster or folder}
    belongsto_filters_merge_mode: ${3:# In merge mode existing settings are merged with the supplied O(belongsto_filters)}
    description: ${4:# The group description}
    managed_filters: ${5:# The tag values per category}
    managed_filters_merge_mode: ${6:# In merge mode existing categories are kept or updated, new categories are added}
    manageiq_connection: ${7:# ManageIQ connection configuration information}
    role: ${8:# The the group role name}
    role_id: ${9:# The the group role id}
    state: ${10:# absent - group should not exist, present - group should be}
    tenant: ${11:# The tenant for the group identified by the tenant name}
    tenant_id: ${12:# The tenant for the group identified by the tenant id}
  tags:
    - community
    - general
    - manageiq_group]]></content>
  <tabTrigger>community.general.manageiq_group</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Management of groups in ManageIQ</description>
</snippet>