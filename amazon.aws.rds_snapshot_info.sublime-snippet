<snippet>
  <content><![CDATA[- name: ${1:obtain information about one or more RDS snapshots}
  amazon.aws.rds_snapshot_info:
    access_key: ${2:# AWS access key ID}
    aws_ca_bundle: ${3:# The location of a CA Bundle to use when validating SSL certificates}
    aws_config: ${4:# A dictionary to modify the botocore configuration}
    db_cluster_identifier: ${5:# RDS cluster name for which to find snapshots}
    db_cluster_snapshot_identifier: ${6:# Name of an RDS cluster snapshot}
    db_instance_identifier: ${7:# RDS instance name for which to find snapshots}
    db_snapshot_identifier: ${8:# Name of an RDS (unclustered) snapshot}
    debug_botocore_endpoint_logs: ${9:# Use a C(botocore}
    endpoint_url: ${10:# URL to connect to instead of the default AWS endpoints}
    profile: ${11:# A named AWS profile to use for authentication}
    region: ${12:# The AWS region to use}
    secret_key: ${13:# AWS secret access key}
    session_token: ${14:# AWS STS session token for use with temporary credentials}
    snapshot_type: ${15:# Type of snapshot to find}
    validate_certs: ${16:# When set to C(false), SSL certificates will not be validated for communication with the AWS APIs}
  tags:
    - amazon
    - aws
    - rds_snapshot_info]]></content>
  <tabTrigger>amazon.aws.rds_snapshot_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>obtain information about one or more RDS snapshots</description>
</snippet>