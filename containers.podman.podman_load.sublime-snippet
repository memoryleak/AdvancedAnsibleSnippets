<snippet>
  <content><![CDATA[- name: ${1:Load image from a tar file}
  containers.podman.podman_load:
    input: ${2:# Path to image file to load}
    executable: ${3:# Path to C(podman) executable if it is not in the C($PATH) on the machine running C(podman)}
  tags:
    - containers
    - podman
    - podman_load]]></content>
  <tabTrigger>containers.podman.podman_load</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Load image from a tar file</description>
</snippet>