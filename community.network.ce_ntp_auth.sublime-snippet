<snippet>
  <content><![CDATA[- name: ${1:Manages NTP authentication configuration on HUAWEI CloudEngine switches}
  community.network.ce_ntp_auth:
    key_id: ${2:# Authentication key identifier (numeric)}
    auth_pwd: ${3:# Plain text with length of 1 to 255, encrypted text with length of 20 to 392}
    auth_mode: ${4:# Specify authentication algorithm}
    auth_type: ${5:# Whether the given password is in cleartext or has been encrypted}
    trusted_key: ${6:# Whether the given key is required to be supplied by a time source for the device to synchronize to the time source}
    authentication: ${7:# Configure ntp authentication enable or unconfigure ntp authentication enable}
    state: ${8:# Manage the state of the resource}
  tags:
    - community
    - network
    - ce_ntp_auth]]></content>
  <tabTrigger>community.network.ce_ntp_auth</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages NTP authentication configuration on HUAWEI CloudEngine switches</description>
</snippet>