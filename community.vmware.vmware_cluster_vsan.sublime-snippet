<snippet>
  <content><![CDATA[- name: ${1:Manages virtual storage area network (vSAN) configuration on VMware vSphere clusters}
  community.vmware.vmware_cluster_vsan:
    cluster_name: ${2:# The name of the cluster to be managed}
    datacenter: ${3:# The name of the datacenter}
    enable: ${4:# Whether to enable vSAN}
    vsan_auto_claim_storage: ${5:# Whether the VSAN service is configured to automatically claim local storage on VSAN-enabled hosts in the cluster}
    advanced_options: ${6:# Advanced VSAN Options}
    hostname: ${7:# The hostname or IP address of the vSphere vCenter or ESXi server}
    username: ${8:# The username of the vSphere vCenter or ESXi server}
    password: ${9:# The password of the vSphere vCenter or ESXi server}
    validate_certs: ${10:# Allows connection when SSL certificates are not valid}
    port: ${11:# The port number of the vSphere vCenter or ESXi server}
    proxy_host: ${12:# Address of a proxy that will receive all HTTPS requests and relay them}
    proxy_port: ${13:# Port of the HTTP proxy that will receive all HTTPS requests and relay them}
  tags:
    - community
    - vmware
    - vmware_cluster_vsan]]></content>
  <tabTrigger>community.vmware.vmware_cluster_vsan</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages virtual storage area network (vSAN) configuration on VMware vSphere clusters</description>
</snippet>