<snippet>
  <content><![CDATA[- name: ${1:Configure MX layer 2 interfaces}
  cisco.meraki.meraki_mx_l2_interface:
    access_policy: ${2:# The name of the policy}
    allowed_vlans: ${3:# Comma-delimited list of the VLAN ID's allowed on the port, or 'all' to permit all VLAN's on the port}
    auth_key: ${4:# Authentication key provided by the dashboard}
    drop_untagged_traffic: ${5:# Trunk port can Drop all Untagged traffic}
    enabled: ${6:# Enabled state of port}
    host: ${7:# Hostname for Meraki dashboard}
    internal_error_retry_time: ${8:# Number of seconds to retry if server returns an internal server error}
    net_id: ${9:# ID number of a network}
    net_name: ${10:# Name of a network}
    number: ${11:# ID number of MX port}
    org_id: ${12:# ID of organization associated to a network}
    org_name: ${13:# Name of organization}
    output_format: ${14:# Instructs module whether response keys should be snake case (ex}
    output_level: ${15:# Set amount of debug output during module execution}
    port_type: ${16:# Type of port}
    rate_limit_retry_time: ${17:# Number of seconds to retry if rate limiter is triggered}
    state: ${18:# Modify or query an port}
    timeout: ${19:# Time to timeout for HTTP requests}
    use_https: ${20:# If C(no), it will use HTTP}
    use_proxy: ${21:# If C(no), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    validate_certs: ${22:# Whether to validate HTTP certificates}
    vlan: ${23:# Native VLAN when the port is in Trunk mode}
  tags:
    - cisco
    - meraki
    - meraki_mx_l2_interface]]></content>
  <tabTrigger>cisco.meraki.meraki_mx_l2_interface</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configure MX layer 2 interfaces</description>
</snippet>