<snippet>
  <content><![CDATA[- name: ${1:Manage the system attributes on Cisco NXOS devices}
  cisco.nxos.nxos_system:
    domain_lookup: ${2:# Enables or disables the DNS lookup feature in Cisco NXOS}
    domain_name: ${3:# Configures the default domain name suffix to be used when referencing this node by its FQDN}
    domain_search: ${4:# Configures a list of domain name suffixes to search when performing DNS name resolution}
    hostname: ${5:# Configure the device hostname parameter}
    name_servers: ${6:# List of DNS name servers by IP address to use to perform name resolution lookups}
    state: ${7:# State of the configuration values in the device's current active configuration}
    system_mtu: ${8:# Specifies the mtu, must be an integer or keyword 'default'}
  tags:
    - cisco
    - nxos
    - nxos_system]]></content>
  <tabTrigger>cisco.nxos.nxos_system</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage the system attributes on Cisco NXOS devices</description>
</snippet>