<snippet>
  <content><![CDATA[- name: ${1:Gather information about ELB target groups in AWS}
  community.aws.elb_target_group_info:
    access_key: ${2:# AWS access key ID}
    aws_ca_bundle: ${3:# The location of a CA Bundle to use when validating SSL certificates}
    aws_config: ${4:# A dictionary to modify the botocore configuration}
    collect_targets_health: ${5:# When set to C(True), output contains targets health description}
    debug_botocore_endpoint_logs: ${6:# Use a C(botocore}
    endpoint_url: ${7:# URL to connect to instead of the default AWS endpoints}
    load_balancer_arn: ${8:# The Amazon Resource Name (ARN) of the load balancer}
    names: ${9:# The names of the target groups}
    profile: ${10:# A named AWS profile to use for authentication}
    region: ${11:# The AWS region to use}
    secret_key: ${12:# AWS secret access key}
    session_token: ${13:# AWS STS session token for use with temporary credentials}
    target_group_arns: ${14:# The Amazon Resource Names (ARN) of the target groups}
    validate_certs: ${15:# When set to C(false), SSL certificates will not be validated for communication with the AWS APIs}
  tags:
    - community
    - aws
    - elb_target_group_info]]></content>
  <tabTrigger>community.aws.elb_target_group_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Gather information about ELB target groups in AWS</description>
</snippet>