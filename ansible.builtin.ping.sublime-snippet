<snippet>
  <content><![CDATA[- name: ${1:Try to connect to host, verify a usable python and return V(pong) on success}
  ansible.builtin.ping:
    data: ${2:# Data to return for the RV(ping) return value}
  tags:
    - ping]]></content>
  <tabTrigger>ansible.builtin.ping</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Try to connect to host, verify a usable python and return V(pong) on success</description>
</snippet>