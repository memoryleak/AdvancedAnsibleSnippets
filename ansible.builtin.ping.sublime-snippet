<snippet>
  <content><![CDATA[- name: ${1:Try to connect to host, verify a usable python and return C(pong) on success}
  ansible.builtin.ping:
    data: ${2:# Data to return for the C(ping) return value}
  tags:
    - ping]]></content>
  <tabTrigger>ansible.builtin.ping</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Try to connect to host, verify a usable python and return C(pong) on success</description>
</snippet>