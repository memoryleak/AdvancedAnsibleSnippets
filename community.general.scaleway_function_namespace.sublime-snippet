<snippet>
  <content><![CDATA[- name: ${1:Scaleway Function namespace management}
  community.general.scaleway_function_namespace:
    api_timeout: ${2:# HTTP timeout to Scaleway API in seconds}
    api_token: ${3:# Scaleway OAuth token}
    api_url: ${4:# Scaleway API URL}
    description: ${5:# Description of the function namespace}
    environment_variables: ${6:# Environment variables of the function namespace}
    name: ${7:# Name of the function namespace}
    project_id: ${8:# Project identifier}
    query_parameters: ${9:# List of parameters passed to the query string}
    region: ${10:# Scaleway region to use (for example V(fr-par))}
    secret_environment_variables: ${11:# Secret environment variables of the function namespace}
    state: ${12:# Indicate desired state of the function namespace}
    validate_certs: ${13:# Validate SSL certs of the Scaleway API}
    wait: ${14:# Wait for the resource to reach its desired state before returning}
    wait_sleep_time: ${15:# Time to wait before every attempt to check the state of the resource}
    wait_timeout: ${16:# Time to wait for the resource to reach the expected state}
  tags:
    - community
    - general
    - scaleway_function_namespace]]></content>
  <tabTrigger>community.general.scaleway_function_namespace</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Scaleway Function namespace management</description>
</snippet>