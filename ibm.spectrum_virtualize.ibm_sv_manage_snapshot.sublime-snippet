<snippet>
  <content><![CDATA[- name: ${1:This module manages snapshots (PiT image of a volume) on IBM Spectrum Virtualize family storage systems}
  ibm.spectrum_virtualize.ibm_sv_manage_snapshot:
    clustername: ${2:# The hostname or management IP of the Spectrum Virtualize storage system}
    domain: ${3:# Domain for the Spectrum Virtualize storage system}
    ignorelegacy: ${4:# Specifies the addition of the volume snapshots although there are already legacy FlashCopy mappings using the volume as a source}
    log_path: ${5:# Path of debug log file}
    name: ${6:# Specifies the name of a snapshot}
    old_name: ${7:# Specifies the old name of a snapshot}
    ownershipgroup: ${8:# Specifies the name of the ownershipgroup}
    password: ${9:# REST API password for the Spectrum Virtualize storage system}
    retentiondays: ${10:# Specifies the retention period in days}
    safeguarded: ${11:# Flag to create a safeguarded snapshot}
    snapshot_pool: ${12:# Specifies the name of child pool within which the snapshot is being created}
    src_volume_names: ${13:# Specifies the name of the volumes for which the snapshots are to be created}
    src_volumegroup_name: ${14:# Specifies the name of the source volume group for which the snapshot is being created}
    state: ${15:# Creates, updates (C(present)) or deletes (C(absent)) a snapshot}
    token: ${16:# The authentication token to verify a user on the Spectrum Virtualize storage system}
    username: ${17:# REST API username for the Spectrum Virtualize storage system}
    validate_certs: ${18:# Validates certification}
  tags:
    - ibm
    - spectrum_virtualize
    - ibm_sv_manage_snapshot]]></content>
  <tabTrigger>ibm.spectrum_virtualize.ibm_sv_manage_snapshot</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>This module manages snapshots (PiT image of a volume) on IBM Spectrum Virtualize family storage systems</description>
</snippet>