<snippet>
  <content><![CDATA[- name: ${1:Managed bastion host resource}
  azure.azcollection.azure_rm_bastionhost:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${7:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${8:# Azure client ID}
    cloud_environment: ${9:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    disable_copy_paste: ${10:# Enable or Disable Copy or Paste feature of the Bastion Host resource}
    disable_instance_discovery: ${11:# Determines whether or not instance discovery is performed when attempting to authenticate}
    enable_file_copy: ${12:# Enable or Disable File Copy feature of the Bastion Host resource}
    enable_ip_connect: ${13:# Enable or Disable IP Connect feature of the Bastion Host resource}
    enable_shareable_link: ${14:# Enable or Disable Shareable Link of the Bastion Host resource}
    enable_tunneling: ${15:# Enable or Disable Tunneling feature of the Bastion Host resource}
    ip_configurations: ${16:# An array of bastion host IP configurations}
    location: ${17:# The resource location}
    log_mode: ${18:# Parent argument}
    log_path: ${19:# Parent argument}
    name: ${20:# The name of the bastion host}
    password: ${21:# Active Directory user password}
    profile: ${22:# Security profile found in ~/}
    resource_group: ${23:# The name of the resource group}
    scale_units: ${24:# The scale units for the Bastion Host resource}
    secret: ${25:# Azure client secret}
    sku: ${26:# The sku of this Bastion Host}
    state: ${27:# Assert the state of the pirvate link service}
    subscription_id: ${28:# Your Azure subscription Id}
    tags: ${29:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${30:# Azure tenant ID}
    thumbprint: ${31:# The thumbprint of the private key specified in I(x509_certificate_path)}
    x509_certificate_path: ${32:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_bastionhost]]></content>
  <tabTrigger>azure.azcollection.azure_rm_bastionhost</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Managed bastion host resource</description>
</snippet>