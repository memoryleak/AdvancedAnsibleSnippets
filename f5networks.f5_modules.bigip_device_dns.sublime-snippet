<snippet>
  <content><![CDATA[- name: ${1:Manage DNS settings on a BIG-IP}
  f5networks.f5_modules.bigip_device_dns:
    cache: ${2:# Specifies whether the system caches DNS lookups or performs the operation each time a lookup is needed}
    name_servers: ${3:# A list of name servers the system uses to validate DNS lookups}
    search: ${4:# A list of domains the system searches for local domain lookups, to resolve local host names}
    ip_version: ${5:# Specifies whether the DNS specifies IP addresses using IPv4 or IPv6}
    state: ${6:# The state of the variable on the system}
    provider: ${7:# A dict object containing connection details}
  tags:
    - f5networks
    - f5_modules
    - bigip_device_dns]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_device_dns</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage DNS settings on a BIG-IP</description>
</snippet>