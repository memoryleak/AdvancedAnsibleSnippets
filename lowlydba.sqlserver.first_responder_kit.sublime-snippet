<snippet>
  <content><![CDATA[- name: ${1:Install/update the First Responder Kit scripts}
  lowlydba.sqlserver.first_responder_kit:
    branch: ${2:# Specifies an alternate branch of the First Responder Kit to install}
    database: ${3:# Name of the target database}
    force: ${4:# If this switch is enabled, the FRK will be downloaded from the internet even if previously cached}
    local_file: ${5:# Specifies the path to a local file to install FRK from}
    only_script: ${6:# Specifies the name(s) of the script(s) to run for installation}
    sql_instance: ${7:# The SQL Server instance to modify}
    sql_password: ${8:# Password for SQL Authentication}
    sql_username: ${9:# Username for SQL Authentication}
  tags:
    - lowlydba
    - sqlserver
    - first_responder_kit]]></content>
  <tabTrigger>lowlydba.sqlserver.first_responder_kit</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Install/update the First Responder Kit scripts</description>
</snippet>