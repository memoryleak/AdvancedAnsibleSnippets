<snippet>
  <content><![CDATA[- name: ${1:Manages NetStream template configuration on HUAWEI CloudEngine switches}
  community.network.ce_netstream_template:
    state: ${2:# Specify desired state of the resource}
    type: ${3:# Configure the type of netstream record}
    record_name: ${4:# Configure the name of netstream record}
    match: ${5:# Configure flexible flow statistics template keywords}
    collect_counter: ${6:# Configure the number of packets and bytes that are included in the flexible flow statistics sent to NSC}
    collect_interface: ${7:# Configure the input or output interface that are included in the flexible flow statistics sent to NSC}
    description: ${8:# Configure the description of netstream record}
  tags:
    - community
    - network
    - ce_netstream_template]]></content>
  <tabTrigger>community.network.ce_netstream_template</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages NetStream template configuration on HUAWEI CloudEngine switches</description>
</snippet>