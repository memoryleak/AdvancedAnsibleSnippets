<snippet>
  <content><![CDATA[- name: ${1:Bidirectional Forwarding Detection (BFD) global-level configuration}
  cisco.nxos.nxos_bfd_global:
    echo_interface: ${2:# Loopback interface used for echo frames}
    echo_rx_interval: ${3:# BFD Echo receive interval in milliseconds}
    interval: ${4:# BFD interval timer values}
    slow_timer: ${5:# BFD slow rate timer in milliseconds}
    startup_timer: ${6:# BFD delayed startup timer in seconds}
    ipv4_echo_rx_interval: ${7:# BFD IPv4 session echo receive interval in milliseconds}
    ipv4_interval: ${8:# BFD IPv4 interval timer values}
    ipv4_slow_timer: ${9:# BFD IPv4 slow rate timer in milliseconds}
    ipv6_echo_rx_interval: ${10:# BFD IPv6 session echo receive interval in milliseconds}
    ipv6_interval: ${11:# BFD IPv6 interval timer values}
    ipv6_slow_timer: ${12:# BFD IPv6 slow rate timer in milliseconds}
    fabricpath_interval: ${13:# BFD fabricpath interval timer values}
    fabricpath_slow_timer: ${14:# BFD fabricpath slow rate timer in milliseconds}
    fabricpath_vlan: ${15:# BFD fabricpath control vlan}
  tags:
    - cisco
    - nxos
    - nxos_bfd_global]]></content>
  <tabTrigger>cisco.nxos.nxos_bfd_global</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Bidirectional Forwarding Detection (BFD) global-level configuration</description>
</snippet>