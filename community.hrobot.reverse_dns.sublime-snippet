<snippet>
  <content><![CDATA[- name: ${1:Set or remove reverse DNS entry for IP}
  community.hrobot.reverse_dns:
    hetzner_password: ${2:# The password for the Robot web-service user}
    hetzner_user: ${3:# The username for the Robot web-service user}
    ip: ${4:# The IP address to set or remove a reverse DNS entry for}
    state: ${5:# Whether to set or update (V(present)) or delete (V(absent)) the reverse DNS entry for O(ip)}
    value: ${6:# The reverse DNS entry for O(ip)}
  tags:
    - community
    - hrobot
    - reverse_dns]]></content>
  <tabTrigger>community.hrobot.reverse_dns</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Set or remove reverse DNS entry for IP</description>
</snippet>