<snippet>
  <content><![CDATA[- name: ${1:Manage filesystem on Unity storage system}
  dellemc.unity.filesystem:
    access_policy: ${2:# Access policy of a filesystem}
    cap_unit: ${3:# The unit of the filesystem size}
    data_reduction: ${4:# Boolean variable, specifies whether or not to enable compression}
    description: ${5:# Description about the filesystem}
    filesystem_id: ${6:# The id of the filesystem}
    filesystem_name: ${7:# The name of the filesystem}
    is_thin: ${8:# Boolean variable, specifies whether or not it is a thin filesystem}
    locking_policy: ${9:# File system locking policies}
    nas_server_id: ${10:# ID of the NAS server on which filesystem will be hosted}
    nas_server_name: ${11:# Name of the NAS server on which filesystem will be hosted}
    password: ${12:# The password of the Unity management server}
    pool_id: ${13:# This is the ID of the pool where the filesystem will be created}
    pool_name: ${14:# This is the name of the pool where the filesystem will be created}
    port: ${15:# Port number through which communication happens with Unity management server}
    quota_config: ${16:# Configuration for quota management}
    replication_params: ${17:# Settings required for enabling or modifying replication}
    replication_state: ${18:# State of the replication}
    size: ${19:# The size of the filesystem}
    smb_properties: ${20:# Advance settings for SMB}
    snap_schedule_id: ${21:# This is the id of an existing snapshot schedule which is to be associated with the filesystem}
    snap_schedule_name: ${22:# This is the name of an existing snapshot schedule which is to be associated with the filesystem}
    state: ${23:# State variable to determine whether filesystem will exist or not}
    supported_protocols: ${24:# Protocols supported by the file system}
    tiering_policy: ${25:# Tiering policy choices for how the storage resource data will be distributed among the tiers available in the pool}
    unispherehost: ${26:# IP or FQDN of the Unity management server}
    username: ${27:# The username of the Unity management server}
    validate_certs: ${28:# Boolean variable to specify whether or not to validate SSL certificate}
  tags:
    - dellemc
    - unity
    - filesystem]]></content>
  <tabTrigger>dellemc.unity.filesystem</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage filesystem on Unity storage system</description>
</snippet>