<snippet>
  <content><![CDATA[- name: ${1:Manage encap ranges assigned to pools (fvns:EncapBlk and fvns:VsanEncapBlk)}
  cisco.aci.aci_encap_pool_range:
    allocation_mode: ${2:# The method used for allocating encaps to resources}
    annotation: ${3:# User-defined string for annotating an object}
    certificate_name: ${4:# The X}
    description: ${5:# Description for the pool range}
    host: ${6:# IP Address or hostname of APIC resolvable by Ansible control host}
    name_alias: ${7:# The alias for the current object}
    output_level: ${8:# Influence the output of this ACI module}
    output_path: ${9:# Path to a file that will be used to dump the ACI JSON configuration objects generated by the module}
    password: ${10:# The password to use for authentication}
    pool: ${11:# The name of the pool that the range should be assigned to}
    pool_allocation_mode: ${12:# The method used for allocating encaps to resources}
    pool_type: ${13:# The encap type of C(pool)}
    port: ${14:# Port number to be used for REST connection}
    private_key: ${15:# Either a PEM-formatted private key file or the private key content used for signature-based authentication}
    range_end: ${16:# The end of encap range}
    range_name: ${17:# The name to give to the encap range}
    range_start: ${18:# The start of the encap range}
    state: ${19:# Use C(present) or C(absent) for adding or removing}
    suppress_previous: ${20:# If C(true), a GET to check previous will not be sent before a POST update to APIC}
    suppress_verification: ${21:# If C(true), a verifying GET will not be sent after a POST update to APIC}
    timeout: ${22:# The socket level timeout in seconds}
    use_proxy: ${23:# If C(false), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_ssl: ${24:# If C(false), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${25:# The username to use for authentication}
    validate_certs: ${26:# If C(false), SSL certificates will not be validated}
  tags:
    - cisco
    - aci
    - aci_encap_pool_range]]></content>
  <tabTrigger>cisco.aci.aci_encap_pool_range</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage encap ranges assigned to pools (fvns:EncapBlk and fvns:VsanEncapBlk)</description>
</snippet>