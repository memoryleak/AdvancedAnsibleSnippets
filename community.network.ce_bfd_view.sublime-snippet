<snippet>
  <content><![CDATA[- name: ${1:Manages BFD session view configuration on HUAWEI CloudEngine devices}
  community.network.ce_bfd_view:
    admin_down: ${2:# Enables the BFD session to enter the AdminDown state}
    description: ${3:# Specifies the description of a BFD session}
    detect_multi: ${4:# Specifies the local detection multiplier of a BFD session}
    local_discr: ${5:# Specifies the local discriminator of a BFD session}
    min_rx_interval: ${6:# Specifies the minimum interval for sending BFD packets}
    min_tx_interval: ${7:# Specifies the minimum interval for receiving BFD packets}
    remote_discr: ${8:# Specifies the remote discriminator of a BFD session}
    session_name: ${9:# Specifies the name of a BFD session}
    state: ${10:# Determines whether the config should be present or not on the device}
    tos_exp: ${11:# Specifies a priority for BFD control packets}
    wtr_interval: ${12:# Specifies the WTR time of a BFD session}
  tags:
    - community
    - network
    - ce_bfd_view]]></content>
  <tabTrigger>community.network.ce_bfd_view</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages BFD session view configuration on HUAWEI CloudEngine devices</description>
</snippet>