<snippet>
  <content><![CDATA[- name: ${1:Device compliance report for devices managed in OpenManage Enterprise}
  dellemc.openmanage.ome_configuration_compliance_info:
    baseline: ${2:# The name of the created baseline}
    ca_path: ${3:# The Privacy Enhanced Mail (PEM) file that contains a CA certificate to be used for the validation}
    device_id: ${4:# The ID of the target device which is associated with the I(baseline)}
    device_service_tag: ${5:# The device service tag of the target device associated with the I(baseline)}
    hostname: ${6:# OpenManage Enterprise IP address or hostname}
    password: ${7:# OpenManage Enterprise password}
    port: ${8:# OpenManage Enterprise HTTPS port}
    timeout: ${9:# The socket level timeout in seconds}
    username: ${10:# OpenManage Enterprise username}
    validate_certs: ${11:# If C(false), the SSL certificates will not be validated}
    x_auth_token: ${12:# Authentication token}
  tags:
    - dellemc
    - openmanage
    - ome_configuration_compliance_info]]></content>
  <tabTrigger>dellemc.openmanage.ome_configuration_compliance_info</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Device compliance report for devices managed in OpenManage Enterprise</description>
</snippet>