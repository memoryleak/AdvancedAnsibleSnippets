<snippet>
  <content><![CDATA[- name: ${1:NetApp Cloud Manager Aggregate}
  netapp.cloudmanager.na_cloudmanager_aggregate:
    capacity_tier: ${2:# The aggregate's capacity tier for tiering cold data to object storage}
    client_id: ${3:# The connector ID of the Cloud Manager Connector}
    disk_size_size: ${4:# The required size of the disks}
    disk_size_unit: ${5:# The disk size unit ['GB' or 'TB']}
    environment: ${6:# The environment for NetApp Cloud Manager API operations}
    feature_flags: ${7:# Enable or disable a new feature}
    home_node: ${8:# The home node that the new aggregate should belong to}
    iops: ${9:# Provisioned IOPS}
    name: ${10:# The name of the new aggregate}
    number_of_disks: ${11:# The required number of disks in the new aggregate}
    provider_volume_type: ${12:# The cloud provider volume type}
    refresh_token: ${13:# The refresh token for NetApp Cloud Manager API operations}
    sa_client_id: ${14:# The service account secret client ID for NetApp Cloud Manager API operations}
    sa_secret_key: ${15:# The service account secret key for NetApp Cloud Manager API operations}
    state: ${16:# Whether the specified aggregate should exist or not}
    throughput: ${17:# Unit is Mb/s}
    working_environment_id: ${18:# The public ID of the working environment where the aggregate will be created}
    working_environment_name: ${19:# The working environment name where the aggregate will be created}
  tags:
    - netapp
    - cloudmanager
    - na_cloudmanager_aggregate]]></content>
  <tabTrigger>netapp.cloudmanager.na_cloudmanager_aggregate</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp Cloud Manager Aggregate</description>
</snippet>