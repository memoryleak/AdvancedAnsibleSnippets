<snippet>
  <content><![CDATA[- name: ${1:Manage Azure DevTest Lab Custom Image instance}
  azure.azcollection.azure_rm_devtestlabcustomimage:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${6:# Controls the source of the credentials to use for authentication}
    author: ${7:# The author of the custom image}
    cert_validation_mode: ${8:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${9:# Azure client ID}
    cloud_environment: ${10:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    description: ${11:# The description of the custom image}
    lab_name: ${12:# The name of the lab}
    linux_os_state: ${13:# The state of the Linux OS}
    log_mode: ${14:# Parent argument}
    log_path: ${15:# Parent argument}
    name: ${16:# The name of the custom image}
    password: ${17:# Active Directory user password}
    profile: ${18:# Security profile found in ~/}
    resource_group: ${19:# The name of the resource group}
    secret: ${20:# Azure client secret}
    source_vm: ${21:# Source DevTest Lab virtual machine name}
    state: ${22:# Assert the state of the Custom Image}
    subscription_id: ${23:# Your Azure subscription Id}
    tags: ${24:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${25:# Azure tenant ID}
    thumbprint: ${26:# The thumbprint of the private key specified in I(x509_certificate_path)}
    windows_os_state: ${27:# The state of the Windows OS}
    x509_certificate_path: ${28:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_devtestlabcustomimage]]></content>
  <tabTrigger>azure.azcollection.azure_rm_devtestlabcustomimage</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure DevTest Lab Custom Image instance</description>
</snippet>