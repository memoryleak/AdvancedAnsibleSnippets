<snippet>
  <content><![CDATA[- name: ${1:Manage services with nosh}
  community.general.nosh:
    enabled: ${2:# Enable or disable the service, independently of C(*}
    name: ${3:# Name of the service to manage}
    preset: ${4:# Enable or disable the service according to local preferences in C(*}
    state: ${5:# V(started)/V(stopped) are idempotent actions that will not run commands unless necessary}
    user: ${6:# Run system-control talking to the calling user's service manager, rather than the system-wide service manager}
  tags:
    - community
    - general
    - nosh]]></content>
  <tabTrigger>community.general.nosh</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage services with nosh</description>
</snippet>