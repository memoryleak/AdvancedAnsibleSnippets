<snippet>
  <content><![CDATA[- name: ${1:This module configures and manages provisioning policies on IBM Spectrum Virtualize family storage systems}
  ibm.spectrum_virtualize.ibm_sv_manage_provisioning_policy:
    clustername: ${2:# The hostname or management IP of the Spectrum Virtualize storage system}
    domain: ${3:# Domain for the Spectrum Virtualize storage system}
    username: ${4:# REST API username for the Spectrum Virtualize storage system}
    password: ${5:# REST API password for the Spectrum Virtualize storage system}
    token: ${6:# The authentication token to verify a user on the Spectrum Virtualize storage system}
    log_path: ${7:# Path of debug log file}
    state: ${8:# Creates, updates (C(present)), or deletes (C(absent)) a provisioning policy}
    name: ${9:# Specifies the name of the provisioning policy}
    capacitysaving: ${10:# Specifies the policy capacity savings}
    deduplicated: ${11:# Specifies when volumes should be deduplicated}
    old_name: ${12:# Specifies the old name of the provisioning policy during renaming}
    validate_certs: ${13:# Validates certification}
  tags:
    - ibm
    - spectrum_virtualize
    - ibm_sv_manage_provisioning_policy]]></content>
  <tabTrigger>ibm.spectrum_virtualize.ibm_sv_manage_provisioning_policy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>This module configures and manages provisioning policies on IBM Spectrum Virtualize family storage systems</description>
</snippet>