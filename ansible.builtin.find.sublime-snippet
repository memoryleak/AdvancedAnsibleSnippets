<snippet>
  <content><![CDATA[- name: ${1:Return a list of files based on specific criteria}
  ansible.builtin.find:
    age: ${2:# Select files whose age is equal to or greater than the specified time}
    age_stamp: ${3:# Choose the file property against which we compare age}
    contains: ${4:# A regular expression or pattern which should be matched against the file content}
    depth: ${5:# Set the maximum number of levels to descend into}
    exact_mode: ${6:# Restrict mode matching to exact matches only, and not as a minimum set of permissions to match}
    excludes: ${7:# One or more (shell or regex) patterns, which type is controlled by O(use_regex) option}
    file_type: ${8:# Type of file to select}
    follow: ${9:# Set this to V(true) to follow symlinks in path for systems with python 2}
    get_checksum: ${10:# Set this to V(true) to retrieve a file's SHA1 checksum}
    hidden: ${11:# Set this to V(true) to include hidden files, otherwise they will be ignored}
    mode: ${12:# Choose objects matching a specified permission}
    paths: ${13:# List of paths of directories to search}
    patterns: ${14:# One or more (shell or regex) patterns, which type is controlled by O(use_regex) option}
    read_whole_file: ${15:# When doing a C(contains) search, determines whether the whole file should be read into memory or if the regex should be applied to the file line-by-line}
    recurse: ${16:# If target is a directory, recursively descend into the directory looking for files}
    size: ${17:# Select files whose size is equal to or greater than the specified size}
    use_regex: ${18:# If V(false), the patterns are file globs (shell)}
  tags:
    - find]]></content>
  <tabTrigger>ansible.builtin.find</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Return a list of files based on specific criteria</description>
</snippet>