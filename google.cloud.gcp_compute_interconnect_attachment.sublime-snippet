<snippet>
  <content><![CDATA[- name: ${1:Creates a GCP InterconnectAttachment}
  google.cloud.gcp_compute_interconnect_attachment:
    access_token: ${2:# An OAuth2 access token if credential type is accesstoken}
    admin_enabled: ${3:# Whether the VLAN attachment is enabled or disabled}
    auth_kind: ${4:# The type of credential used}
    bandwidth: ${5:# Provisioned bandwidth capacity for the interconnect attachment}
    candidate_subnets: ${6:# Up to 16 candidate prefixes that can be used to restrict the allocation of cloudRouterIpAddress and customerRouterIpAddress for this attachment}
    description: ${7:# An optional description of this resource}
    edge_availability_domain: ${8:# Desired availability domain for the attachment}
    encryption: ${9:# Indicates the user-supplied encryption option of this interconnect attachment: NONE is the default value, which means that the attachment carries unencrypted traffic}
    env_type: ${10:# Specifies which Ansible environment you're running this module within}
    interconnect: ${11:# URL of the underlying Interconnect object that this attachment's traffic will traverse through}
    ipsec_internal_addresses: ${12:# URL of addresses that have been reserved for the interconnect attachment, Used only for interconnect attachment that has the encryption option as IPSEC}
    mtu: ${13:# Maximum Transmission Unit (MTU), in bytes, of packets passing through this interconnect attachment}
    name: ${14:# Name of the resource}
    project: ${15:# The Google Cloud Platform project to use}
    region: ${16:# Region where the regional interconnect attachment resides}
    router: ${17:# URL of the cloud router to be used for dynamic routing}
    scopes: ${18:# Array of scopes to be used}
    service_account_contents: ${19:# The contents of a Service Account JSON file, either in a dictionary or as a JSON string that represents it}
    service_account_email: ${20:# An optional service account email address if machineaccount is selected and the user does not wish to use the default email}
    service_account_file: ${21:# The path of a Service Account JSON file if serviceaccount is selected as type}
    state: ${22:# Whether the given object should exist in GCP}
    type: ${23:# The type of InterconnectAttachment you wish to create}
    vlan_tag8021q: ${24:# The IEEE 802}
  tags:
    - google
    - cloud
    - gcp_compute_interconnect_attachment]]></content>
  <tabTrigger>google.cloud.gcp_compute_interconnect_attachment</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Creates a GCP InterconnectAttachment</description>
</snippet>