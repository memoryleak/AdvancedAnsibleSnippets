<snippet>
  <content><![CDATA[- name: ${1:Create, update or destroy reverse_proxy exception entry in Sophos UTM}
  community.general.utm_proxy_exception:
    name: ${2:# The name of the object}
    op: ${3:# The operand to be used with the entries of the path parameter}
    path: ${4:# The paths the exception in the reverse proxy is defined for}
    skip_custom_threats_filters: ${5:# A list of threats to be skipped}
    skip_threats_filter_categories: ${6:# Define which categories of threats are skipped}
    skipav: ${7:# Skip the Antivirus Scanning}
    skipbadclients: ${8:# Block clients with bad reputation}
    skipcookie: ${9:# Skip the Cookie Signing check}
    skipform: ${10:# Enable form hardening}
    skipform_missingtoken: ${11:# Enable form hardening with missing tokens}
    skiphtmlrewrite: ${12:# Protection against SQL}
    skiptft: ${13:# Enable true file type control}
    skipurl: ${14:# Enable static URL hardening}
    source: ${15:# Define which categories of threats are skipped}
    status: ${16:# Status of the exception rule set}
    headers: ${17:# A dictionary of additional headers to be sent to POST and PUT requests}
    utm_host: ${18:# The REST Endpoint of the Sophos UTM}
    utm_port: ${19:# The port of the REST interface}
    utm_token: ${20:# The token used to identify at the REST-API}
    utm_protocol: ${21:# The protocol of the REST Endpoint}
    validate_certs: ${22:# Whether the REST interface's ssl certificate should be verified or not}
    state: ${23:# The desired state of the object}
  tags:
    - community
    - general
    - utm_proxy_exception]]></content>
  <tabTrigger>community.general.utm_proxy_exception</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Create, update or destroy reverse_proxy exception entry in Sophos UTM</description>
</snippet>