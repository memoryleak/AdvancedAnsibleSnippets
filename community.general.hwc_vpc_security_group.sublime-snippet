<snippet>
  <content><![CDATA[- name: ${1:Creates a resource of Vpc/SecurityGroup in Huawei Cloud}
  community.general.hwc_vpc_security_group:
    state: ${2:# Whether the given object should exist in Huawei Cloud}
    name: ${3:# Specifies the security group name}
    enterprise_project_id: ${4:# Specifies the enterprise project ID}
    vpc_id: ${5:# Specifies the resource ID of the VPC to which the security group belongs}
    identity_endpoint: ${6:# The Identity authentication URL}
    user: ${7:# The user name to login with (currently only user names are supported, and not user IDs)}
    password: ${8:# The password to login with}
    domain: ${9:# The name of the Domain to scope to (Identity v3)}
    project: ${10:# The name of the Tenant (Identity v2) or Project (Identity v3)}
    region: ${11:# The region to which the project belongs}
    id: ${12:# The id of resource to be managed}
  tags:
    - community
    - general
    - hwc_vpc_security_group]]></content>
  <tabTrigger>community.general.hwc_vpc_security_group</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Creates a resource of Vpc/SecurityGroup in Huawei Cloud</description>
</snippet>