<snippet>
  <content><![CDATA[- name: ${1:Retrieves process IDs list if the process is running otherwise return empty list}
  community.general.pids:
    name: ${2:# The name of the process(es) you want to get PID(s) for}
    pattern: ${3:# The pattern (regular expression) to match the process(es) you want to get PID(s) for}
    ignore_case: ${4:# Ignore case in pattern if using the I(pattern) option}
  tags:
    - community
    - general
    - pids]]></content>
  <tabTrigger>community.general.pids</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Retrieves process IDs list if the process is running otherwise return empty list</description>
</snippet>