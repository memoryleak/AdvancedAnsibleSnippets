<snippet>
  <content><![CDATA[- name: ${1:Manages accounts on Apache CloudStack based clouds}
  ngine_io.cloudstack.cs_account:
    account_type: ${2:# Type of the account}
    api_http_method: ${3:# HTTP method used to query the API endpoint}
    api_key: ${4:# API key of the CloudStack API}
    api_secret: ${5:# Secret key of the CloudStack API}
    api_timeout: ${6:# HTTP timeout in seconds}
    api_url: ${7:# URL of the CloudStack API e}
    api_verify_ssl_cert: ${8:# Verify CA authority cert file}
    domain: ${9:# Domain the account is related to}
    email: ${10:# Email of the user to be created if account did not exist}
    first_name: ${11:# First name of the user to be created if account did not exist}
    last_name: ${12:# Last name of the user to be created if account did not exist}
    ldap_domain: ${13:# Name of the LDAP group or OU to bind}
    ldap_type: ${14:# Type of the ldap name}
    name: ${15:# Name of account}
    network_domain: ${16:# Network domain of the account}
    password: ${17:# Password of the user to be created if account did not exist}
    poll_async: ${18:# Poll async jobs until job has finished}
    role: ${19:# Creates the account under the specified role name or id}
    state: ${20:# State of the account}
    timezone: ${21:# Timezone of the user to be created if account did not exist}
    username: ${22:# Username of the user to be created if account did not exist}
    validate_certs: ${23:# If V(false), SSL certificates will not be validated}
  tags:
    - ngine_io
    - cloudstack
    - cs_account]]></content>
  <tabTrigger>ngine_io.cloudstack.cs_account</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages accounts on Apache CloudStack based clouds</description>
</snippet>