<snippet>
  <content><![CDATA[- name: ${1:Manage MX appliance layer 7 firewalls in the Meraki cloud}
  cisco.meraki.meraki_mx_l7_firewall:
    state: ${2:# Query or modify a firewall rule}
    net_name: ${3:# Name of network which MX firewall is in}
    net_id: ${4:# ID of network which MX firewall is in}
    rules: ${5:# List of layer 7 firewall rules}
    categories: ${6:# When C(True), specifies that applications and application categories should be queried instead of firewall rules}
    auth_key: ${7:# Authentication key provided by the dashboard}
    host: ${8:# Hostname for Meraki dashboard}
    use_proxy: ${9:# If C(no), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_https: ${10:# If C(no), it will use HTTP}
    output_format: ${11:# Instructs module whether response keys should be snake case (ex}
    output_level: ${12:# Set amount of debug output during module execution}
    timeout: ${13:# Time to timeout for HTTP requests}
    validate_certs: ${14:# Whether to validate HTTP certificates}
    org_name: ${15:# Name of organization}
    org_id: ${16:# ID of organization}
    rate_limit_retry_time: ${17:# Number of seconds to retry if rate limiter is triggered}
    internal_error_retry_time: ${18:# Number of seconds to retry if server returns an internal server error}
  tags:
    - cisco
    - meraki
    - meraki_mx_l7_firewall]]></content>
  <tabTrigger>cisco.meraki.meraki_mx_l7_firewall</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage MX appliance layer 7 firewalls in the Meraki cloud</description>
</snippet>