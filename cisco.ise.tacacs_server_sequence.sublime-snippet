<snippet>
  <content><![CDATA[- name: ${1:Resource module for TACACS Server Sequence}
  cisco.ise.tacacs_server_sequence:
    description: ${2:# TACACS Server Sequence's description}
    id: ${3:# TACACS Server Sequence's id}
    localAccounting: ${4:# LocalAccounting flag}
    name: ${5:# TACACS Server Sequence's name}
    prefixDelimiter: ${6:# The delimiter that will be used for prefix strip}
    prefixStrip: ${7:# Define if a delimiter will be used for prefix strip}
    remoteAccounting: ${8:# RemoteAccounting flag}
    serverList: ${9:# The names of TACACS external servers separated by commas}
    suffixDelimiter: ${10:# The delimiter that will be used for suffix strip}
    suffixStrip: ${11:# Define if a delimiter will be used for suffix strip}
    ise_hostname: ${12:# The Identity Services Engine hostname}
    ise_username: ${13:# The Identity Services Engine username to authenticate}
    ise_password: ${14:# The Identity Services Engine password to authenticate}
    ise_verify: ${15:# Flag to enable or disable SSL certificate verification}
    ise_version: ${16:# Informs the SDK which version of Identity Services Engine to use}
    ise_wait_on_rate_limit: ${17:# Flag for Identity Services Engine SDK to enable automatic rate-limit handling}
    ise_debug: ${18:# Flag for Identity Services Engine SDK to enable debugging}
    ise_uses_api_gateway: ${19:# Flag that informs the SDK whether to use the Identity Services Engine's API Gateway to send requests}
    ise_uses_csrf_token: ${20:# Flag that informs the SDK whether we send the CSRF token to ISE's ERS APIs}
  tags:
    - cisco
    - ise
    - tacacs_server_sequence]]></content>
  <tabTrigger>cisco.ise.tacacs_server_sequence</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Resource module for TACACS Server Sequence</description>
</snippet>