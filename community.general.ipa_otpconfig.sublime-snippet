<snippet>
  <content><![CDATA[- name: ${1:Manage FreeIPA OTP Configuration Settings}
  community.general.ipa_otpconfig:
    ipatokentotpauthwindow: ${2:# TOTP authentication window in seconds}
    ipatokentotpsyncwindow: ${3:# TOTP synchronization window in seconds}
    ipatokenhotpauthwindow: ${4:# HOTP authentication window in number of hops}
    ipatokenhotpsyncwindow: ${5:# HOTP synchronization window in hops}
    ipa_port: ${6:# Port of FreeIPA / IPA server}
    ipa_host: ${7:# IP or hostname of IPA server}
    ipa_user: ${8:# Administrative account used on IPA server}
    ipa_pass: ${9:# Password of administrative user}
    ipa_prot: ${10:# Protocol used by IPA server}
    validate_certs: ${11:# This only applies if C(ipa_prot) is I(https)}
    ipa_timeout: ${12:# Specifies idle timeout (in seconds) for the connection}
  tags:
    - community
    - general
    - ipa_otpconfig]]></content>
  <tabTrigger>community.general.ipa_otpconfig</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage FreeIPA OTP Configuration Settings</description>
</snippet>