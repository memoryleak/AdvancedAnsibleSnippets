<snippet>
  <content><![CDATA[- name: ${1:Manage NetApp Azure Files capacity pool}
  netapp.azure.azure_rm_netapp_capacity_pool:
    account_name: ${2:# The name of the NetApp account}
    ad_user: ${3:# Active Directory username}
    adfs_authority_url: ${4:# Azure AD authority url}
    api_profile: ${5:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${6:# Use to control if tags field is canonical or just appends to existing tags}
    auth_source: ${7:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${8:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${9:# Azure client ID}
    cloud_environment: ${10:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    location: ${11:# Resource location}
    log_mode: ${12:# Parent argument}
    log_path: ${13:# Parent argument}
    name: ${14:# The name of the capacity pool}
    password: ${15:# Active Directory user password}
    profile: ${16:# Security profile found in ~/}
    resource_group: ${17:# Name of the resource group}
    secret: ${18:# Azure client secret}
    service_level: ${19:# The service level of the file system}
    size: ${20:# Provisioned size of the pool (in chunks)}
    state: ${21:# State C(present) will check that the capacity pool exists with the requested configuration}
    subscription_id: ${22:# Your Azure subscription Id}
    tags: ${23:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${24:# Azure tenant ID}
  tags:
    - netapp
    - azure
    - azure_rm_netapp_capacity_pool]]></content>
  <tabTrigger>netapp.azure.azure_rm_netapp_capacity_pool</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage NetApp Azure Files capacity pool</description>
</snippet>