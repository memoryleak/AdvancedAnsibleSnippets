<snippet>
  <content><![CDATA[- name: ${1:Manage Azure Traffic Manager endpoint}
  azure.azcollection.azure_rm_trafficmanagerendpoint:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    auth_source: ${5:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${6:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${7:# Azure client ID}
    cloud_environment: ${8:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    disable_instance_discovery: ${9:# Determines whether or not instance discovery is performed when attempting to authenticate}
    enabled: ${10:# The status of the endpoint}
    geo_mapping: ${11:# The list of countries/regions mapped to this endpoint when traffic manager profile has routing_method of C(geographic)}
    location: ${12:# Specifies the location of the external or nested endpoints when using the 'Performance' traffic routing method}
    log_mode: ${13:# Parent argument}
    log_path: ${14:# Parent argument}
    min_child_endpoints: ${15:# The minimum number of endpoints that must be available in the child profile in order for the parent profile to be considered available}
    name: ${16:# The name of the endpoint}
    password: ${17:# Active Directory user password}
    priority: ${18:# The priority of this endpoint when traffic manager profile has routing_method of I(priority)}
    profile: ${19:# Security profile found in ~/}
    profile_name: ${20:# Name of Traffic Manager profile where this endpoints attaches to}
    resource_group: ${21:# Name of a resource group where the Traffic Manager endpoint exists or will be created}
    secret: ${22:# Azure client secret}
    state: ${23:# Assert the state of the Traffic Manager endpoint}
    subscription_id: ${24:# Your Azure subscription Id}
    target: ${25:# The fully-qualified DNS name of the endpoint}
    target_resource_id: ${26:# The Azure Resource URI of the of the endpoint}
    tenant: ${27:# Azure tenant ID}
    thumbprint: ${28:# The thumbprint of the private key specified in I(x509_certificate_path)}
    type: ${29:# The type of the endpoint}
    weight: ${30:# The weight of this endpoint when traffic manager profile has routing_method of C(weighted)}
    x509_certificate_path: ${31:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_trafficmanagerendpoint]]></content>
  <tabTrigger>azure.azcollection.azure_rm_trafficmanagerendpoint</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure Traffic Manager endpoint</description>
</snippet>