<snippet>
  <content><![CDATA[- name: ${1:Add/Remove COE cluster from OpenStack Cloud}
  openstack.cloud.coe_cluster:
    cluster_template_id: ${2:# The template ID of cluster template}
    discovery_url: ${3:# Url used for cluster node discovery}
    docker_volume_size: ${4:# The size in GB of the docker volume}
    flavor_id: ${5:# The flavor of the minion node for this ClusterTemplate}
    keypair: ${6:# Name of the keypair to use}
    labels: ${7:# One or more key/value pairs}
    master_flavor_id: ${8:# The flavor of the master node for this ClusterTemplate}
    master_count: ${9:# The number of master nodes for this cluster}
    name: ${10:# Name that has to be given to the cluster template}
    node_count: ${11:# The number of nodes for this cluster}
    state: ${12:# Indicate desired state of the resource}
    timeout: ${13:# Timeout for creating the cluster in minutes}
    cloud: ${14:# Named cloud or cloud config to operate against}
    auth: ${15:# Dictionary containing auth information as needed by the cloud's auth plugin strategy}
    auth_type: ${16:# Name of the auth plugin to use}
    region_name: ${17:# Name of the region}
    wait: ${18:# Should ansible wait until the requested resource is complete}
    api_timeout: ${19:# How long should the socket layer wait before timing out for API calls}
    validate_certs: ${20:# Whether or not SSL API requests should be verified}
    ca_cert: ${21:# A path to a CA Cert bundle that can be used as part of verifying SSL API requests}
    client_cert: ${22:# A path to a client certificate to use as part of the SSL transaction}
    client_key: ${23:# A path to a client key to use as part of the SSL transaction}
    interface: ${24:# Endpoint URL type to fetch from the service catalog}
    availability_zone: ${25:# Ignored}
    sdk_log_path: ${26:# Path to the logfile of the OpenStackSDK}
    sdk_log_level: ${27:# Log level of the OpenStackSDK}
  tags:
    - openstack
    - cloud
    - coe_cluster]]></content>
  <tabTrigger>openstack.cloud.coe_cluster</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Add/Remove COE cluster from OpenStack Cloud</description>
</snippet>