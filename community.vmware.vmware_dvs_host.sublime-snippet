<snippet>
  <content><![CDATA[- name: ${1:Add or remove a host from distributed virtual switch}
  community.vmware.vmware_dvs_host:
    esxi_hostname: ${2:# The ESXi hostname}
    hostname: ${3:# The hostname or IP address of the vSphere vCenter or ESXi server}
    lag_uplinks: ${4:# The ESXi hosts vmnics to use with specific LAGs}
    password: ${5:# The password of the vSphere vCenter or ESXi server}
    port: ${6:# The port number of the vSphere vCenter or ESXi server}
    proxy_host: ${7:# Address of a proxy that will receive all HTTPS requests and relay them}
    proxy_port: ${8:# Port of the HTTP proxy that will receive all HTTPS requests and relay them}
    state: ${9:# If the host should be present or absent attached to the vSwitch}
    switch_name: ${10:# The name of the Distributed vSwitch}
    username: ${11:# The username of the vSphere vCenter or ESXi server}
    validate_certs: ${12:# Allows connection when SSL certificates are not valid}
    vendor_specific_config: ${13:# List of key, value dictionaries for the Vendor Specific Configuration}
    vmnics: ${14:# The ESXi hosts vmnics to use with the Distributed vSwitch}
  tags:
    - community
    - vmware
    - vmware_dvs_host]]></content>
  <tabTrigger>community.vmware.vmware_dvs_host</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Add or remove a host from distributed virtual switch</description>
</snippet>