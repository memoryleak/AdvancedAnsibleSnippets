<snippet>
  <content><![CDATA[- name: ${1:Manage External Subnet objects (l3ext:Subnet)}
  cisco.aci.aci_l3out_extsubnet:
    aggregate: ${2:# Determines the Aggregate Routes for the Subnet}
    annotation: ${3:# User-defined string for annotating an object}
    certificate_name: ${4:# The X}
    description: ${5:# Description for the External Subnet}
    extepg: ${6:# Name of an existing ExtEpg}
    host: ${7:# IP Address or hostname of APIC resolvable by Ansible control host}
    l3out: ${8:# Name of an existing L3Out}
    name_alias: ${9:# The alias for the current object}
    network: ${10:# The network address for the Subnet}
    output_level: ${11:# Influence the output of this ACI module}
    output_path: ${12:# Path to a file that will be used to dump the ACI JSON configuration objects generated by the module}
    password: ${13:# The password to use for authentication}
    port: ${14:# Port number to be used for REST connection}
    private_key: ${15:# Either a PEM-formatted private key file or the private key content used for signature-based authentication}
    scope: ${16:# Determines the scope of the Subnet}
    state: ${17:# Use C(present) or C(absent) for adding or removing}
    subnet_name: ${18:# Name of External Subnet being created}
    suppress_previous: ${19:# If C(true), a GET to check previous will not be sent before a POST update to APIC}
    suppress_verification: ${20:# If C(true), a verifying GET will not be sent after a POST update to APIC}
    tenant: ${21:# Name of an existing tenant}
    timeout: ${22:# The socket level timeout in seconds}
    use_proxy: ${23:# If C(false), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    use_ssl: ${24:# If C(false), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${25:# The username to use for authentication}
    validate_certs: ${26:# If C(false), SSL certificates will not be validated}
  tags:
    - cisco
    - aci
    - aci_l3out_extsubnet]]></content>
  <tabTrigger>cisco.aci.aci_l3out_extsubnet</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage External Subnet objects (l3ext:Subnet)</description>
</snippet>