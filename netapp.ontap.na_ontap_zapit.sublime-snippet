<snippet>
  <content><![CDATA[- name: ${1:NetApp ONTAP Run any ZAPI on ONTAP}
  netapp.ontap.na_ontap_zapit:
    cert_filepath: ${2:# path to SSL client cert file (}
    feature_flags: ${3:# Enable or disable a new feature}
    hostname: ${4:# The hostname or IP address of the ONTAP instance}
    http_port: ${5:# Override the default port (80 or 443) with this port}
    https: ${6:# Enable and disable https}
    key_filepath: ${7:# path to SSL client key file}
    ontapi: ${8:# The ontap api version to use}
    password: ${9:# Password for the specified user}
    use_rest: ${10:# This module only support ZAPI and will can not be swtich to REST}
    username: ${11:# This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required}
    validate_certs: ${12:# If set to C(no), the SSL certificates will not be validated}
    vserver: ${13:# if provided, forces vserver tunneling}
    zapi: ${14:# A dictionary for the zapi and arguments}
  tags:
    - netapp
    - ontap
    - na_ontap_zapit]]></content>
  <tabTrigger>netapp.ontap.na_ontap_zapit</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp ONTAP Run any ZAPI on ONTAP</description>
</snippet>