<snippet>
  <content><![CDATA[- name: ${1:Manage configuration templates in the Meraki cloud}
  cisco.meraki.meraki_config_template:
    auth_key: ${2:# Authentication key provided by the dashboard}
    auto_bind: ${3:# Optional boolean indicating whether the network's switches should automatically bind to profiles of the same model}
    config_template: ${4:# Name of the configuration template within an organization to manipulate}
    host: ${5:# Hostname for Meraki dashboard}
    internal_error_retry_time: ${6:# Number of seconds to retry if server returns an internal server error}
    net_id: ${7:# ID of the network to bind or unbind configuration template to}
    net_name: ${8:# Name of the network to bind or unbind configuration template to}
    org_id: ${9:# ID of organization associated to a configuration template}
    org_name: ${10:# Name of organization containing the configuration template}
    output_format: ${11:# Instructs module whether response keys should be snake case (ex}
    output_level: ${12:# Set amount of debug output during module execution}
    rate_limit_retry_time: ${13:# Number of seconds to retry if rate limiter is triggered}
    state: ${14:# Specifies whether configuration template information should be queried, modified, or deleted}
    timeout: ${15:# Time to timeout for HTTP requests}
    use_https: ${16:# If C(no), it will use HTTP}
    use_proxy: ${17:# If C(no), it will not use a proxy, even if one is defined in an environment variable on the target hosts}
    validate_certs: ${18:# Whether to validate HTTP certificates}
  tags:
    - cisco
    - meraki
    - meraki_config_template]]></content>
  <tabTrigger>cisco.meraki.meraki_config_template</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage configuration templates in the Meraki cloud</description>
</snippet>