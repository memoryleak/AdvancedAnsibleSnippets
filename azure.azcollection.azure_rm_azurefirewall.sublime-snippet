<snippet>
  <content><![CDATA[- name: ${1:Manage Azure Firewall instance}
  azure.azcollection.azure_rm_azurefirewall:
    ad_user: ${2:# Active Directory username}
    adfs_authority_url: ${3:# Azure AD authority url}
    api_profile: ${4:# Selects an API profile to use when communicating with Azure services}
    append_tags: ${5:# Use to control if tags field is canonical or just appends to existing tags}
    application_rule_collections: ${6:# Collection of application rule collections used by Azure Firewall}
    auth_source: ${7:# Controls the source of the credentials to use for authentication}
    cert_validation_mode: ${8:# Controls the certificate validation behavior for Azure endpoints}
    client_id: ${9:# Azure client ID}
    cloud_environment: ${10:# For cloud environments other than the US public cloud, the environment name (as defined by Azure Python SDK, eg, C(AzureChinaCloud), C(AzureUSGovernment)), or a metadata discovery endpoint URL (required for Azure Stack)}
    ip_configurations: ${11:# IP configuration of the Azure Firewall resource}
    location: ${12:# Resource location}
    log_mode: ${13:# Parent argument}
    log_path: ${14:# Parent argument}
    name: ${15:# The name of the Azure Firewall}
    nat_rule_collections: ${16:# Collection of NAT rule collections used by Azure Firewall}
    network_rule_collections: ${17:# Collection of network rule collections used by Azure Firewall}
    password: ${18:# Active Directory user password}
    profile: ${19:# Security profile found in ~/}
    resource_group: ${20:# The name of the resource group}
    secret: ${21:# Azure client secret}
    state: ${22:# Assert the state of the AzureFirewall}
    subscription_id: ${23:# Your Azure subscription Id}
    tags: ${24:# Dictionary of string:string pairs to assign as metadata to the object}
    tenant: ${25:# Azure tenant ID}
    thumbprint: ${26:# The thumbprint of the private key specified in I(x509_certificate_path)}
    x509_certificate_path: ${27:# Path to the X509 certificate used to create the service principal in PEM format}
  tags:
    - azure
    - azcollection
    - azure_rm_azurefirewall]]></content>
  <tabTrigger>azure.azcollection.azure_rm_azurefirewall</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage Azure Firewall instance</description>
</snippet>