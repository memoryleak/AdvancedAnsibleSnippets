<snippet>
  <content><![CDATA[- name: ${1:Manage BIG-IP configuration sections}
  f5networks.f5_modules.bigip_config:
    merge_content: ${2:# Loads the specified configuration that you want to merge into the running configuration}
    provider: ${3:# A dict object containing connection details}
    reset: ${4:# Loads the default configuration on the device}
    save: ${5:# The C(save) argument instructs the module to save the running-config to startup-config}
    verify: ${6:# Validates the specified configuration to see whether it is valid to replace the running configuration}
  tags:
    - f5networks
    - f5_modules
    - bigip_config]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_config</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage BIG-IP configuration sections</description>
</snippet>