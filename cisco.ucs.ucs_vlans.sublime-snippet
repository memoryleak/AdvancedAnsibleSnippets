<snippet>
  <content><![CDATA[- name: ${1:Configures VLANs on Cisco UCS Manager}
  cisco.ucs.ucs_vlans:
    fabric: ${2:# The fabric configuration of the VLAN}
    hostname: ${3:# IP address or hostname of Cisco UCS Manager}
    id: ${4:# The unique string identifier assigned to the VLAN}
    multicast_policy: ${5:# The multicast policy associated with this VLAN}
    name: ${6:# The name assigned to the VLAN}
    native: ${7:# Designates the VLAN as a native VLAN}
    password: ${8:# Password for Cisco UCS Manager authentication}
    port: ${9:# Port number to be used during connection (by default uses 443 for https and 80 for http connection)}
    proxy: ${10:# If use_proxy is no, specfies proxy to be used for connection}
    sharing: ${11:# The Sharing Type field}
    state: ${12:# If C(present), will verify VLANs are present and will create if needed}
    use_proxy: ${13:# If C(no), will not use the proxy as defined by system environment variable}
    use_ssl: ${14:# If C(no), an HTTP connection will be used instead of the default HTTPS connection}
    username: ${15:# Username for Cisco UCS Manager authentication}
  tags:
    - cisco
    - ucs
    - ucs_vlans]]></content>
  <tabTrigger>cisco.ucs.ucs_vlans</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Configures VLANs on Cisco UCS Manager</description>
</snippet>