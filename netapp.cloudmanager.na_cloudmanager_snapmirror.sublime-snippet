<snippet>
  <content><![CDATA[- name: ${1:NetApp Cloud Manager SnapMirror}
  netapp.cloudmanager.na_cloudmanager_snapmirror:
    capacity_tier: ${2:# The volume capacity tier for tiering cold data to object storage}
    client_id: ${3:# The connector ID of the Cloud Manager Connector}
    destination_aggregate_name: ${4:# The aggregate in which the volume will be created}
    destination_svm_name: ${5:# The name of the destination SVM}
    destination_volume_name: ${6:# The name of the destination volume to be created for snapmirror relationship}
    destination_working_environment_id: ${7:# The public ID of the working environment of the destination volume}
    destination_working_environment_name: ${8:# The working environment name of the destination volume}
    environment: ${9:# The environment for NetApp Cloud Manager API operations}
    feature_flags: ${10:# Enable or disable a new feature}
    max_transfer_rate: ${11:# Maximum transfer rate limit KB/s}
    policy: ${12:# The SnapMirror policy name}
    provider_volume_type: ${13:# The underlying cloud provider volume type}
    refresh_token: ${14:# The refresh token for NetApp Cloud Manager API operations}
    sa_client_id: ${15:# The service account secret client ID for NetApp Cloud Manager API operations}
    sa_secret_key: ${16:# The service account secret key for NetApp Cloud Manager API operations}
    schedule: ${17:# The name of the Schedule}
    source_svm_name: ${18:# The name of the source SVM}
    source_volume_name: ${19:# The name of the source volume}
    source_working_environment_id: ${20:# The public ID of the working environment of the source volume}
    source_working_environment_name: ${21:# The working environment name of the source volume}
    state: ${22:# Whether the specified snapmirror relationship should exist or not}
    tenant_id: ${23:# The NetApp account ID that the Connector will be associated with}
  tags:
    - netapp
    - cloudmanager
    - na_cloudmanager_snapmirror]]></content>
  <tabTrigger>netapp.cloudmanager.na_cloudmanager_snapmirror</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>NetApp Cloud Manager SnapMirror</description>
</snippet>