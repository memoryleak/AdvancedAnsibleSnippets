<snippet>
  <content><![CDATA[- name: ${1:Manage IPSec policies on a BIG-IP}
  f5networks.f5_modules.bigip_ipsec_policy:
    auth_algorithm: ${2:# Specifies the algorithm to use for IKE authentication}
    description: ${3:# Description of the policy}
    encrypt_algorithm: ${4:# Specifies the algorithm to use for IKE encryption}
    ipcomp: ${5:# Specifies whether to use IPComp encapsulation}
    ipv4_interface: ${6:# When C(mode) is C(interface), indicates if the IPv4 C(any) address should be used}
    kb_lifetime: ${7:# Specifies the length of time before the IKE security association, in kilobytes}
    lifetime: ${8:# Specifies the length of time before the IKE security association expires, in minutes}
    mode: ${9:# Specifies the processing mode}
    name: ${10:# Specifies the name of the IPSec policy}
    partition: ${11:# Device partition to manage resources on}
    perfect_forward_secrecy: ${12:# Specifies the Diffie-Hellman group to use for IKE Phase 2 negotiation}
    protocol: ${13:# Specifies the IPsec protocol}
    provider: ${14:# A dict object containing connection details}
    route_domain: ${15:# Specifies the route domain, when C(interface) is selected for the C(mode) setting}
    state: ${16:# When C(present), ensures the resource exists}
    tunnel_local_address: ${17:# Specifies the local endpoint IP address of the IPsec tunnel}
    tunnel_remote_address: ${18:# Specifies the remote endpoint IP address of the IPsec tunnel}
  tags:
    - f5networks
    - f5_modules
    - bigip_ipsec_policy]]></content>
  <tabTrigger>f5networks.f5_modules.bigip_ipsec_policy</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manage IPSec policies on a BIG-IP</description>
</snippet>