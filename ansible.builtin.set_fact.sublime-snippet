<snippet>
  <content><![CDATA[- name: ${1:Set host variable(s) and fact(s)}
  ansible.builtin.set_fact:
    cacheable: ${2:# This boolean converts the variable into an actual 'fact' which will also be added to the fact cache}
    key_value: ${3:# The M(ansible}
  tags:
    - set_fact]]></content>
  <tabTrigger>ansible.builtin.set_fact</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Set host variable(s) and fact(s)</description>
</snippet>