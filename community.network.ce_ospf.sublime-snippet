<snippet>
  <content><![CDATA[- name: ${1:Manages configuration of an OSPF instance on HUAWEI CloudEngine switches}
  community.network.ce_ospf:
    process_id: ${2:# Specifies a process ID}
    area: ${3:# Specifies the area ID}
    addr: ${4:# Specifies the address of the network segment where the interface resides}
    mask: ${5:# IP network wildcard bits in decimal format between 0 and 32}
    auth_mode: ${6:# Specifies the authentication type}
    auth_text_simple: ${7:# Specifies a password for simple authentication}
    auth_key_id: ${8:# Authentication key id when C(auth_mode) is 'hmac-sha256', 'md5' or 'hmac-md5}
    auth_text_md5: ${9:# Specifies a password for MD5, HMAC-MD5, or HMAC-SHA256 authentication}
    nexthop_addr: ${10:# IPv4 address for configure next-hop address's weight}
    nexthop_weight: ${11:# Indicates the weight of the next hop}
    max_load_balance: ${12:# The maximum number of paths for forward packets over multiple paths}
    state: ${13:# Determines whether the config should be present or not on the device}
  tags:
    - community
    - network
    - ce_ospf]]></content>
  <tabTrigger>community.network.ce_ospf</tabTrigger>
  <scope>source.yaml,source.ansible</scope>
  <description>Manages configuration of an OSPF instance on HUAWEI CloudEngine switches</description>
</snippet>